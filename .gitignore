# Local-only DB schema snapshot (structure + row-count estimates).
# Regenerate from Supabase when the schema changes — never push to the public repo.
multiply-schema.json

# Local-only data dumps / archives — never push to the public repo.
# Pattern (not exact filename) so future dumps are covered too.
SCHEMA_DUMP_SECRET*
*.rar
