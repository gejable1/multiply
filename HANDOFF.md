# MULTIPLY — HANDOFF.md

**Last updated:** July 27, 2026 (**Session 82 -- the ledger told two lies and a normalizer was hiding both · the #400 ordering arc CLOSED end to end (poster re-keyed flat, ONE resolver in shared.js, `?v=13->14` across 22 consumers) · the template frozen behind a platform-admin gate · and the pathway FORK MODEL designed with the Pastor.** 4 PRs #258-#261 + migrations 095-097, ZERO rollbacks. **095/096** the S46 ledger (#212) was wrong in two ways at once: five migrations (078-082) ran with NO `schema_migrations` INSERT in their bodies at all (#402), and five rows from S52-S56 stored the full migration NAME as the version -- invisible because the pre-flight `lpad(version,3,'0')` TRUNCATES a longer string on the right and rendered them as tidy three-digit numbers (#401). Both repairs proven on PG16 from the exact pre-095 state, forward and on rerun; both verify blocks rewritten to assert only their own effects after the first pair asserted a GLOBAL row count that every future migration would invalidate (#403). **#259/097 + #260** the ordering arc: `order_map` re-keyed `level|category` -> `level`, the poster keeping its category headings while sorting each group by the FLAT level list; conversion of the one legacy row was REJECTED after PG16 showed it would hoist two books above `join-lc`/`usbong1`/`daily-devo` on every L0 member's phone. `moveRung` now RELOCATES rather than swaps -- a harness on the rendered flat list caught a swap flinging `join-lc` from first to last while the poster showed one rung moving one notch (#404). Then ONE resolver in `multiply_shared.js` (`pathwayOrder.load/forLevel/sort`, `?v=13->14`, 22 consumers) read by all four surfaces; the poster's duplicate `_sectionComparator` deleted, because two copies of a ranking rule IS #400 (#405). Poster order === tool order, asserted. **#261** the template frozen for the fork: a review notice plus Edit restricted to `is_platform_admin`, revealed only after the check passes so every failure path stays read-only (#406). **Design (no code):** the pathway FORK MODEL agreed with the Pastor -- template nobody can edit, each church owns a versioned copy, draft/publish, retire-never-delete, snapshots, pre-publish sanity check, lean starter, print/share, template updates by notification with per-item opt-in; plus a full 210-rung template review (34% of it is descriptors, not steps; Ministry empty at L2/L3/L5 because the roles were filed as competencies). Two new canonical docs: `PATHWAY_FORK_DESIGN.md`, `TEMPLATE_CLEANUP.md`. Three source-reading lapses recorded honestly (#407). Invariants #401-#407 → **count 407.**) _prior:_ July 26, 2026 (**Session 81 -- the `members` INSERT hole closed and a platform-admin lockdown found while proving it · the poster editor that silently un-wired auto rungs, and the engine bug that had been masking it · auto-completion v2's overlay resolver · the salvation diagnostic reweighted from accident to judgement, then its results panel made readable.** 5 PRs #252-#256 + migrations 092-094, ZERO rollbacks. **#252/092** any member could mint an L5 platform-admin row and claim it via `set_pin`; PG16 also surfaced `is_platform_admin` writable by all 12 tenant pastors -- the only gate on the shared BASE catalog (#394); the level rule moved policy->trigger because an INSERT WITH CHECK fires on the proposed row of an upsert (#393). **#253/093** `customizeBase()` dropped four base columns it never displayed (#395), un-wiring any retitled rung -- masked for weeks by v1's base-only join (#396); 2 rows repaired, 131 legitimate completions preserved. **#254/094** `v_effective_auto_rungs`, one resolver, matching on `auto_source_key`; 13 rows inserted, 108 = 9 x 12 churches. **#255** weights declared A18/B12/C22/D18/E12/F9/G9 (#397); **#256** the card read `136/170` beside `71%` until the Pastor caught it on his phone (#398). Ledger drift: 093 ran and sat uncommitted for three turns (#399). Also a long L0 evaluation session -- the Pastor corrected two of my three main critiques (every member already has a discipler and a Sunday huddle; Usbong is EVANGELISM over 10-16 weeks, not curriculum) and his gate placement stood: block L1->L2, not L0->L1. Invariants #393-#400 → **count 400.**) _prior:_ EOF
python3 -c "
for f in ['jumpstart.md','lastupdated.md']:
    b=open(f,'rb').read(); print(f,len(b),'non-ASCII',sum(1 for x in b if x>127))
" July 25, 2026 (**Session 80 -- the quiz-visibility arc CLOSED (grant-wiring + 090 guardrail + ghost-header hide) · the pathway-denominator hotfix + reference-rung coaching panel · facilitator delegation · the embed-modal off-screen fix · is_facilitator DERIVED from level (9 pastors unlocked) · and the flagship: MD migrated off legacy `checks` onto `pathway_rungs`.** 17 PRs (#235-#250) + migrations 088-091, ZERO rollbacks, ZERO forced gates -- every mismatch halted cleanly and got diagnosed. **#235/#236 + 088** conflict-safe + co-teacher attendance (shared.js `?v=12->13`, 22 consumers) + facilitator delegation (`lc_meeting_guide_shares` + `auth_can_facilitate_guide()`); **#239 + 089** delegation hotfix (attach `trg_set_church_id` -- 014 only covered then-existing tables). **#240** Meeting Prep echo guard. **#241 (PR-F)** the pathway-denominator hotfix -- both bars counted `published!==false` only, ignoring `trackable`, dividing ~5x too many since July 6 (#383). **#242/#243 (docs)** "Two Views of the Pathway" -> `GRACE_PATHWAY.md` + Pastor-manual chapter. **#244 (PR-J) + 090** quiz visibility WIRED to the single lesson access-grant resolver (#380) + a `lesson_id`-non-null-when-active CHECK (#381, closes #127's fail-open hole). **#245 (PR-K)** the reference-rung "Coaching notes" collapsed panel (MLT+MMT, display-only #383). **#246 (PR-L)** hide the whole quiz panel when zero visible quizzes (#382). **#247 (PR-M)** pin the pipeline poster edit modals to the viewport in embed mode -- they were opening off-screen when scrolled (#384; the "dead buttons, clean console" bug). **#248 (PR-N) + 091** is_facilitator DERIVED from pipeline_level via a trigger (L5->Pastor, L2-4->LC Leader; #386); a read-only both-directions diagnostic (#387) revealed 9 tenant PASTORS unflagged -> silently locked out of their own meeting-guide library (`auth_is_leader()` reads exactly is_facilitator #385) -- all fixed, zero surprise un-flags; MD hid the derived checkbox + repointed `isFac` to `#fl>=2` (caught a `_canEditLc` regression). **#249 (PR-O) the flagship** -- MD's member-modal Pathway tab + members-table Progress column + Pipeline-Distribution tiles all migrated off legacy `checks` onto `pathway_rungs`+`pathway_progress` via ONE church-wide cache loaded before renderAll (#388); full L0->L5, pastor marks any rung (RLS 057 allows L5-writes-any); the "0/24" blindness ends. PR-O mismatched WANT twice from an inline-paste anchor hand-edit (#391) -> delivered as a sha-verified upload; also surfaced the astral-emoji surrogate-pair crash (#389) and the emit-`\u`-as-literal-text-for-ASCII-output rule (#390). **#250 (PR-P) HOTFIX** -- PR-O left a bare `${lc.length}` in the members-table Progress cell while its compute line was swapped to `_pp`; a runtime `ReferenceError` on every row blanked the table (all gates passed because the harness unit-tested the helpers, never rendered the template #392); one-cell fix, members restored. Invariants #380-#392 -> **count 392.**) _prior:_ July 22, 2026 (**Session 79 -- the `app_sessions` engagement reader (MD church-wide + MLT "My Flock") · SVI Wave 5c Multiplication + the pathway auto-completion engine that made it honest (1/12 -> 11/12) · a three-leak tenancy hardening (301 -> 215) · the SVI guest trim (215 -> 211) · LACR monthly range + top-5 celebration.** PRs #226-#233 + migrations 083-087. **#226/#227** the engagement reader over the S78 `app_sessions` logbook (last-active / active-this-week / to-onboard; MD church-wide, MLT `?scope=mine`). **#228 + 083** the **Multiplication** metric (`disciples_advancing`, care-lens, opt-in L2+, "N of M disciples advanced a rung"). **#229 + 084** `auto_complete_pathway_rungs()` -- the 9 objective auto rungs (assessments/courses/streaks) tick themselves into `pathway_progress` with real dates (#375/#376/#377), idempotent, hooked ahead of the weekly compute; PG16-proven (#379); backfill moved Gerry 1/12 -> 11/12. **#230/085 + #231/086** sealed THREE allow-all RLS stubs the S46 sweep missed -- `svi_snapshots_read USING(true)` (the "301") + `interventions`/`ministry_intake` `"Enable all for anon"` (anon read/write) -- a write-only stub sweep afterward was EMPTY (#372). **#232 + 087** SVI excludes guests now (#373) + purged existing guest/test snapshots (#374) -> **211**, the true roster. **#233** LACR From/To monthly range + Top-5-groups celebration fallback. Invariants #372-#379 -> **count 379.**) _prior:_ July 22, 2026 (**Session 78 -- Family Huddle offline precache + the long-deferred S71 telemetry rebuild landed end-to-end: one unified `app_sessions` logbook, server-derived leader-ness, expired-session reaping, and `leader_sessions` DROPPED.** PRs #219-#224 + migrations 081, 082 - `multiply_shared.js` bumped `?v=11->12`. **#219** added `family_huddle_card.html` to the SW `SHELL_ASSETS` + `CACHE_VERSION` v3->v4 so the family-worship card works offline. **Telemetry rebuild (#220-#224):** **081** created `app_sessions` (every login, member+leader; tenant RLS; `reap_expired_sessions()`) + losslessly backfilled `leader_sessions`. **#221** `auth-login` now derives `isLeader` from `pipeline_level>=2` (the spoofable `body.stamp` fiction is dead #322/#366) + stamps every login + reaps. **#222** `token-login` stamps cold-link logins too (2h TTL). **#223** repointed `logoutLeader` + the MD pin-reset to `app_sessions` (`member_id`) + bumped all 21 shared.js consumers `?v=11->12` (JS is network-first so no `CACHE_VERSION` bump #369). **082** dropped `leader_sessions` after re-backfilling any late rows (#368). Verified LIVE via the `app_sessions` count tick (3221->3222 on a login #371). Invariants #365-#371 -> **count 371.**) _prior:_ July 21, 2026 (**Session 77 -- the Giving Mirror (a private, on-device giving calculator) - attendance shows ACTIVE batch enrollees only - the Ministry Recommender ported into MLT as an LCL care activity - the bilingual Family Huddle family-worship card, deployed to MMT and reflowed to open embedded.** PRs #213-#217, ZERO migrations, `multiply_shared.js` untouched. **#213** the **Giving Mirror** -- income in, percentage out vs the 10% tithe milestone + a one-page budget for margin, on the MMT Giving Journey card (L1+); 100% on-device (localStorage keyed by memberId + Clear), no server footprint at all (#363). Its modal shipped in-prompt, drifted on `\u` escape-flattening (20 bytes), HALTED at the WANT gate, and was reshipped as a sha-verified UPLOAD (#360). **#214** attendance pickers (GP/BTLI/Usbong) + their dropdown counts now filter `status==='active'` -- withdrawn/graduated members were reappearing in the checklist and inflating counts because `cohort_members` keeps them for the roster History (#364). **#215** **Ministry Fit** -- a per-member card in the MLT member detail that opens the SHARED `ministry_recommender.html` scoped by the LCL's own JWT (RLS); pure reuse, no AI duplicated. **#216** the **Family Huddle** card (a new standalone bilingual family-worship resource: the FAITH5+Discovery-Bible-Study rhythm, a worked example on Mark 10:13-16, and six passages) + an MMT Journey tile. **#217** reflowed the card from cramped print-pages into one roomy scrolling doc and ported `_openLessonViewerModal` into MMT so the tile opens it EMBEDDED (#362), not a new tab. Delivery: recursive sha-locate for uploads (#361); five clean back-checked PRs. Invariants #360-#364 -> **count 364.**) _prior:_ July 20, 2026 (**Session 76 -- Meeting Prep freshman-readability - the duplicate-name cure (an empty-only, server-guarded member delete) - the "Iba pa" check-in fix - the weekly attendance gate RESTORED after ~2 months silently gone - a SW cache bump.** PRs #207-#211 + migrations 079, 080. **#207** both Meeting Prep prompt builders now require freshman-HS-simple guides (EN+TL). **#208 + migration 079** `mlt_delete_empty_member` -- an empty-only, server-guarded delete for LCLs (SECURITY DEFINER, catalog-driven over every live FK into `members`, telemetry denylist, pre-clears NO-ACTION telemetry, refuses ANY formation footprint #355); button at the bottom of the member detail; proven on PG16 (5 tests) before shipping; typos are self-fixed by the disciple renaming in MMT. **#209 + migration 080** widened `lcg_checkins_reason_chk` to accept `'other'` -- the S75 "Iba pa" reason shipped the UI but not the CHECK (#290/#336 recurred #357); not a cache/install issue. **#210** RESTORED `runWeeklyAttendanceGate` (Inv #100), silently dropped in a May-28 whole-file upload and absent ~2 months (the doc still described it #356); restored from last-good `10971c8`, attendance-only (inbox owns announcements), once-per-open L2+, fail-soft; resolver smoke-tested 7/7. Its ~20 KB base64 block homoglyph-corrupted in transit -> CC HALTED -> reshipped as a sha-verified whole-file upload (#358). **#211** SW `CACHE_VERSION` `v2->v3-2026-07-20` (HTML/JS is network-first so online users already get fresh; the bump refreshes the offline shell #359). Invariants #355-#359 -> **count 359.**) _prior:_ July 19, 2026 (**Session 75 -- the Giving Journey gate arc (L1 lockstep + "theirs to close" at any level + cumulative per-member rung gate) - an "Iba pa" check-in reason - the lone CRLF blob renormalized (Invariant #3 restored) - and Meeting Prep's source generalized to devotional | BTLI | paste | upload, all client-side.** 8 PRs #198-#205 + migration 078 - `multiply_shared.js` untouched. **#198** L1-gated the MLT giving companion card to mirror MMT (#348) AND freed the L0 close-trap -- an already-open covenant now renders at any level so it stays the member's to close (#349); doctrine line trued up per #341. **#199** cumulative per-member rung gate -- an LCL affirms only rungs at/below each disciple's own level; higher rungs show locked "Opens at Level N"; MMT stays full aspiration (#350). **#200** an "Iba pa" (Others) check-in reason that requires its detail box before submitting + `CHECKIN_LABELS` taught the new code (#351). **#201** migration 078 committed (`source_kind`/`source_title`/`source_text` on `lc_meeting_guides`). **#202** renormalized the one CRLF blob `btli1_l3_participant.html` to LF -- codeload/`git show` are EOL-blind, so it was found + fixed via clone + `git cat-file blob` (#352, #3 restored). **#203-#205** Meeting Prep Bring-your-own-material: a source toggle (Devotional | Bring your own) with `mpBuildPromptCustom` under a Scripture-anchor guardrail (#354), a "Start from a BTLI lesson" pre-fill querying the `pipeline_lessons` catalog (RLS global-OR-own, overlay-precedence dedup #353), and `.txt`/`.md` upload -- all client-side. Juana's orphaned L0 covenant closed via one human-gated SQL. Invariants #348-#354 -> **count 354.**) _prior:_ July 17, 2026 (**Session 74 -- the AI-tailored salvation Debrief Guide, completed end-to-end: the LCL's guide now speaks in the member's own words · + the `.nojekyll` deploy cure and SW auto-purge · 6 PRs · ZERO migrations · 6 invariants.** PRs #191–#196. **#191 (MLT)** Zone 2 relabeled "Uncertain 🟡" (was a misleading "Stable & Growing 🔵") + Debrief/Intervention buttons for zones ≥2 + both as in-app modals (`openDebrief` → `facilitator_debrief_guide.html?id=…&embed=1`). **#192** AI-tailored debrief — Standard↔Tailored 🎯 pill; the BYO-AI panel emits a `tailored` map, AI resolves each tagged ask-box bracket from the member's own salvation `open_answers` under a hard no-verdict guardrail. **#193** parser hardening (curly quotes, trailing commas, unescaped inner double-quotes) + Past-Debriefs modal viewport-anchored in embed + "AI Assisted Debrief" rename. **#194** revert-fix (standard text moved from a quote-corrupting `data-std` attr to a JS `_stdAskText` map) + tailored mode persisted in `ai_parsed_json._guideMode` + auto-re-apply on resume. **#195** the completeness pass — the Pastor caught uneven tailoring: `diagnostic_results` stores only SECTION aggregates, never per-question scale scores, so `[their B1 score]`/`[lowest habit]` were un-fillable (#345) → A5 now references the member's basis-of-acceptance answer (`a-basis`), B repoints to the stored B2 answer (`b-basis`), the fruit box fills `[highest/lowest-rated fruit]` from `section_fruit_*` scores, E reworded, `[Name]` filled at load via `_fillPlaceholder` (leaf-only, #346); folded in fresh-visibility (auto-apply tailored on fresh parse + scroll-to-guide). **First full BASE→patcher→WANT delivery through CC** (#347). **#196** the deploy cure — after #195 merged the fix wouldn't show: `main` had it but the published `github-pages` deployment was an older commit (#343), because the "pages build and deployment" Action went red on `GET api.github.com … 503` (Jekyll's metadata plugin, run by default #342). Re-running published it; then **`.nojekyll`** (MULTIPLY is pure-static — Jekyll was pure overhead + a single point of failure) + a **CACHE_VERSION bump** to `v2-2026-07-17` so clients auto-purge stale caches (#344). On-device all green. Invariants #342–#347 → **count 347.**) _prior:_ July 16, 2026 (**Session 73 — the Giving Journey shipped end-to-end: a member-consent giving model that is opt-in, companioned, and never measured · MMT empty-state study card · 6 PRs + migration 077 · ?v=11 untouched · 8 invariants.** PRs #184, #186–#189. **Migration 077** = `giving_covenant` (member-insert-only RLS, no finance columns — consent + companionship only #330) + 6 formation rungs L1–L4 (`category='formation'`, CHECK widened #336; `celebrate_publicly`/`trackable` false; `meta.track='giving'`). **#186 (MMT)** the always-visible door + covenant-gated rungs + "double yes" opt-in naming the discipler, member-only close; `renderPathway` filters `track=giving` from the metro AND the done-count (absence is not a verdict #334). **#187 (MLT)** the companion card — only disciples who named THIS leader (RLS `partner_id=auth_member_id`), invisible when none, no church-wide view even for the pastor #332. **#188 (MMT)** L1-gate the door (#341) + affirmed-as-formation ("becoming true in you", never a checkmark #335). **#189 (MLT)** interactive affirm-toggle, one-on-one, via the proven `_mltPwToggle` writer (church_id trigger-stamped, completed_at defaults, delete keyed by member+level+rung, row-existence=done #337) — the Pastor pushed back on pure self-attest: the LCL checks like every rung (template is Jesus, "whatever I have commanded you"). **#184** MMT "This Week's Study" always renders with a bilingual empty-state. **Non-code:** the Agape pastor's missing study card = a content gap not a bug (`lc_meeting_guides` RLS needs a published+shared guide; Agape had none). **Process (new device, 3 near-misses, 0 damage):** CC caught on a stale S44 clone, then the WRONG repo (`eastbridge-op`), then a stale post-device session — each halted by the BASE/WANT gate + read-first (#338); upload lands at a separate `/root/.claude/uploads` path proven by WANT not a glob (#339); codeload lags like raw, SHA-checked after every merge (#340). Invariants #334–#341 → **count 341.**) _prior:_ July 14, 2026 (**Session 71 — the session where the DATA was the bug: a 93% "engagement collapse" that never happened · collapsible preaching banners · MLT Quick-Actions relocation · Meeting Prep AI links · a promo deck · 4 PRs + 7 invariants, ZERO migrations.** PRs #172–#175. **UI arc:** **#172** killed the MLT heads-up "0 members" flash — not a race, a **duplicate `display` in one inline style** (`display:none; …; display:flex`) so the banner painted VISIBLE at parse time until JS caught up ~800ms later; the show-branch's `style.display=''` also *deleted* the property rather than restoring it (Inv #320). **#173** made the preaching reminder banners **collapsible**, keyed to `(preach_date|stage)` so an escalation 7d→3d→0d mints a new key and reverts to the caller's default — **L5 starts shrunk, L4-and-below stay expanded** (the Pastor preaches weekly; the banner is clutter, not news). `shared.js ?v=9→?v=10` across **21** consumers; `CLAUDE.md` still claimed v=4. **#174** relocated **My Attendance** (→ new `screenMyAttendance`, fold + `mlt_attest_open` key RETIRED) and the **Preaching Calendar** into Quick Actions (11→13 buttons); `renderL5AttestCard` upgraded to `_liveLeaderLevel()` (#315). **#175** put **real AI links** (Gemini FIRST — Android leaders are already signed into Google; ChatGPT and Claude both wall a first-timer) into Meeting Prep step 2, armed after Copy and disarmed on date change so a stale "Copied ✓" can't lie. **THE BIG ONE — the engagement collapse that wasn't (Inv #322/#324):** asked to measure MMT/MLT usage, I read `leader_sessions` and found weekly logins down **93%** (706→48), **18 of 49 leaders** last active in a five-day window landing on **June 15** — the exact date the SW cache froze (#310). I charted it, and told the Pastor **not to hold his LC meeting** and to apologise to sixty leaders. **It was entirely an artifact.** `auth-login` decides `isLeader` from **`body.stamp`, a CLIENT-supplied flag**, and only the LEGACY `leader_login.html` sends it — the unified shell routes everyone through `member_login.html`, so **every shell login is invisible to `leader_sessions`.** The leaders had changed doors, not left. The honest column (`member_last_login`, which the shell DOES write) says **46/49 leaders active in 7 days, 49/49 in 30.** **And the denominator (Inv #325):** member adoption read a worrying 35%/7d — until we subtracted the **38 of 161 members who have NO PIN and literally cannot log in.** Of the **123 who hold a key, 122 have used it — 99%.** The bottleneck was never obedience; it was **onboarding**, and it had been diluting every percentage the platform ever produced. Also found: `ended_at` is stamped **only** on explicit logout so expired sessions are never reaped — one Logout rewrote all 20 of the Pastor's historical rows to the same millisecond, and `ended_at IS NULL` means "never explicitly closed," not "active" (Inv #323). **Non-code:** a self-contained **promo deck** (`multiply_promo.html`, 20 slides, hold-time proportional to word count, parchment→midnight at the member→leader handover — both palettes lifted from the tools themselves); the **`meeting-prep-generate` EF was SPECCED and then argued AGAINST** (cost is $0–21/mo, so cost was never the blocker — the real risks are the EF wall-clock timeout on a 4-guide ~5k-token generation, and Tagalog quality; the free BYO-AI copy/paste path costs the church nothing and now has links). **A member reported "wavy capital letters" in the devotional and feared for his eyesight** — MMT sets body text in **Fraunces**, a DISPLAY face with a **WONK** axis (Inv #326). **TODO next session:** (1) fix `auth-login` to derive `isLeader` from `pipeline_level`, not `body.stamp` — until then all leader telemetry is fiction; (2) one **`app_sessions`** table for everyone (kills the leader/member asymmetry AND #322/#323 together) rather than bolting on `member_sessions`; (3) reap expired sessions; (4) MMT font: body→Literata, kill uppercase in reading flow, add the A−/A+ slider, add an **Easy Read** (Atkinson Hyperlegible) mode; (5) the **stray-file deletion PR** (`multiply_shared (7).js` + `strengths_profile (11).html` — orphaned since S42, and the latter is a **pre-tenancy page GitHub Pages serves publicly**, defanged by RLS but loaded); (6) hand each LCL their list of un-onboarded members — **that** is the LC meeting, not a rebuke. Invariants #320–#326 → **count 326.**) _prior:_ July 13, 2026 (**Session 70 — the silent-bugs session: four things were broken and nobody knew · LCLs moved out of MD · the member notification rail + rung-aware celebration · 9 PRs + migrations 073–076 · 11 invariants.** PRs #161–#169. **(1) `allow-modals` (#161/#162, Inv #309):** the "delete does nothing" report was not an Attendance bug — sandboxed embed iframes lacked `allow-modals`, so `confirm()` silently returned falsy and **six** MD tools' destructive actions bailed with zero feedback (`alert()` was muzzled too, so even errors were silent). Fixed in all three embed hosts. **(2) The service worker cache had been FROZEN since June 15 (#165, Inv #310):** `networkFirst` cloned the Response *after* `caches.open()` resolved — by then the body was consumed, the clone threw while evaluating the argument (so `.catch()` never saw it), and **every** HTML/JS cache write silently failed. Likely the root of months of hard-refresh pain. **(3) A `LeaderScope` TDZ ReferenceError (#165, Inv #311)** silently killed the Pastor's MLT add-member toggle — guarded by `typeof`, which does NOT protect a TDZ binding. **(4) THE BIG ONE (#166, Inv #315):** the duplicate-member epidemic was traced to `currentLeader`, a parse-time snapshot of `window.LEADER` that is EMPTY inside the shell iframe (#169). Add Member showed a blank Discipler box, `saveNewMember` wrote `discipler_id: null` → **orphan members, invisible in their own LCL's list** → the LCL re-added them → duplicates. Rebuilt as live getters (one change healed 35 call sites) + a fail-closed refusal to save without a discipler. **MD/MLT audit + gate (#163/#164, Inv #314):** MD had NO level gate at all — any leader could reach Settings, admin editors and SVI weights. Audited the capability diff (surprise: **MD still runs the legacy `checks` pipeline; MLT/MMT are on `pathway_rungs`**), closed the one real gap (**MLT EOLO card**, lazy-loaded, #163) so LCLs *gained* something on the way out, then gated MD to **Coach (L3+)** across three files. 48 Rosehill LCLs affected, zero Coaches — verified. **The notification rail (#167/#168 + migration 073):** members were RECEIVING notifications (`prayer_save`) and had **nowhere to read them** — MMT only ever INSERTED. Tightened `notifications` RLS to `recipient_id = auth_member_id()` (073, INSERT left church-scoped so you can still notify others), then shipped the **MMT inbox** + the first member-bound **celebration** on rung completion. **Rung-aware, then celebration-aware (#169 + migrations 074/075/076):** the first celebration copy said *"marked 'Joy' as complete"* — theologically wrong, and the pathway revisits the fruit at L1/L2/L3, so a disciple would be told *"you completed Love"* three times. Rewrote with **formation language** for the nine fruit (Gal 5:22-23: the discipler *bears witness*, never certifies) and per-category copy for the other 8 categories (Inv #317: key on TITLE, not `category` — `character` is a mixed bag). Backfilled **all 204 base rungs + 5 Rosehill overlay rungs** with Tagalog (074/075, 148 titles reviewed row-by-row by the Pastor; 28 proper nouns deliberately kept English) and repaired 3 corrupted titles. Finally **076 + Meeting Prep "🎉 Celebrate these wins"** — and the Pastor drew the pastoral line better than the proposal (Inv #316): public celebration is for **EVENTS**, never **STATES**, because the *silence about everyone else becomes a verdict*. `meta.celebrate_publicly` is FAIL-CLOSED on 56 base + 3 overlay rungs. **A tenancy scare that was my own bug (Inv #313):** an unscoped diagnostic query in the SQL Editor (which bypasses RLS) returned 12 Agape members inside a "Rosehill" list; the wall held, the query didn't. **Process (Inv #318/#319):** three back-checks failed because I hand-edited prompts after proving them; **eight** self-verify counts were wrong while CC was right every time — stop counting greps, measure byte deltas. CC twice **refused to fabricate migration SQL** it hadn't been given. Also shipped: **BTLI Lesson 3 deck** (timer pills + 4 dedicated TANONG slides, 18→20, clock lands on 55:00). Invariants #309–#319 → **count 319.**) _prior:_ July 11, 2026 (**Session 69 — SVI Wave 5b-2 (per-church metric windows) + Option B (multi-ministry bonus scorer) · 3 PRs + migrations 071/072 · 6 invariants.** PRs #157/#158/#159. **5b-2a (#157 + migration 071):** new `svi_metric_overrides` table (per-church `lookback_days` per metric, mirrors `svi_weight_profiles` RLS + autostamp #266; a REGULAR composite unique #303) + EF widest-fetch/per-member-bound window resolution (`effLookback`/`widestLookback`, count_rows+rate_rows only #304). **Caught a latent bug (#305):** `allLite` never selected `church_id` → the `events_held` prayer denominator was silently n/a for ALL members since 5b-1 (held-dates keyed to `""`); the one-line fix revived prayer as a live weight-10 dimension — caught by a parity spot-check on REAL data, not synthetic. **5b-2b (#158):** pastor-only "🗓️ SVI Windows" MD UI — tune per-metric days without SQL; input=default DELETEs the override (#306). **Option B (#159 + migration 072):** reward multi-ministry WITHOUT lowering anyone — kept `service_ministry_role` boolean, ADDED `service_multi_ministry` bonus (n/a for 0–1, 7 for two, 10 for three) via a new generic `count_fields` compute_type (#307, CHECK-widened #290/#302), weight 0. Data: 38 multi-servers (8 two, 30 three, verified real). Per-church window PROVEN end-to-end (Rosehill prayer→28d moved a member 60→100). **Process (#308):** a FIND/REPLACE anchor emitted from a file slice carried a phantom trailing blank line → FIND matched zero; CC stopped, reported, re-anchored — outcome byte-identical. Migrations 071/072 run; EF redeployed; `service_multi_ministry` weighted. Invariants #303–#308 → **count 308.**) _prior:_ July 10, 2026 (**Session 68 — SVI Wave 5b-1: the rate engine (`rate_rows` compute_type · rostered vs events_held denominators · #290 CHECK-widen) · 1 PR + migration 070 · 3 invariants.** PR #153 + **migration 070** (already run `all_ok=PASS`, committed #242). Wave 5a was count-first; 5b-1 adds the **`rate_rows`** compute_type to `compute-svi-weekly` — a denominator prefetch + `computeRateRows` handler returning a 0..1 rate fed to the pre-existing `rate_to_score` (only numerator÷denominator was missing, #300). Two denominator modes (#301): **`rostered`** (SS/BTLI/Batch) = present/(present+absent) so an absence lowers the rate + 0-logged = not-enrolled = null (#281 preserved); **`events_held`** (Prayer, church-wide) = present/distinct-held-dates, held=0 = null, present=0 while held>0 = 0 (counted). Both n/a route through `null_if_zero`. **#290 in practice (#302):** migration 070 DROP+re-ADD the `compute_type` CHECK to include `rate_rows` before the UPDATEs. Converted the 4 attendance metrics in place (¾+→10/≥40%→7/≥15%→4, tunable in 5b-2); 6 stay counts. EF redeployed, recomputed — **dormant lessened, thriving increased.** Delivery = readable `<<<FIND>>>/<<<REPLACE>>>` anchor blocks (#298), back-check byte-clean (scope = 2 files). Care-detail % display fixed (PR #155): rate metrics now render "attended X% of …" (MD+MLT). Invariants #300–#302 → **count 302.**) _prior:_ July 10, 2026 (**Session 67 — Meeting Prep arc A→B→C (browsable lessons → church Library + Share-to-my-LCG clone → celebration opener + bilingual EN/TL) · MMT reads the Tagalog guide (bilingual loop closed) · Library shows your own shared lessons · LC Encouragement · MLT manual embed · base64→`str_replace` delivery switch · 8 PRs + migrations 068/069 + 8 invariants.** PRs #145–#151. **#145** LC Encouragement P1 (LCG Pulse “🌱 Building Rhythm” one-way `kind='encouragement'` nudge + editable greeting) + **migration 068** (CHECK→three kinds, #292). **#146** MLT User’s Manual opens in-app (embed modal, #293). **Meeting Prep (MLT `openMeetingPrep`):** **#147 Phase A** browsable “My lessons” + read-only modal *(stale-base caught → rebased → fetch-first #294)*; **#148 Phase B** + **migration 069** church **Library** + **Share-to-my-LCG** clone (`source_guide_id`, #295) + opt-in publish + `auth_is_leader()` (#296) + `*_md_tl`; **#149 Phase C** celebration opener + **EN/TL** four-fence prompt + graceful `mpParse` (#297) + Tagalog editor + “+ New lesson” blank-at-today. **#150** MMT reads `participant_md_tl` — member sees the Tagalog guide + live toggle; the CSS `.tl-text` toggle can’t touch rendered markdown, so pick the source column + re-render (#299); **bilingual loop closed** (author → read). **#151** MLT Library now lists your OWN shared guides too (“· you” tag; own → Edit not clone, via a `_mine` flag). **Delivery shift:** an 18 KB base64 patcher drifted in transit (sha-mismatch caught by CC’s checksum gate, ran nothing) → switched to readable `str_replace` **anchor blocks** (single copyable `<<<FIND>>>/<<<REPLACE>>>` block) — everything after reshipped byte-identical first try (#298). Both **068 & 069 run**. Invariants #292–#299 → **count 299.**) _prior:_ July 8, 2026 (**Session 66 — SVI manual pair · full in-app embed sweep (viewers + editors) · the Celebrate feature end-to-end (send→Amen→LC feed) + perfect-Wednesday badge · 8 PRs + migrations 066/067 + 5 invariants.** PRs #132–#135, #138, #140–#142. **SVI docs:** #132 pastor-manual (md Ch.2) → Wave-5 computation methods (count-first signals table, blend/n-a/Insufficient/trend, per-level weights, care-detail, six→seven fix); #133 leader-manual (mlt Ch.5) shepherding playbook — split by audience (#287). **Embed sweep:** #134 viewers (Pastor's Manual + Leadership Pipeline) + #135 six editors (attendance/devotional/quiz admin + transfer/bulk/preaching) → all MD/Settings pages open in the in-app `_openLessonViewerModal(...?embed=1, label, true)` modal; embed-class script + `html.embed` fixed→absolute Samsung CSS; Tier-2 editors hide own back-chrome + `_exitTool()` closes the modal via `window.parent._closeLessonViewer()` instead of navigating the iframe (#288). **Celebrate (full arc):** #138 P1 send/log/champion-chip in LCG Pulse + kind-aware `loadCheckins` + **migration 066** (`kind` col + status-CHECK extended to `'sent'` — the miss I made, CC caught: verify CHECK DDL not just column defaults, #290); #140 P2a leader banner + 🙏 Amen (`responded_at` = publish gate); #141 P2b 🏆 LC-feed group badge + **migration 067** (member-read RLS via `auth_discipler_id()`); #142 P2c 🙏 Wednesday Prayer Meeting perfect-attendance badge. `lcg_leader_checkins` is now dual-kind (#289); LC celebrations feed = computed group-badge assembler, auto-wins pull-not-materialize (#291). Chain: pastor Sends → leader Amens → badge lights for the whole LC. **Resolved:** the "Ministry Meeting" Heads-Up dropdown = config-not-bug (MD → Heads-Up Settings); back-checked CC's devotional file-upload branch. **REMINDER:** run migration **067** in Supabase (the P2b badge stays dark until then; 066 already run). Invariants #287–#291 → **count 291.**) _prior:_ July 7, 2026 (**Sessions 61–65 — SVI Wave 5a: 10 count-first disciplemaking metrics (engine + modal) · in-app report embed rollout COMPLETED to MLT & MD · `.crm` phone-scroll fix.** Migrations `064`/`065` + PRs #123–#129. **Report embed (done):** #123 (S61) rolled the pilot in-app pattern to MLT (LC Member / Member Attendance / Quiz Scores — `_openLessonViewerModal(...?embed=1, label, hideNewTab=true)` + per-report embed pass; member_attendance's post-scroll `.detail-overlay` also gets `scrollTo(0,0)` on open in embed, #279); #125 (S63) rolled all 5 MD launchers + gave `lcg_pulse` its embed pass; #124 (S62) capped the `.crm` care-detail modal at 88vh with an internally-scrolling body so the ✕ is reachable on phone. Every report now opens in-app from both MLT and MD. **SVI Wave 5a (the arc):** **`064`** seeded 10 count-first metrics into the GLOBAL `svi_metrics` catalog at weight 0 (opt-in, church-blind, #280): Prayer Meeting attendance, Sunday School, BTLI attendance, BTLI lessons passed, Special Batch, Pipeline rungs completed (`pathway_progress` movement), Celebration (`gratitude_entries`), Meeting-prep (`lc_meeting_responses`), LC guide prep (`lc_meeting_guides` by `lcl_id`), Preaching (`wednesday_preaching`, Wed+Sun via `service_type`) — activities the app already logged but SVI threw away. **S64 EF (#126):** `null_if_zero` (opt-in → null not 0 for non-participants) + the sufficiency `metricsTotal--` guard (n/a doesn't flip regulars to "insufficient", #281) + **church-scoped weight profiles** (fixed a latent cross-church collision — profiles were keyed by level only; now `${church_id}|${level}` + per-church default + snapshot `church_id` stamp + no-profile guard, #282). **`065` + S65 modal (#128):** `compute_config.unit` display noun → descriptive care-detail explanations ("6 Prayer Meetings attended") + n/a = "not applicable to this member" (#284, MD+MLT lockstep). **Result:** after redeploy + weighting, many "dormant" members correctly moved to "warming" — the old SVI had a blind spot for the very disciplemaking activities (prayer meeting, BTLI, pipeline movement) at the center of the vision. **Gotcha (#283):** SVI weights are per-LEVEL — weighting "default" moved lower-level members but not the L5 pastor until his own L5 profile was weighted (`Σweight=129` unchanged was the tell). **Process (#285/#286):** compute-svi-weekly is dashboard-deployed (root `index.ts`; merge ≠ live — Pastor redeploys, Verify-JWT OFF); GitHub web-editor commits arrive CRLF → need an LF-normalization follow-up (#127 for `064`, #129 for `065`); `schema_migrations` columns are `version, filename, applied_at, note`. **PARKED (SVI horizon):** Wave 5b = rate-based metrics + per-church windows (Gerry's church-tunable "days", safe once rate-based) + streak/consistency + batch-teacher split + multi-ministry scorer; Wave 5c = the **Multiplication flagship** ("are a leader's disciples advancing?" via `discipler_id` → disciples' `pathway_progress`, care-lens not KPI). **STILL TO DO:** finish weighting the 10 metrics across ALL levels (L1–L5) in MD → SVI Weights. Invariants #279–#286 → **count 286.**) _prior:_ July 7, 2026 (**Session 60 — maintenance + polish sweep: seven clean PR merges + a live-code reconciliation.** Shipped to `main`: **MMT "This Week's Study" date picker** (#116 — defaults to the latest LCL guide, dropdown of published guides theme-labeled from `devotionals.title`, per-guide response reload; RLS already scopes a member to their own LCL via `auth_discipler_id`); **MLT Meeting-Prep opener reshaped** (#117 — "Welcome & Positive/Negative": each member shares a win AND a struggle; the closing prayer folds in the negatives instead of a separate prayer-request round); **MMT announcements poll fix** (#118 — fetch-once + render-from-cache per render + visibility-gated 2-min poll, #277); **in-app reports pilot** (#119/#120 — LC Attendance Compliance now opens inside MLT via the reused `_openLessonViewerModal` overlay + `lc_attendance_report.html?embed=1` Samsung-safe mode; New-tab hidden for auth'd reports, #275; on-device confirmed); **service-worker idle-wake fix** (#121 — `networkFirst` 3.5s timeout race → the 3-4 min white-screen-on-wake becomes a ~3.5s cache serve, #276; on-device confirmed: 10-min idle resumes clean). **Reconciliation:** **Step 4 (the AI-research chip modal) was found ALREADY LIVE on `main`** (built S59, undocumented) — now recorded (#273); the session-open "frozen at S57/264" read was a `raw.githubusercontent` CDN stale-cache (#278) — the docs were in fact current through S58/272. **Auth model documented** (#274). **Investigated + parked:** the MMT lazy-load boot (#1) — audited: `renderHome` home-critical = streak/reflection/devo/**prayer** (CC wrongly listed prayer deferrable), `goTo` does CSS show/hide with NO re-render, so deferring needs a re-render-after-batch step; deferrable = library/celebration/caveats/lcDetails (~10 table-fetches). The "idle → bounce to menu" follow-up was unnecessary — cured by #121 (the bounce was the SW hang; genuine 8h expiry can't be re-handshaked). Invariants #273–#278 → **count 278.**) _prior:_ July 6, 2026 (**Session 58 — the per-church Leadership Pipeline: Full-Unify catalog (`059`/`060`) → data-driven + L5-editable poster · church-stamp + interleave-order (`061`/`063`) · True North onboard (`062`) · LCG Pulse Building-Rhythm · `noopener` redirect-loop fix · 8 invariants.** Migrations `059`–`063` + PRs #107/#108/#111 + direct fix `f2abd2f`; **3b (#112) show-hidden+interleave-reorder IN FLIGHT at close.** **Catalog:** `059` `meta jsonb`; `060` Full-Unify — `trackable` column + 167 reference rungs seeded from the static poster + `meta` attached to 11 spine rungs (204 base total, #265). **Poster:** #108 made `leadership_pipeline.html` data-driven (base+overlay resolve, `published:false` tombstoned, chip modal from `meta`); #111 added L5 Edit Mode — overlay CRUD (add/customize/tombstone/restore/edit/delete/publish/trackable), client sends NO `church_id` (`061` stamps it, #266), manual insert-or-update not partial-index upsert (#267), levels locked. **Order:** `063` `pathway_section_order` for full base+overlay interleave reorder (#269/#270). **Onboard:** `062` True North (slug `truenorth`, church_id `03ce9f8d-292c-449e-8a4e-c164cbf4d29c`, L5 Timothy, NULL pin) — 2nd tenant for overlay-isolation testing. **Pulse:** #107 Building-Rhythm partition + alphabetical + solo-LCG weak-guard (#271). **Loop fix:** `noopener` on the pipeline launcher stripped the sessionStorage leader session → redirect loop; removed to match the pulse launch (#268). **Process:** uncommitted-but-applied migrations broke a CC back-check (#272). Invariants #265–#272 → **count 272.**) _prior:_ July 4, 2026 (**Session 57 — Leadership Pathway v1 shipped: per-level rungs (`057`, partial-unique base+overlay #253) + per-member LCL-marked progress (`discipler_of` #254) · MMT Journey read + MLT marking · footprints identity · BTLI L2 deck rebuild + generic `BTLI_SLIDE_SPEC.md` · 5 invariants.** Migration `057` + PRs #101/#104/#105/#106. **Pathway:** `pathway_rungs` catalog (partial-unique #253) + `pathway_progress` (#260); 37 base rungs L0–L5; RLS member-own / LCL-marks-disciples (#254) / L3+ coach / L5 pastor; `renderPathway` MMT ladder + clickable metro, `renderMltPathway`/`_mltPwToggle` MLT; ladder→footprints across both (#264). **Decided/parked:** SVI = care-lens-not-gate + Flock Map (#261); per-church pipeline = shared frame + per-church rung overlay via `published` (#262); MLT "nudge". **Slides:** BTLI L2 rebuilt (23 slides, per-question Participant-Guide-Question slides + timer badges + generous notes), generic `BTLI_SLIDE_SPEC.md` on main (#263). Invariants #260–#264 → **count 264.**) _prior:_ July 2, 2026 (**Session 55 — LCG shepherd's check-in loop end-to-end · tenancy-unique sweep (052/053) + repo-hygiene consolidation · security lockdown (055) · L0 pipeline restructure wired into MD · 8 invariants.** PRs #87–#100 + migrations `052`–`056`. **Devotional fix:** root cause = single-church `UNIQUE(entry_date)` → `052` composite (#218). **Sweep:** `053` composites `wednesday_preaching`/`ministries`/`preachers`; 4 catalog uniques parked for partial-index #253; migrations relocated to `migrations/` (#252). **Security:** `055` locked 2 exposed `_backup_*` tables (#255). **Check-in loop:** `054`/`056` — Pulse send #93 → MLT 7-reason reply #95 → editable-template message #96/#97 (#257) → ack chip #98 → detail modal #99 → grid-weld hotfix #100 (#256); self-report-not-surveil (#258), per-member RLS via `auth_member_id()` in leader sessions (#254). **L0:** New Believer·Follow Jesus added (Stott+Putman, PDL retired), diagram wired into MD Settings (#91, #259). New shorthand `gmt`. Invariants #252–#259 → **count 259.**) _prior:_ July 1, 2026 (**Session 54 — two church onboardings (CEF #10, GGCF #11) · a `members`-RLS self-escalation HOTFIX · pastor-tools UI lockdown · two Pastor's-Manual chapters · MMT “Change my PIN” + a new `change_pin` EF action · 6 invariants.** PRs #78–#86 + migrations `049`–`051`. **Onboardings:** CEF (`049`, slug `cef`, L5 Arnaldo J Arnaldo) + GGCF (`051`, slug `ggcf`, L5 Geraldine Minguez) — NULL pin → first-login `set_pin`, re-stamp-not-delete (#235); 11 churches total. **SECURITY:** `members` had church-scoped INSERT/UPDATE/DELETE policies with NO level gate → any member could DELETE the roster or self-promote then re-login as pastor; `050` fixed it (DELETE needs L5; a BEFORE UPDATE trigger guards the privilege columns), validated 3/3 in-browser as a live L2 (#249). Client-side **pastor-tools UI lockdown** ahead of L2 onboarding (PR #82). **MMT cosmetics:** card unification (#79), attendance-sheet wrap fix (#80), footprints milestone icon, Home eyebrow/label cleanup + “Provision Notebook/Thanksgiving” rename. **Manual:** Chapter 3 “Loading the Daily Devotional” (#84) + SVI now documents Prayer as the LIVE 7th dimension (#85, #250 — `009` confirmed live: 458 recent snapshots scoring prayer). **Change my PIN (PR #86):** the `auth-login` EFs are now IN the repo (#246 — read the contract before building); `set_pin` is first-login-only (`pin_already_set` 409), so a new **`change_pin`** action (bcrypt-verify current server-side → overwrite, no session) backs an MMT Profile modal (#247); the EF redeploy is a separate human-gated step that must land with/before the merge (#248). `m.md` now drops two full files for manual upload (#251). Invariants #246–#251 → **count 251.**) _prior:_ June 30, 2026 (**Session 53 — BYO-AI Meeting Prep arc: a daily devotional → any-AI → Facilitator + Participant guides, with interactive member responses · Ministry Recommender close-out (DB-driven archetypes) · Pastor's Manual + MLT manual Ch4 · 10 invariants.** PRs #65–#77 + migrations `043`–`048`. **Meeting Prep:** `047 lc_meeting_guides` + `auth_discipler_id()`; MLT authoring screen (pick devotional → `mpBuildPrompt` → any AI → paste → fenced parse `===FACILITATOR===`/`===PARTICIPANT===`/`===END===` + manual fallback → review → publish; guides bound to the 1-hour meeting; #236/#237/#238), MMT "This Week's Study" card (#72), revisit fix #75 (local-date not UTC #243, resume-most-recent, clean empty-state, visible errors), prompt tweak #76. **Interactive:** `048 lc_meeting_responses` + `discipler_of()`; MMT i_will/reach/notes autosave, private-by-default + opt-in share-to-leader (#240, #74). **Recommender:** shared `ministry_archetypes` ⋈ each church's `ministries.archetype_key`, overlay-wins-by-key, no hardcoded array (#244, PRs #65–#68, migrations `043`–`046`; `046` renamed Agape → Agape Christian Church). **Manuals:** `md_manual.html` (#69) + MLT manual Chapter 4 "Meeting Prep & the LC Meeting" (#77, v1.2). **Process:** commit each migration immediately to close the #212 lag (#242); CC prompts now a single self-contained Python patch after a heredoc-gluing trap (#241); render-verify SVG diagrams (#245). Invariants #236–#245 → **count 245.**) _prior:_ June 29, 2026 (**Session 52 — Gratitude Journal + unified Home CTA+sheet · attendance recorder fix + full 'Unknown' backfill · 7-church pastor cohort · GP-batch own-scope · 8 invariants.** PRs #56–#62 + a grammar push + migrations `038`–`042`. **Gratitude Journal:** `gratitude_entries` (`038`, member-own + same-church-shared RLS, multiple/day), MMT Home card w/ autosave + "share as a win", 🌾 `loadCelebrationFeed` win-type (#56); collapsed into a CTA+`lc-detail` sheet w/ a "My Thanksgiving" list (view/share/delete) + a CSS de-scope fix (#57, Inv #229); leader's-own-shared-win fix via the leader-inclusive id set (#58, Inv #230); past-tense "gave thanks" label (Inv #231). **I'm-here** self-attest collapsed onto the same CTA+sheet (always-on CTA + "now" pill, empty-state, ids preserved so `renderAttestCard` untouched; #59, Inv #228/#229). **Attendance recorder:** batch/quick-mark saves stamped `logged_by='Unknown'` from the frozen `currentLeader` shim — fixed with `_liveLeaderIdName()` (live session) at all 3 write paths (#60, Inv #232); then all **288** historical Unknown rows attributed (279 LC-scoped→LCL `039`, 8 GP→pastor `040`, 1 removed-member orphan→Amy by id `041`) → 0 Unknown (Inv #233). **Multi-church:** `042` pre-seeded 7 tenant churches + L5 pastors (NULL pin → first-login `set_pin`; explicit `church_id`; Joey re-stamped Rosehill→Saturate over delete since member FKs block deletes; Inv #235). **GP-batch** attendance picker scoped to `owner_id` (#62, Inv #234). Migrations `038`–`041` committed via PR #61; `042` pending repo commit. **`m.md` now = net additions only** (CC splices). Invariants #228–#235 → **count 235.**) _prior:_ June 29, 2026 (**Session 51 — Pastor-gated add-member governance (#227) + MLT slate refit.** A per-church `churches.settings.mlt_add_member` toggle (default off) governs MLT's "+ Add Member": the Pastor flips it in MD Settings (`isPastor`-gated), which writes through the new **`church-settings-flag` Edge Function** (Verify-JWT-OFF, internal HS256; derives church from the JWT, requires `pipeline_level>=5` server-side, merges only the one settings key - `churches` has SELECT-only RLS so a client write was impossible). MLT enforces: button shows always for L2+, ENABLED only when on (a visible "+ Add Member · off" slate pill otherwise), `openAddMember` fail-closed guard, soft `_checkAddMemberDup` still backs save. Squashed an embed-mode bug (extends #169): MLT's `currentLeader` is a parse-time `const` snapshot of `window.LEADER`, frozen at level-0 in the shell until the postMessage session lands - the gate now reads level via `_liveLeaderLevel()` (`getValidSession()`-first). Also: **MLT repainted** near-black -> slate-blue (4-token swap; light text + accents preserved) with the disabled add-member state made visible; the **mobile member-modal footer** made horizontally scrollable (<=640px) so **Delete** is reachable on phones; the **MMT Journey nav icon** swapped ladder -> footprints (the ladder emoji rendered as a tofu box on many devices). PRs #48, #50-#55 merged + the journey-icon push direct-to-main; #47/#49 were no-ops (HANDOFF docs / EF-source already on main). **NEXT (fresh session): the daily Gratitude Journal** - locked design: a new `gratitude_entries` table (mirrors `devotional_reflections` + a `shared_to_lc` bool), an MMT **Home**-tab card ("what are you thankful to God for today?") with a **"share as a win in LC celebrations"** checkbox, and a new **gratitude win-type** in `loadCelebrationFeed` so shared entries surface to LC-mates (captures answered-prayer thanks that bypassed `prayer_requests`); multiple entries/day allowed; shared entries surface only in the member-facing celebration feed. Carry-overs from S50: `ministry_recommender` per-church profiles, overlay authoring + #218 audit, sample-guide seeder, BTLI L12. Invariant #227 -> **count 227.**)** _prior:_ June 26, 2026 (**Session 50 — De-brand arc (every user-visible "Rosehill"/"Pastor Gerry" gone) + a self-refreshing `schema.json`.** Two arcs shipped to `main`: **(A) Dynamic church branding** — MD header now paints `churches.name` via the JWT (`paintChurchName`, PR #37); a new standalone **`multiply_brand.js`** module (PR #38, UMD sibling of `multiply_close.js`, `?v=1`) auto-paints `[data-church-tmpl]` tokens (`{church}`/`{CHURCH}`/`{slug}`/`{SLUG}`) across 5 DOM pages, with an **anti-leak** static fallback that carries NO church name (upgrades only when the JWT resolves, Inv #222); "Pastor Gerry"→"your pastor" (PR #39, 8 spots); JS-string surfaces (WhatsApp signature, the 2 diagnostic PDF headers, member-dashboard brand+CSV) dynamic'd off each page's own JWT'd client (PR #41); 24 static curriculum/manual chrome swaps Rosehill→MULTIPLY (PR #42, MULTIPLY = publisher); MD guest-text + CSV genericized; 4 in-lesson visible strings genericized (PR #44). **ZERO user-visible "Rosehill" remains anywhere** — only code comments + one internal debug tool. A **#36 regression was caught + fixed** (PR #40): removing the obsolete Access-Control Settings section shifted `#panel-settings .ss:nth-child(3)` onto Pastor Tools (ministries manager clobbered it; raw `${ROSEHILL_MINISTRIES_PLACEHOLDER}` showed) → gave the section a stable `id`, repointed 4 JS refs (**Inv #226**). **(B) Self-refreshing schema** — a new **`schema-dump` EF** + **`.github/workflows/refresh-schema.yml`** (PRs #45/#46) now auto-commit an authoritative **structure-only `schema.json`** to repo root on every migration/EF push (+ weekly cron + manual dispatch); GitHub runners reach `supabase.co` where Claude/CC bash cannot, so the runner curls the EF, strips volatile `generated_at`+`est_rows` (byte-identical re-dumps, honors #124), and commits only on real change (**Inv #223**). **GREEN** — `schema.json` live (64 tables, ~460 KB); Claude now reads ground truth instead of guessing columns. Debug arc surfaced ops invariants: dashboard-editor EFs default **Verify-JWT ON** → secret-gated S2S calls 401 at the gateway until `--no-verify-jwt`/toggled off (**Inv #224**); **"Re-run jobs" replays the pinned stale SHA** and gets push-rejected `fetch first` — use **"Run workflow"** for a fresh run from current `main` (**Inv #225**). Invariants #222–#226 → **count 226.****) _prior:_ June 26, 2026 (**Session 49 — Phase B: shared-content catalog publish-lane EF + RLS-enable — the MULTI-TENANCY ARC is COMPLETE.** Shipped to `main` (PR #34, #215 back-check clean): the **`catalog-publish` Edge Function** (service-role, BASE-only publish lane — verifies the caller's HS256 JWT then gates on `members.is_platform_admin`; insert/upsert force `church_id=NULL`, update/delete scoped `.is('church_id', null)` so the lane can never touch a tenant overlay; allowlists the 7 catalog tables + ops insert/update/upsert/delete; deployed `--no-verify-jwt`, smoke-tested ×2); **`036`** `members.is_platform_admin` (seeded the platform author ONLY — Gerry `547ebda6` — decoupled from any church UUID, Inv #220); **`MultiplyShared.catalogWrite()`** + **23 call-site swaps** across `multiply_dashboard.html`/`btli_quiz_admin.html`/`lesson_quiz_editor.html` (`?v=8→?v=9` across 19 loaders); **`037`** flipped **RLS ON** for all 7 catalog tables (pre-flight 4/4 policies + 0 stubs each; self-verify green ×7). **Agape leak-test (RLS live, member `4c79ce89`):** sees base + own overlay, BLIND to Rosehill's overlay, base-write rejected `403 not_platform_admin`. **EVERY per-church table — config, `churches`, and shared-content catalog — is now isolated; the tenancy arc begun in Phase 1 (S29) is DONE.** Near-miss caught: a literal-grep blind spot hid that `lesson_quiz_editor.html` writes `usbong_quizzes` via a dynamic `db.from(tbl)` (Inv #221). Invariants #219–#221 → count 221.**) _prior:_ June 26, 2026 (**Session 48 — Closing the tenancy gap: catalog structure + `churches` isolation (+ Agape SVI seed).** Shipped to `main` (PRs #32/#33, #215 back-check clean both): **Model 3 shared-content catalog tenancy `033`** (nullable `church_id`, NULL=global base + per-church overlay; 4 tenant policies on the 7 catalog tables — btli_quizzes, cohort_programs, library_chapters/quizzes/resources, pipeline_lessons, usbong_quizzes — RLS left DISABLED pending **Phase B** publish-lane); **Agape `svi_weight_profiles` seed + composite-unique repair `034`** (dropped two tenancy-blind uniques → `UNIQUE(church_id, profile_key)` + `UNIQUE(church_id, applies_to_level) WHERE is_active`; seeded Agape a copy-as-template of Rosehill's 7 profiles, genericized level_5 note — both churches now at 7); **`churches` registry own-row-only RLS `035`** (SELECT `id = auth_church_id()`, writes service-role/owner-only, RLS ENABLED, **leak-tested in-browser**: Agape JWT → 1 row, Rosehill invisible). All per-church CONFIG + `churches` now isolated; only the 7 catalog tables await Phase-B RLS-enable (blocked on the publish-lane EF, not a decision). Invariants #217–#218 → count 218.**) _prior:_ June 25, 2026 (**Session 47 — `system_settings` per-church isolation (`030`/`031`, PR #28, Option C — retired anon admin page, dropped `single_row` guard + 2 live public stubs, `id`→sequence, upsert self-provision, Agape seeded id=4) · MD Settings render-fix (PR #29 — hide-only gates raced LeaderScope in the shell; `initSettingsPanel` re-asserts on open) · `svi_weight_profiles` per-church RLS (`032`, PR #30 — dropped public-read + service_role-write, 4 tenant policies; `svi_metrics` stays global). ALL per-church CONFIG tables now isolated. Invariants #213–#216 → count 216.**) _prior:_ June 24, 2026 (**Session 46 — `schema_migrations` ledger + reconcile-on-open · RLS-coverage sweep landed 21 tables · `leader_sessions` EF `church_id` stamp · self-verifying-migration habit · FIRST tenant church onboarded (Agape).** Shipped to `main` (PRs #18–#26): the ledger (`023`, Inv #212) makes the dashboard-run-not-committed gap self-detecting; the Inv #208 sweep enforced RLS on 21 more tables — tier-a `024` (6), devotionals `025` (Model A per-church + autostamp re-attached), `021` assessment-tables catch-up, tier-b `026` (13), `leader_sessions` `027` (+ `auth-login` now stamps `church_id`, Inv #211); every migration now self-verifies, rerun-until-true (Inv #210); **Agape onboarded** (`028`/`029` — Millete cluster pastor Jojo + 14 moved + flipped to full members, Jojo at L3). Invariants #210–#212 → count 212. Still open (non-blocking): `system_settings` isolation (next, a mini-project), the 8 no-`church_id` shared-content tables (anon-write hardening + a curriculum-edit decision), the sample-guide seeder.** _prior:_ June 23, 2026 (**Session 45 — logout JWT-hygiene + RLS version-of-record catch-up + SECURITY DEFINER view audit.** Shipped to `main`: (1) **JWT-clear-on-logout** — `_clearSessionState()` in `multiply_shared.js` clears all 3 keys (`multiply_leader_session`/`multiply_member_session`/`multiply_jwt`), `?v=7→?v=8` across 19 consumers, legacy `leader_login` `logoutAndRestart` teardown matched (PRs #13/#15, browser-proven: sessionStorage empty after logout, Inv #205); (2) **migrations 015–020 committed** to the repo as version-of-record (#14 — the RLS-rollout dashboard-only gap, closed); (3) **SECURITY DEFINER view audit** — 6 owner-views bypassed RLS (`v_btli_course_grade`, `v_btli_member_best`, `v_usbong_course_grade`, `v_usbong_lesson_grade`, `v_pending_transfers`, `v_svi_latest`); `migrations/022` flipped all 6 to `security_invoker` + enabled RLS on the 2 quiz-attempt base tables they read (`btli_quiz_attempts`/`usbong_quiz_attempts`, 4 policies each). Run FLAT after a wrapped `BEGIN/COMMIT` first run silently rolled back (Inv #209); live-verified (`rls=true`, `policy_count=4`, `null_church=0`, 6 views `security_invoker=true`) + browser-smoked (report renders church-scoped, member quiz-submit saves; Inv #206/#207). **BIG FINDING: multi-tenancy is NOT complete (corrects S44) — ~22 church-stamped tables still RLS-off; no active cross-tenant leak today (Rosehill-only data) but a hard prereq before Agape onboarding (Inv #208).** Invariants #205–#209 → count 209.** **Session 44 — login rewire EXECUTED + `members` RLS ENABLED — the LAST per-church table; ALL tables now enforce tenancy, isolation proven in-browser (L5 Rosehill 236 own rows vs Agape test 1 row, zero leakage). Both login pages rewired to ZERO client-side `members` access + de-branded (`72012c0`); `auth-login` gained `min_level` + `stamp:'leader'` (`6ab9aa0`, deployed); unified-shell single-door login model confirmed — `leader_login` now legacy fallback (Inv #204). Invariant #204 → count 204.** **Session 43 - `members`-RLS prep: own-anon readers (`member_self_edit` Inv #198, `bulk_send_links` Inv #200) CLOSED; `auth-login` consolidated to `search`/`login`/`set_pin` (Inv #199) + deployed; both EFs committed to repo (Inv #202, dashboard-gap closed); login rewire DESIGNED (Inv #201 - `members` RLS needs server-side login); next = execute login rewire then enable `members` RLS. Invariants #198-#203 -> count 203.** - **Session 42 - the assessment-token + RLS arc COMPLETE; `member_profiles` + `gifts_diagnostic` RLS-live + isolation-proven -> 16 tables.** New infra: the **`token-login` Edge Function** (Inv #190 - dashboard-deployed, `verify_jwt` OFF, reuses `JWT_SECRET`, mints a +2h church-scoped JWT from a `profile_tokens` token; proven for Rosehill + Agape) and **`multiply_tenancy.js?v=2`** (Inv #191/#192 - standalone UMD sibling of `multiply_close.js`; `tokenLogin()` + `bootstrap()` + JWT-attached `getDB()`; NOT in shared.js, no `?v=` lockstep). All **7 assessment instruments** wired to `MultiplyTenancy` + **`ministry_recommender`** C3b-attached. A **resume regression was caught on the disc canary** (Inv #193 - render-before-tenancy: build inputs synchronously BEFORE the awaited `bootstrap()`, else the autosave IIFE attaches to non-existent inputs) and fixed before fan-out. RLS migration (DROP the `"Enable all for anon"` allow-all stubs on BOTH tables (Inv #186) -> backfill 28 NULL -> Rosehill -> 4 `TO authenticated` policies on `auth_church_id()` -> ENABLE) verified (`policy_count=4`, `null_church=0`) + **isolation-proven via `token-login` JWTs** (Agape `[]` vs Rosehill rows). **Phase 2d:** cold assessment SHARE links now `?token=` via `getOrCreateMemberToken` (one token/member, reuse-or-mint; Inv #195); in-app "Take" stays `?id=` (Inv #194 - `?id=` cold = anon = RLS-blocked); removed the MD salvation-tab raw-`?id=` URL preview footgun (Inv #196). **Standing rule:** recommend the most elegant + scalable option, never the easiest (Inv #197). **Invariants #190-#197 added -> count 197.** Deployed `multiply_shared.js` unchanged at `?v=7`; `multiply_tenancy.js` at `?v=2`. Pending: **`member_self_edit.html` + `members`/login RLS (LAST table, anon pre-auth)** - commit `auth-login` + `token-login` to repo - orphan `gifts_diagnostic` `member_id=null` row - stray `strengths_profile (11).html` + `multiply_shared (7).js` delete - 2 own-anon debug tools - SECURITY DEFINER view audit - BTLI L12 (UNLAD-L2 scan).** - **Session 41 - RLS rollout (3 safe-now batches, 13 tables) + MLT My Profile + preaching-calendar JWT gate.** RLS now enforced on **13 more tables** across 4 migrations: **`016`** cohort family (4: cohorts, cohort_members, cohort_lesson_unlocks, pipeline_lesson_grants); **`017`** communication family (5: announcements, announcement_acks, announcement_retraction_dismissals, absence_notices, notifications) — which **caught + dropped two stale `public` allow-all stubs on `absence_notices`** that were silently defeating isolation (Inv #186); **`018`** `sermons`; **`019`** preaching (3: preachers, wednesday_preaching, preaching_swap_requests). Each proven by structural verify (`policy_count=4` EXACTLY, `null_church=0`) + curl isolation (Agape `[]` vs Rosehill rows) + app-load smoke (Inv #187). The pre-RLS CC audit now also scans `pg_policies` for allow-all stubs. **`preaching_calendar.html` JWT-gated (Option A, `841a8bf`)** — the `?me=` logged-out path now shows a login panel + makes zero DB calls, and swap-submit requires a JWT; this closed the last anon path so `019` could ship (Inv #188). **MLT 'My Profile' SHIPPED (`80b4b1c`)** — a header **My Profile** button (replacing 🔄) opens a tabbed screen: My Info (→ `member_self_edit.html` via self-edit token) + My Assessments (take/view all 7 via the owner’s id; gifts dual-table check; Salvation no-view). **MD embed-badge fix (`1fb9311`, Inv #169)** — `paintLeaderBadge` was an anonymous boot IIFE missed by the embed re-sync (so the Pastor’s My Profile button + leader badge never appeared in the shell); converted to a named hoisted function + re-called in the re-sync. `member_self_edit.html` got a `multiply_close.js` Close + generic "MULTIPLY Pipeline" de-branding (`f32200d`, Inv #29). **All CC prompts now delivered as single pastable fenced blocks (Inv #189).** **14 tables total now RLS-live** (+ the `debrief_records` canary `015`). **Invariants #186–#189 added → count 189.** Deployed `multiply_shared.js` unchanged at `?v=7`. Pending: **assessment token-flow** (still the gate before `member_profiles`/`gifts_diagnostic` RLS) · `members`/login RLS (last) · SECURITY DEFINER view audit · preaching-calendar `members` anon-read (before `members` RLS) · stray git-tracked `multiply_shared (7).js` housekeeping delete · BTLI L12 (UNLAD-L2 scan).** — **Session 40 — Multi-tenant Phase 2 is LIVE end-to-end. Custom **HS256 JWT (Path 1, signed with the project's LEGACY shared secret** — current key is asymmetric ECC P-256, not exportable; legacy still verifies; never revoke it; Inv #180) minted by the **`auth-login` Edge Function (verify_jwt OFF**, bcrypt-verifies the PIN with `npm:bcryptjs@2.4.3`, signs with EF secret `JWT_SECRET`; Inv #181). Client stores it in `sessionStorage.multiply_jwt` (C1 `c7ac579`) and **`getDB()` attaches `Authorization: Bearer`** (C2 `e39fc27`, `?v=6→?v=7` across **19 consumers**, Inv #184). `church_id` **auto-stamps on insert** via the `set_church_id_from_jwt` trigger (45 tables, except `devotionals`+backups; `migrations/014`, Inv #182). **FIRST RLS canary LIVE on `debrief_records`** (`migrations/015`) — isolation **PROVEN** (Agape token → `[]` vs Rosehill token → 5 rows); **Test A reads + writes confirmed — a new debrief was created in-app AND revisited to verify it actually persisted** (Inv #185). Mixed-client fix: MD switched to `getDB()` (C3a `0f3ab90`); 3 standalone pages inline-attach the JWT (C3b-1 `91b927d`); the DB client MUST carry the JWT for RLS (Inv #183). Agape church seeded (`migrations/013`). Non-tenancy: MLT 'My Attendance' fold (`bcf984d`); new Quiz Scores report + MD/MLT launchers + 'how to read' legend (`32efab2`, `a53090b`). **Invariants #180–#185 added → count 185.** Deployed `multiply_shared.js` = `?v=7`.** — **Session 39 — assessment Close/return polish · shared `multiply_close.js` module + 7-instrument sweep · MMT lands back on Discover after an assessment · enneagram lang-toggle moved bottom-right + font slider added (all 7 instruments now have Close+font+language parity) · devotional streak/SVI now require a ≥10-word reflection (`migrations/011` cleanup RUN) · LC Celebration feed floor-6 + always-on LC-leader streak + no-discipler self-anchor · MMT font-zoom compression fix. Deployed `multiply_shared.js` is `?v=6`. Invariants #173–#179 added → count 179.** — **Session 38 — SVI: embed-scoping fixes · transparent care-detail modal · LC-meeting scoring · + Preaching Auto-fill window anchored to schedule-end**. The arc: (1) **Embed-timing bug class fixed (Inv #169)** — in the shell iframe (`?embed=1`) the session arrives async via postMessage AFTER `const LEADER = window.LEADER||{}` binds, so LEADER stayed `{}` → the Pastor's SVI report showed 'your disciples · 0', the '🔓 All Members' tab stayed hidden, and `_crScopedIds` scoped him to disciples-only. Fix: an embed-only re-sync `MultiplyShared.embedSessionReady(3000).then(()=>Object.assign(LEADER,s))` that mutates LEADER **in place** and re-runs the boot IIFEs (`revealPastorTab`, `_refreshMyMinistryNav`, scope-count setter) — Pastor now sees **231 church-wide**. (2) **Care-detail modal made transparent** — each dimension shows `score×weight=contribution`(+raw); a **Score-computation** panel proves `Σsw÷Σw×10 = total ✓`; a **How-each-number-is-derived** section is data-driven from `svi_metrics` (compute_type/score_rules/windows). (3) **LC-meeting scoring SHIPPED (commit e00c435, `migrations/010` RUN, Inv #171)** — `service_lc_led` from the member's **own LC-Meeting attendance** (L2+ only; L<2 = null = not counted); `gather_lc`/`fellowship_lc_rate` **softened** to a `no_meeting_floor_score`(=4) when the member's LC didn't meet that window (real low score kept if the LC met but the member was absent); 'meeting held' = ≥1 group member present. (4) **MMT greeting** now time-of-day-aware (Manila hour → umaga/hapon/gabi). (5) **Preaching Auto-fill (`preaching_admin.html`, Inv #172)** — the fill window now anchors to the **END of the schedule**: `startDate = later of (max assigned preach_date, today)`, `windowEnd = +84d`, so each press extends a full 12 weeks **past the schedule's end** instead of a shrinking rolling-from-today window; gaps **before** the last-assigned date are intentionally skipped (forward-extend only); the confirm now shows the 'From … to …' span. **✅ EF 150s blocker RESOLVED (Inv #170):** the **`svi-ef-bulk-prefetch`** refactor SHIPPED + the Pastor REDEPLOYED `compute-svi-weekly` — the full **231-member compute now finishes in seconds**. Reviewed clean on `main`: bulk prefetch fully paginated (`fetchAllPaged`/`.range()`), `computeMemberSnapshot` **synchronous with zero `await supabase.from()` in the per-member loop**. Dry-run parity = **expected results, no drift** (Inv #112). **LC-meeting scoring (Inv #171) is now LIVE** — scores shifted as designed (e.g. **May Ann fell below 70.67**). **Invariants added: #169–#172. Count now 172.** — Prior **Session 37 housekeeping** — **`lc_group` decommission now COMPLETE**: `migrations/004` (promote-by-id) + `005` (drop dormant indexes) **RUN** by the Pastor; phantom `006` **removed** (the 3 views never existed — confirmed live). Transfer mis-attribution check = clean (2 stale `discipler_name` rows from a leader rename, `discipler_id` correct, no buggy promote, no remediation). **Inv #166 strengthened** → ALL denormalized text mirrors (`discipler_name`/`member_name`/lc_group family/…) are decorative; resolve `id` → current record, never stored text. Optional cosmetic `migrations/007` (refresh stale `discipler_name`) staged, human-gated. — Prior **Session 36 — `lc_group` decommission (Inv #166, Tiers 1–4) · Prayer Waves 1 bugfixes + Wave 2 · devotional-nag fix · MLT sorting**. The big arc: `lc_group`/`member_lc_group`/`event_lc_group` are now **decorative** — all grouping/matching/filtering/counting keys on `discipler_id` → LCL. Shipped: **Tier 1** attendance reports re-attributed by member `discipler_id` + three-state heartbeat (LACR `e23554f`, Pulse `bca7871`, Inv #167); **Tier 2** repo audit + `diag_lc_group_views.sql` (3 views dormant, `ba8d647`); **Tier 3** announcement targeting → `lcl:` keys across MD/MLT/MMT (`e889be1`); **Tier 4** absence-notice heads-up → own-disciples-only (`dc15c8f`) + transfer-state by `pending_facilitator_id` + MMT LC-family by `discipler_id` (`0dfc641`). **Tier 4B** human-gated SQL **proposed, not run** (`migrations/004` promote-by-id, `005` drop dormant indexes, `006` drop dormant views ⛔ blocked on captured defs). **Tier 4C** `index.ts` svi `lc_group` passenger — deferred (needs Edge redeploy). Also: **Prayer Wave 1 bugfixes** (requester-only answered, answer→saver propagation, save-on-answered stamp, no-active-action-on-answered, single answered indicator, My-List overlay) + **Wave 2 SHIPPED** (intercession "N praying", pull aggregates, praise→celebration feed, soft care flag, list filter — `7daca68`, no SQL, `?v=4`); **devotional nag existence-gated** (`c0f41aa`, Inv #168); **MLT** batch-roster + My-Batches list sorting (`ce4c88f`, `3836edd`). **Invariants added: #166–#168. Count now 168.** Phase-2 RLS flag: cross-member prayer writes (intercession/care/propagation) → `SECURITY DEFINER` RPCs. **W3/W4 prayer waves HELD.** Was Session 35 — **Prayer / Panalangin Wave 1**: a member-facing prayer-request board on **MMT**. `migrations/003_prayer_wave1.sql` (run + verified in Supabase, `verify_003` `all_ok:true`; rollback on standby, NOT run) creates **4 per-church tables** — `prayer_requests` / `prayer_list_items` / `prayer_list_opens` / `intercessions` (the last serves Waves 2–4). `member_tool.html`: Home Prayer CTA, compose modal (4 audiences — `lcg` / `individuals` / `all` / `discipler_pastor`), kind-scoped feed, save-to-list, My Prayer List (logs opens), mark-answered. Bilingual, **no points/score/badge UI**. `individuals` picker = full **same-kind** church roster (reach outside the LCG); `discipler_pastor` pastor detection keys on `pipeline_level >= 5`, degrades to discipler-only in a guest/test bubble. Kind bubble = Inv #164, helpers local to the prayer block (Phase B: centralize `sameKind` in `multiply_shared.js`). No `multiply_shared.js` change → `?v=4`. **1 invariant added: #165. Count now 165.** Waves W2 (encouragement) / W3 (reminders + auto-expiry) / W4 (silent SVI prayer category) are **HELD until Pastor's go**. Landed via laptop-cloud `format-patch` → applied + pushed from the desktop. Prior this period: **S34** kind-mirroring Phase A (`fc586a1`, Inv #164). Was: Session 33 — **guest/test containment + self-attest past-date + viewer exit**: (1) members **created by a guest/test user inherit that flag** in both insert paths — MD Add Member + MLT `saveNewMember` — so guest-added members never contaminate Rosehill stats (`4c48e83`, Inv #162); (2) **self-attest past-date** — correcting an OLD leader-marked absence now works at **any age** (Option A: window check moved below the dup check; new claims still 7-day-gated) + a native date picker (`b4b792f`, Inv #163, extends #152); (3) **profile_results_viewer** got an always-visible **✕ Close** button that returns to the firing page — tablet/PWA users were stuck (`58944e9`, applies Inv #12). No `multiply_shared.js` change → `?v=4`. **2 invariants added: #162–#163. Count now 163.** Still offered, not done: one-time **backfill** to flag pre-existing guest-added members.)
**Pastor:** Gerry Limoso · Rosehill Christian Church · ~170 members · Manila UTC+8
**System launch date:** May 1, 2026 (production live ~1 month)
**Repo:** github.com/gejable1/multiply (deployed to gejable1.github.io/multiply)
**Supabase project:** tirzeikbflolaclgtket
**Dev surface (NEW — June 4, 2026):** Development has **migrated to Claude Code**. The canonical dev surface is now this **local git clone** — edit here, `git push` to GitHub, and **Pages auto-deploys**. `CLAUDE.md` is the Claude Code project-memory file (read it alongside this HANDOFF each session). The old **manual download / upload-to-GitHub** workflow is **retired**.

---

## 🎯 Jumpstart prompt for next session

Hi Claude, kapatid -- fresh session. Pull ALL of `HANDOFF.md`, `MULTIPLY_INVARIANTS.md` yourself -- via **git clone + `git cat-file blob origin/main`** (AUTHORITATIVE; codeload tarballs go STALE like raw, and EOL-sensitive checks need clone + `git cat-file blob` #340/#352), plus `schema.json` from repo root (authoritative; read columns from it, never guess -- #223, and see #407: the rule has no size exemption, it was broken three times in S82 inside throwaway diagnostics). Verify the invariant count two ways (highest numbered block + its "count now N" footer) -- you should land on **407** (through Session 82). Also read the two new design docs on main: **`PATHWAY_FORK_DESIGN.md`** and **`TEMPLATE_CLEANUP.md`** -- the next arc is built from them.

**Also cross-check `migrations/` on origin/main against the ledger (#399), and read the RAW versions (#401):** run `SELECT version, length(version), encode(convert_to(version,'UTF8'),'hex') FROM public.schema_migrations ORDER BY 1;` -- do NOT wrap `version` in `lpad`, which truncates longer strings on the right and hid five malformed rows for months. As of S83 the ledger and `migrations/` agree at **96 files / 96 rows** -- reconciled by SET DIFF, not by counting: zero ledger rows without a file, zero files without a ledger row. The earlier "97/97" was an off-by-one: `001`-`097` reads as 97 slots, but **`006` never existed on either side**, so both are 96. Count forward migrations only -- `migrations/` also holds 17 `_rollback` / `_verify` / `_diagnostic` companions, 113 `.sql` files in total.

**Session 82 closed** (PRs #258-#261 + migrations 095-097). The ledger was wrong two ways: 078-082 ran with no stamp in their bodies (#402) and 052-056 stored full migration names as versions (#401). The **#400 ordering arc is CLOSED**: `order_map` is flat per level, ONE resolver lives in `multiply_shared.js` (`pathwayOrder.load/forLevel/sort`, **`?v=14`**, 22 consumers), and MMT/MLT/MD/poster all call it -- poster order === tool order, asserted in a harness. `moveRung` RELOCATES, never swaps (#404). The poster's duplicate comparator is gone (#405). The template is now **frozen**: a review notice shows to everyone and the Edit button is gated to `is_platform_admin` (Gerry only), revealed only after the check passes (#406).

**NEXT UP -- the PATHWAY FORK MODEL, then the template cleanup.** Decided with the Pastor and captured in `PATHWAY_FORK_DESIGN.md`: the shared rows become a read-only template + repository; each church owns a **versioned copy** (`pathway_versions`, a JSON doc) and publishing regenerates that church's `pathway_rungs` + `pathway_section_order` in one transaction -- so every reader shipped in S82 keeps working untouched. Retire-never-delete (progress is keyed by `rung_key` text and NOTHING in the 76-table schema references `pathway_rungs.id`, which is what makes this safe). Draft/publish, snapshots + restore, pre-publish sanity check, lean starter template, print + read-only share link, template updates by notification with per-item opt-in. Pastor-edited only; one published pipeline per church; draft is the pastor's desk; a retired rung stays visible to the member who completed it (denominator = today's published trackable rungs, so no `7/6`). Two additions from the Pastor at the close: **every item keeps its `trackable` checkbox** -- descriptors stay rungs and gain a `kind` field (`step` | `descriptor`) controlling only how they RENDER, because some churches track everything as a matter of culture; and **each church defines its own per-level TIMELINE**, edited in the same flat view. One schema gap to close first: `pathway_rungs` has NO unique constraint on `(church_id, level, rung_key)` -- verified collision-free today, but a double publish would duplicate a whole pipeline.

**Then `TEMPLATE_CLEANUP.md`** -- one pass over the 210 template rungs BEFORE any church forks, because afterwards the same fix is twelve conversations. 72 rows (34%) are descriptors, not steps; 19 duplicate resource rows collapse to 8; three roles (`lead-lc`, `coach-leaders`, `plant`) move competency -> ministry and fill the empty L2/L3/L5 Ministry columns; all nine fruit at every level (currently 4 per level, arbitrary -- love 5x, gentleness once, at L4); `btli1` + `btli-101` merge (course code `btli101` is already live, so the merge is cosmetic); 90-day -> 30-day; EGR becomes trackable at L0. Pastor's EGR role ladder for the serving-roles catalog: attendance L0 (a step), facilitator L1, speaker L2+, organizer L3+. Still open: whether the disqualifying half of a level profile (`no-moral-failure-5-yrs`, `no-active-church-discipline`) should be leader-side only while the aspirational half is public -- raised, not yet answered.

Then: (2) L0 monitoring + a week-24 at-risk reminder + the LCL manual chapter (the five paths behind a low salvation score, the three tests of 1 John); (3) the Tagalog question in the poster editor; (4) SVI Wave 5b polish; (5) assessment re-brand (needs Gerry's new names); (6) BTLI L12 (blocked on the UNLAD-L2 scan) + L13-L20. Note: **FATS**, not FATLESS -- the Pastor confirmed FATS in S82; canonical docs still carrying FATLESS need updating.

**Discipline:** pull deployed files + `schema.json` via git clone + `git cat-file blob` (#340/#352); surgical edits go **BASE->patcher->WANT** through CC as one block (#347), back-checked byte-for-byte vs origin/main before merge (#318/#215) -- and check whether a branch is already MERGED into main before reporting it open. Pure-ASCII patchers may go inline; any hand-edit to a proven patcher invalidates its WANT (#391); two clean-execution WANT mismatches = deliver as a sha-verified UPLOAD located by a recursive sha walk (#339/#361); emit `\uXXXX` as literal text and verify added-non-ASCII delta==0 (#390). A harness must EXECUTE the real thing, lifted VERBATIM from the patched file, never a retyped copy (#392); after any change to a CALCULATION build the rendered output and assert on the text a human reads (#398); when a control edits a projection of a larger structure, render the LARGER structure (#404). SQL human-gated / FLAT (#209) / self-verify **its own effects only** (#403) / stamped in the body (#402) / committed to `migrations/` IMMEDIATELY as LF (#242, #399); prove data-writing or policy migrations on ephemeral PG16 as `postgres` first (#379), including the upsert path (#393). Read RAW before normalizing (#401). Ask what is MASKING a defect that has been live for weeks (#396). One resolver, never two (#405). Permission gates fail CLOSED (#406). Recommend elegant+scalable (#197). Pastor Gerry writes in plain English now, not Taglish. Confirm read-in + count (**407**), then stand by.

## ✅ COMPLETED JULY 27, 2026 — SESSION 82 (the ledger's two lies and the normalizer that hid them · the #400 ordering arc closed end to end · the template frozen behind a platform-admin gate · and the pathway fork model designed · 4 PRs #258-#261 + migrations 095-097 · 7 invariants)

**The ledger repair (#258 + migrations 095, 096).** The S82 pre-flight ran the #399 reconcile and the ledger looked clean. It was wrong in two independent ways. First: `SELECT version FROM schema_migrations ORDER BY version` is a TEXT sort, so four rows stamped unpadded (`73`-`76`) floated above `094`. Second, and worse: the diagnostic that normalized them with `lpad(version,3,'0')` was TRUNCATING five rows that held the full migration name as the version (`052_devotionals_composite_unique`), rendering each as a tidy `052` (#401). The normalizer written to expose drift was hiding it. Separately, five migrations -- **078, 079, 080, 081, 082** -- existed as files, were confirmed applied against `schema.json` (the `source_*` columns, `mlt_delete_empty_member`, `'other'` in `lcg_checkins_reason_chk`, `app_sessions` present, `leader_sessions` absent), and had **no `schema_migrations` INSERT in their bodies at all** (#402) -- not a commit lag like #399, a stamp that was never written. 095 padded the four and backfilled the five (leaving `applied_at` alone: those ran weeks ago). 096 normalized the five long-name rows once hex output made them visible. Both were first written asserting a GLOBAL row count, which every later migration invalidates; both verify blocks were rewritten to assert only their own effects (#403) and re-proven on PG16 from the exact pre-095 state, forward and on rerun. Ledger and `migrations/` now agree, one convention throughout.

**The ordering arc, closed (#259 + migration 097, #260).** #400 left the poster writing `pathway_section_order.order_map` that no tool read, keyed `level|category` while the member's Journey is FLAT. The Pastor chose grouped display over flat truth: the poster keeps its category headings, the order of record is one flat list per level. Migration 097 RESET the single legacy row rather than converting it -- PG16 showed that converting `0|book` to a partial flat list would rank two books ABOVE `join-lc`, `usbong1` and `daily-devo` on every L0 member's phone, since unlisted keys fall back to `sort_order` behind listed ones. A reset costs one pastor one re-click; a conversion silently reorders a Journey. `moveRung` was then changed from SWAP to RELOCATE: a harness rendering the flat list a member would see caught the swap flinging `join-lc` from first to last while the poster showed `baptism` moving one notch inside its Ministry group (#404). PR #260 put ONE resolver in `multiply_shared.js` -- `pathwayOrder.load / forLevel / sort`, listed keys first in map order, everything else by `sort_order`, so an empty map behaves exactly as before and every consumer was safe to ship ahead of any authored order. MMT, MLT and MD load it inside their EXISTING `Promise.all` (no extra round trip) and their six sort sites call it; `?v=13 -> ?v=14` across all 22 consumers with a filesystem sweep for stragglers. The poster's own `_sectionComparator`, left behind by the first PR, was deleted -- two copies of a ranking rule is exactly what #400 was (#405). Asserted in the harness: **poster order === tool order**, with `usbong1` before `join-lc` across category lines, the sequence that could not previously be expressed. Giving-track sorts left untouched; that ladder stays separate.

**The template freeze (#261).** Ahead of the fork model, tenant churches now see a read-only pipeline with a notice explaining the refinement and inviting feedback. The Edit toggle previously revealed on `leaderLevel >= 5` -- all twelve tenant pastors -- and now also requires `is_platform_admin === true`, read from the caller's own `members` row (`members_select_own_church` already permits it, so no `auth-login` change and no Edge Function redeploy). The toggle STARTS hidden and is revealed only after the check passes: error, throw, no row, no session, no client all leave the pipeline read-only, and `'true'`/`1` do not unlock (#406). Ten scenarios executed against the function lifted verbatim; the rendered notice asserted with entities decoded. Verified: exactly one row in the whole platform carries `is_platform_admin` -- Gerry, Rosehill. A UX gate, stated as such in the code; RLS remains the real boundary and already prevents any church writing the shared template.

**Design: the pathway FORK MODEL (`PATHWAY_FORK_DESIGN.md`).** Agreed with the Pastor across the session. The shared `church_id IS NULL` rows become a read-only template and repository; each church owns a copy that IS its pipeline. Authoring happens in a versioned JSON document (`pathway_versions`), and publishing regenerates that church's rung + order rows in one transaction -- which means draft/publish, snapshots and undo all fall out of one decision, and every reader shipped this session keeps working untouched. Two facts make it safe and were verified, not assumed: the RLS already forbids a church writing the template and restricts writes to level >= 5; and `pathway_progress` is keyed by `(church_id, member_id, level, rung_key)` **text**, with NOTHING in the 76-table schema referencing `pathway_rungs.id` -- so rung rows can be rebuilt freely and no member ever loses a completion. Pastor's decisions: retire never delete; template updates by notification with per-item opt-in (never auto-adopted); pastor-edited; one published pipeline per church; draft is the pastor's desk alone; a retired rung stays visible to the member who completed it, with the denominator being today's published trackable rungs so no one sees `7/6`. All six recommendations approved: draft/publish, a completion-count warning before retiring, snapshots + restore, a pre-publish sanity check, a lean starter template, print + read-only share link. Late additions: **every item keeps its `trackable` checkbox** -- descriptors stay rungs and gain a `kind` field controlling only how they render, because some churches track everything as a matter of culture (this replaced an earlier plan to move descriptors into a separate table, and is strictly better); and **per-level TIMELINE per church**, edited in the flat view. One gap to close before any fork code: `pathway_rungs` has NO unique constraint on `(church_id, level, rung_key)` -- collision-free today, but a double publish would duplicate a pipeline.

**Design: the template review (`TEMPLATE_CLEANUP.md`).** All 210 template rungs analysed in code rather than by eye. **34% (72 rows) are descriptors, not steps** -- exactly 12 per level, every level, the tell of a generator filling fixed slots. The nine fruit are distributed arbitrarily: 4 per level (2 at L0), love five times, patience/kindness/gentleness once each -- gentleness only at L4. Ministry is EMPTY at L2, L3 and L5, not for lack of content but because the roles were filed by the skill they require: `lead-lc`, `coach-leaders` and `plant` are Life Group Leader, Coach and Church Planter, sitting under Competency. Eight resources repeat across levels (EGR at L0/L1/L2, the devotional guide at L0/L1/L2), and because progress is recorded per level a member re-ticks the same guide three times. Decisions: all nine fruit at every level; 30-day (not 90); FATS confirmed (docs carrying FATLESS need updating); `btli1` + `btli-101` merge, cosmetic because `lessons/btli101_xrw5fg/` proves `btli101` is already the live course code; EGR trackable at L0. Pastor's EGR role ladder seeds the serving-roles catalog: attendance L0 (a step), facilitator L1, speaker L2+, organizer L3+ -- attending is a step, serving is a role. Pathway after cleanup: 210 -> 126, with L0 dropping from 30 items to 18. **Raised, not yet answered:** whether the disqualifying half of a level profile (`no-moral-failure-5-yrs`, `no-active-church-discipline`, `family-affirmation`) should stay leader-side while the aspirational half is public -- the Pastor's own principle is that public celebration is for events, never states, because silence about everyone else becomes a verdict.

**Three source-reading lapses, recorded (#407).** The `sort_order` distribution was explained from the poster's `nextSort()` formula -- right for form-added rungs, wrong for the hand-seeded spine, and the Pastor's data showed an explicit 1-6 sequence with no ties. "Gentleness appears nowhere" was asserted from a template dump truncated mid-L3. And a verification query used `members.full_name`, which does not exist. None was hard; all three were checks skipped because they felt too small to be worth making. #223 has no size exemption.

**Process.** Every PR back-checked byte-for-byte against `origin/main` before merge; all four merged clean, zero rollbacks, zero forced gates. CC flagged two real risks unprompted from the diff alone (the dormant `order_map` rows, and confirming `is_platform_admin` was actually stamped before merging a gate that depends on it) -- and once reported three branches as unmerged that were in fact merged and deleted, having read its own push history rather than `origin/main` (#215 applies to branch STATE, not just file contents). Invariants #401-#407 → **count 407.**


## ✅ COMPLETED JULY 26, 2026 — SESSION 81 (members INSERT hardening + the platform-admin lockdown found while proving it · the poster editor that silently un-wired auto rungs, and the engine bug that was masking it · auto-completion v2's overlay resolver · the salvation diagnostic reweighted from accident to judgement, and its results panel made readable · 5 PRs #252-#256 + migrations 092-094 · 8 invariants)

**Security (#252 + migration 092).** `members_insert_own_church` checked ONLY `church_id`, so any authenticated member could INSERT a row with `pipeline_level=5`, `is_platform_admin=true` and a NULL `member_pin_hash`, claim it via the first-login-only `set_pin` action, and log in as an L5 pastor. Both exploits were REPRODUCED on an ephemeral PG16 replica of the live policy/trigger set before the fix and blocked after (18/18 assertions). Proving it surfaced a second, larger hole: 050's guard returned early for `auth_level()>=5`, so any of the 12 tenant pastors could self-grant `is_platform_admin` -- the only gate on `catalog-publish`, which writes the BASE curriculum lane shared by every church (#394). The level rule went into the POLICY first and rejected an L3 coach saving the L5 pastor's profile: PG applies an INSERT WITH CHECK to the proposed row even when `ON CONFLICT` resolves to UPDATE, and MD upserts members (#393). Final shape: policy carries the platform-admin rule, trigger carries the level rule and distinguishes create from edit by id-existence; `search_path` pinned on both `members` triggers (#385). Rule: a non-pastor may create a member strictly BELOW their own level -- the doctrine MLT already implemented client-side, now enforced server-side.

**The masked bug (#253 + migration 093).** `customizeBase()` built its overlay payload from the four fields the edit form shows and never carried `completion_source`, `auto_source_key`, `title_tl` or `description_tl` from the base row -- none of those strings appear anywhere in `leadership_pipeline.html` (#395). So retitling a rung silently un-wired it from auto-completion. It stayed invisible because auto-completion v1 joined BASE rungs only and kept ticking from the base definition -- the engine's own bug was masking the editor's (#396). Live diagnostic found exactly 2 damaged rows (Rosehill L2 `conflict`, L2 `disc`); 093 restored them generically, verified by re-running the read-only overlay diagnostic (2 rows changed, the other 7 untouched). The 131 existing `pathway_progress` rows were deliberately LEFT ALONE -- they are legitimate completions, and an earlier proposal to "clean them up" would have erased real member progress.

**Auto-completion v2 (#254 + migration 094).** One resolver, `v_effective_auto_rungs`: for each church, overlay wins by `(level, rung_key)`, then published + auto + non-null `auto_source_key` -- the precedence every client already applies, the way #380 wired quiz visibility to a single resolver. `security_invoker` per migration 022. The engine now matches on `auto_source_key` rather than `rung_key` (1:1 for the 9 base rungs per migration 057, so base behaviour is unchanged) and writes progress at the EFFECTIVE level. v2 never deletes. Production run inserted 13 rows, all completions dated 22-25 Jul -- members who qualified since the last weekly compute, no backfill surprise; resolver returns 108 = 9 base auto rungs x 12 churches, confirming NO church has yet authored an auto rung of its own. Diagnostics found A=0 (no overlay auto rungs), B=0 (no tombstoned base auto rungs), C=2 (the damaged pair).

**The salvation diagnostic (#255, #256).** Section weights were the question count times five, so the GOSPEL carried 5.9% of a salvation diagnostic while habits and church integration carried 35.3% -- an accident, not a decision (#397). It mattered because every respondent has already had 10-16 weeks of one-on-one Usbong plus a weekly huddle, so the habit and community sections barely separate anyone. Weights declared with the Pastor: A18/B12/C22/D18/E12/F9/G9 (he moved 2 points from disciplines to fruit -- internals over externals). Scripture-grounded sections (gospel, basis of assurance, 1 John) go 29% -> 48%. Both scorers -- the live preview AND `collectScores()` -- now call one shared `SALV_WEIGHTED_PCT()`, with a guard that errors if the weights stop totalling 100%. Then the Pastor caught on his phone what every gate had missed: the Overall Score card still printed `136/170` beside `71%` (#398). #256 leads with the weighted percentage, labels the raw figure as raw points, explains that only the 1-5 rating questions score, and reserves right padding on the sticky progress bar so `multiply_close.js`'s fixed pill stops covering the View Results button.

**Ledger drift caught (#399).** 093 ran in Supabase with an 8/8 self-verify and then sat UNCOMMITTED for three turns while other PRs went by -- the exact gap the S46 ledger exists to surface, with nothing looking at it. CC compounded it by reporting "the already-merged migration 093" because the delivery block was NAMED after the migration it accompanied rather than the editor fix it carried.

**Evaluation session (design, not code).** A hard review of Level 0 with the Pastor. Corrections the Pastor made to my analysis, all of which stood: every member already has a discipler and therefore a small group, with a 30-60 minute post-service huddle every Sunday, so "belonging comes last" was wrong; and Usbong is not curriculum but EVANGELISM, conducted one-on-one over 10-16 weeks, which makes the whole "a new believer is 3 weeks in" scenario a straw man. What survived: `join-lc` measures nothing when everyone is already placed; Stott's `basic-christianity` is the wrong book at the wrong moment (move to L2 as apologetics reference); the testimony rung is the true EXIT of L0 (received Usbong -> can give Usbong) and "I have named my one" is where EOLO multiplication actually begins. Assessment placement: run it when Usbong finishes and no later than week 24, leaving 16-20 weeks for debrief + intervention + retake + a second care cycle + the final discipler/LCL conversation before the year closes; a quiet 3-question LCL check at Usbong lesson 5-6 means the assessment should never surprise anyone. Two doors a year (two EGRs) caps the wait for a missed cycle at 6 months, not 12. On the "hanggang matiyak" retake loop: after the SECOND attempt the instrument should STOP and the evidence should shift from self-report to the discipler's observation (1 Jn 2:3 obedience, 3:14 love of the brethren, 4:2 clinging to Christ), with five distinct paths behind one low score -- understanding gap, understands-but-cannot-feel, unrepented pattern, non-spiritual burden (anxiety/scrupulosity, where re-testing actively harms), and genuinely not yet believing. The Pastor's own gate placement is right and mine was wrong: do not block L0->L1 (Unlad and Leading Yourself assume new birth), block L1->L2 where teaching begins -- BTLI Zone-1 already stands there.

**Open at close.** (1) The ORDERING arc, chosen as next: the poster's reorder arrows write `pathway_section_order.order_map`, and MMT/MLT/MD read NONE of it (#400) -- one real instance exists (Rosehill `0|book`, 2026-07-25) that never reached a member's phone; `order_map` is keyed `level|category` while the member's Journey is a FLAT list, so the intended sequence cannot even be expressed. Proposed shape: keep order per-church in `pathway_section_order` (writing `sort_order` directly would reorder all 12 churches at once), re-key it to `level`, put ONE resolver in `multiply_shared.js` (`?v=13 -> ?v=14`, 22 consumers), teach MMT/MLT/MD to call it, and only then add cross-category movement to the poster. Open question for the Pastor: should the poster stay grouped by category visually while the true order is flat underneath, or become flat too? (2) L0 monitoring, reminding (week-24 at-risk flag) and the LCL manual chapter carrying the five paths and the three tests of 1 John. (3) The Tagalog question in the poster editor -- carry the base's Tagalog, or add Tagalog fields to the form. (4) `sort_order` is collision-free at all six levels but seeded in consecutive blocks; a renumber-by-10 hygiene pass is worth doing whenever ordering is touched. Invariants #393-#400 → **count 400.**


## ✅ COMPLETED JULY 22, 2026 — SESSION 79 (The app_sessions engagement reader · SVI Wave 5c Multiplication + the pathway auto-completion engine that made it honest · a three-leak tenancy hardening · SVI guest trim · LACR monthly range + top-5 celebration · 8 PRs #226–#233 + migrations 083–087 · 8 invariants)

**PRs #226–#233 · migrations 083–087 · no `?v=`/shared.js/EF-source changes except compute-svi-weekly (index.ts) · invariants #372–#379 → count 379.** A big session that started at "why 301?" and turned into a genuine tenancy hardening, a flagship metric, and the engine that made the metric tell the truth. Every PR back-checked byte-for-byte vs origin/main; every data-writing migration proven on an ephemeral PG16 first (#379).

**#226 — the engagement reader (MD).** The S78 payoff: `app_sessions` was write-only. New standalone `engagement_report.html` (+ MD launcher card) reads the logbook via the JWT (RLS church-scoped), three lenses — active-this-week, last-active (quiet flags), to-onboard (`member_pin_hash IS NULL`, grouped by LCL). Excludes test + guests. Opens embedded. Zero migration.

**#227 — scope-aware "My Flock" (MLT).** `engagement_report.html?scope=mine` filters to the leader's own disciples (`discipler_id===LEADER.leaderId`), relabels "My Flock", flat to-onboard list; MLT launcher card. MD stays church-wide.

**#228 + migration 083 — SVI Wave 5c · Multiplication (the flagship).** New `disciples_advancing` compute_type + `multiplication` category (both CHECKs widened #290) + the `multiplication_disciples_advancing` metric seeded at weight 0 (opt-in, L2+). Special-cased in `computeMemberSnapshot` like `service_lc_led`: the fraction of a leader's disciples who advanced >=1 pathway rung in 90d; no disciples = n/a, none moving = a gentle flag; stores `detail:{advanced,total}` for the "N of M" care-detail line (MD + MLT `rawMeaning`). A care-lens, never a KPI (#378).

**#229 + migration 084 — the pathway auto-completion engine.** Multiplication first read **1 of 12** for Gerry's flock — honest, but only because `pathway_progress` was manual-only and his disciples' BTLI/assessments/streaks were never *marked*. So: a SECURITY DEFINER `auto_complete_pathway_rungs()` that derives the 9 objective `auto` rungs (5 assessments, 2 all-lessons courses, 2 devotional streaks) into `pathway_progress` with REAL milestone dates (#376), `marked_by=NULL` + `note='auto:<key>'` (#375), idempotent, base rungs only (#377). Character/fruit stay manual. Hooked at the top of the full weekly compute (`!dry_run && !member_id`, best-effort). Proven 10/10 on PG16 (#379). Backfill took Gerry **1/12 → 11/12** (zone warming → thriving) — the same flock, finally visible.

**#230 + migration 085 — the svi_snapshots read leak.** Investigating "why 301?" found `svi_snapshots` RLS-enabled but its read policy was `svi_snapshots_read USING(true)` — an allow-all stub predating the S46 sweep, so every SVI report saw ALL churches. 085 drops it, adds `svi_snapshots_tenant_select` on `church_id = auth_church_id()`. Count dropped **301 → 215** (Rosehill) (#372).

**#231 + migration 086 — two anon read/write leaks.** A write-only-stub audit (#372) surfaced worse: `interventions` and `ministry_intake` each had ONLY `"Enable all for anon" (ALL, USING/WITH CHECK true)` — unauthenticated read+write of every church's care notes + intake. 086 drops both, adds 4 church-scoped `authenticated` CRUD policies each. `intervention_tracker` uses the authenticated JWT so it scopes to own church (verified in-browser: a live "Test." facilitator note saved + persisted). A full write-only stub sweep afterward came back **empty** — the tenancy surface is provably clean.

**#232 + migration 087 — SVI excludes guests.** 215 was still 211 members + 4 guests: the EF excluded test but not guests. Patched `.eq("is_external_user", false)` into BOTH member loads (#373); migration 087 purged existing guest/test snapshots (because `writeSnapshots` upserts-no-delete-orphans #374). Count **215 → 211** — the true, grown Rosehill roster (the old "170" was a pre-entry estimate). EF redeployed so it holds.

**#233 — LACR monthly range + top-5 celebration.** `lc_attendance_report.html` now takes a From/To range (defaults to the current calendar month) instead of a single date, for a monthly attendance celebration. Group % aggregates present-marks over the range ÷ (flock × distinct dates that actually had attendance — so a holiday nobody logged can't drag everyone down). Champions = groups at 100% across the period; when none reach it (the usual monthly case), a **Top-5-groups-by-%** celebration renders automatically with medals. Single-day range unchanged (`eventCount=1`). Range math functionally proven in node (champion 4/4=100%, runner 3/4=75%). Decided GROUPS not members (too many perfect members over a month).

**Also investigated (non-code):** the LACR "Self-Attested · Awaiting Your Confirmation" groups on 7/19 were CORRECT — those 6 LCLs simply hadn't logged in MLT; only their members self-attested (verified against the raw source breakdown: all 6 had only `self_attest`, zero `lcl_logged`). Spotted a possible gap: disciplers with flocks who aren't flagged `is_facilitator` (Cristy Millete, Dina Madeja) whose members fall into the silent "unassigned" bucket — a query offered, not yet run.

Invariants #372–#379 → **count 379.**

## ✅ COMPLETED JULY 22, 2026 — SESSION 78 (Family Huddle offline precache · the long-deferred S71 telemetry rebuild landed end-to-end — unified `app_sessions`, server-derived leader-ness, expired-session reaping, `leader_sessions` dropped · 6 PRs #219–#224 + migrations 081, 082 · 7 invariants)

**PRs #219–#224 · migrations 081, 082 · `multiply_shared.js` bumped `?v=11`→`?v=12` · invariants #365–#371 → count 371.**

**#219 — Family Huddle offline precache.** The S77 Family Huddle card (`family_huddle_card.html`) was network-first only, so a father opening it for family worship with no signal could get nothing. Added it to the SW `SHELL_ASSETS` precache list + bumped `CACHE_VERSION` v3-2026-07-20 → v4-2026-07-21 so installed PWAs purge the old shell and re-precache the card. Two-line SW change; precache is best-effort (`cache.add().catch`) so a missing asset can't break install, and the file's presence was verified (19,696 B) anyway.

**The telemetry rebuild — the arc (#220–#224).** Deferred from S71, where a phantom "93% engagement collapse" turned out to be an artifact of `auth-login` deciding leader-ness from a CLIENT flag (`body.stamp`) that only the legacy login page sent (#322/#323). This session closed it end-to-end, shipped in a strict deploy order so logout was never broken mid-flight, each step back-checked byte-for-byte vs origin/main before merge.

**Migration 081 — unified `app_sessions`.** One logbook for EVERY login (member + leader), replacing the leader-only `leader_sessions`: tenant RLS mirroring the old table, four indexes (active / church / history / reaper), a `reap_expired_sessions()` SECURITY DEFINER function, and a lossless null-safe backfill of all existing `leader_sessions` rows (`is_leader=true`). Proven on a throwaway PG16 — fresh run ok=t, rerun idempotent (no dupes), reaper closes only expired-open rows (#365/#367).

**#221 — `auth-login` server-truth.** `isLeader` now derives from the member row's `pipeline_level >= LEADER_MIN_LEVEL (2)` — never `body.stamp`, which is now ignored so old login pages still work (#322/#366). Every login (both stamp sites) writes an `app_sessions` row + opportunistically reaps; all telemetry best-effort/try-caught so login never depends on it. Validated via esbuild TS transpile + `node --check`.

**#222 — `token-login` cold-link capture.** The assessment / self-edit cold-link EF stamped nothing, so those logins were invisible to telemetry. Added the same `app_sessions` insert (`is_leader` from level, `expires_at` mirroring the token's own 2h TTL) + reap, all best-effort (#365). Pure additions; +16 lines.

**#223 — frontend repoint + the `?v=12` lockstep.** `logoutLeader` (`multiply_shared.js`) and the MD pin-reset writer now close `app_sessions` rows (`member_id`) instead of `leader_sessions`; two stale `index.html` comments trued up. All 21 shared.js consumers bumped `?v=11`→`?v=12`. Because the SW serves JS network-first, the `?v` bump alone delivers the new code — no `CACHE_VERSION` bump needed (#369). Delivered as one 22-file patcher self-gating on a single combined BASE+WANT sha (#370).

**Migration 082 — `leader_sessions` dropped.** The final, irreversible step (authored outside the chat, then back-checked here): guarded, rerunnable, self-verifying, and — the key — it RE-BACKFILLS any `leader_sessions` rows created after 081's backfill (e.g. logins on the old EF before its redeploy) immediately before `DROP TABLE`, so history survives the staggered cutover (#368). schema.json: `leader_sessions` gone, `app_sessions` in, still 74 tables. After this, nothing reads or writes `leader_sessions` (comments only).

**Verified LIVE.** Both EFs redeployed in the Supabase dashboard (the merges only record source #371); proven live by watching `select count(*) from app_sessions` tick 3221 → 3222 on a single login. The leader/member telemetry asymmetry (#322/#323) is closed: one honest logbook, server-derived leader-ness, expired sessions reaped, history preserved. Invariants #365–#371 → **count 371.**

## ✅ COMPLETED JULY 21, 2026 — SESSION 77 (The Giving Mirror (private on-device calculator) — attendance shows active enrollees only — Ministry Fit ported into MLT — the Family Huddle family-worship card + MMT embed — 5 PRs — ZERO migrations — 5 invariants)

**PRs #213-#217 - no migrations - `multiply_shared.js` untouched - invariants #360-#364 -> count 364.**

**#213 - the Giving Mirror.** The Giving Journey doctrine already SPECCED a private calculator (GIVING_JOURNEY.md Guardrail #5 + Sources #2/#6), so this slice built the mirror the doctrine promised: a quiet "My giving mirror (private)" button on the MMT Giving Journey card (L1+, reachable before any covenant). Income + what-you-set-aside in; the real percentage out, shown against the 10% tithe milestone (the ONLY fixed marker -- the other rungs are heart-moves, never amounts); plus an optional one-page budget that reveals margin. It persists ONLY to `localStorage` keyed by memberId with a Clear button and has ZERO server footprint -- no Supabase write, no covenant column, no rung tick, no `?v=` bump (#363). The rare fully-additive client build: it works for all tenants and can never leak because it never touches the network.

**#213 delivery near-miss (#360/#361).** Proven pure-ASCII-at-source, the patcher was first shipped IN-PROMPT and CC's WANT gate HALTED on a 20-byte divergence: a paste layer flattened the `\u2014`/`\u{1FA9E}` escapes into glyphs. Reshipped as a sha-verified UPLOAD -- byte-faithful, landed on WANT exactly. Also surfaced: uploads nest under a session subdir, so the locate must be a RECURSIVE sha walk, not a flat glob (#361).

**#214 - attendance = active enrollees only.** A Pastor bug report: withdrawn batch members still showed in Log Attendance and the "N members" count. `cohort_members` keeps withdrawn (`'withdrew'`) + graduated rows so the roster modal can render its History; the three attendance pickers (GP/BTLI/Usbong) and their counts were reading the full list unfiltered. Fixed all six sites with the `status==='active'` filter the roster view already uses (#364). Frontend-only; withdrawn members stay in History, their past attendance untouched.

**#215 - Ministry Fit in MLT.** The MD "Ministry Recommender" turned out not to be inline AI code but a shared standalone page (`ministry_recommender.html`) that MD merely opens per member -- and it authenticates off the shared `multiply_jwt`, so under an LCL's session RLS scopes its candidate list + reads to that LCL's own disciples, and its weight sliders stay pastor-PIN-gated. So the "port" was a REUSE: a per-member "Ministry Fit" card added to the MLT member detail (`buildDetailSections`) opening the same page scoped to the disciple. No AI duplicated, no SQL, no EF, no `?v=` bump. LCLs read the fit; they don't retune the engine.

**#216 - the Family Huddle card + MMT tile.** A pastoral deliverable turned platform resource: a bilingual family-worship card for new-believer fathers -- the field consensus is read + talk + pray, ~15 min, consistency over perfection (Deut 6). Built as a hybrid of FAITH5's warmth (highs/lows -> read -> ... -> pray -> a father's blessing) and Discovery Bible Study's reproducible question engine (God? people? "I will"? who will I tell?), with a worked example on Mark 10:13-16 (Jesus welcomes the children) whose possible answers are framed as a safety net, NOT an answer key, and six ready passages (lost sheep -> EOLO, the loaves -> a small gift used, foot-washing -> servant leadership). Shipped as a new standalone `family_huddle_card.html` (added to the repo) + a Journey tile in MMT (opened via `window.open` in this PR).

**#217 - reflow + embed.** The fixed print-cards read cramped on a phone, so the card was reflowed into ONE roomy, continuously scrolling document (mobile-first; the dads use phones/tablets anyway). And the tile now opens it EMBEDDED in-app: ported the minimal `_openLessonViewerModal`/`_closeLessonViewer` (a sandboxed full-screen iframe overlay + back chevron) from MLT/MD into MMT, switched the tile to `_openLessonViewerModal('family_huddle_card.html?embed=1','Family Huddle', true)` (#362), and gave the card a `?embed=1` body hook. This patcher was genuinely pure-ASCII (double-backslash `\\u` escapes) and shipped IN-PROMPT cleanly -- the counter-proof to #360. MMT now shares the embed capability with MD/MLT.

**All five merged, live on `main`, each back-checked byte-for-byte vs origin/main before merge.** Invariants #360-#364 -> **count 364.**

## ✅ COMPLETED JULY 20, 2026 — SESSION 76 (Meeting Prep freshman-readability · the duplicate-name cure = empty-only guarded member delete · the "Iba pa" check-in fix · the weekly attendance gate RESTORED after ~2 months silently gone · a SW cache bump · PRs #207–#211 + migrations 079, 080 · 5 invariants)

**#207 — Meeting Prep readability.** Both prompt builders (`mpBuildPrompt` devotional + `mpBuildPromptCustom` BYO/BTLI/paste/upload) now require the generated Facilitator + Participant guides (EN + TL) to be simple enough for a freshman high-schooler (~13) to both UNDERSTAND and FACILITATE. Prompt-text only — no migration, no `?v=` bump.

**#208 + migration 079 — the duplicate-name cure.** The LCL now has a danger-styled "🗑 Delete this member" at the bottom of the member detail (own-LCL/Pastor, mirrors Reset PIN), but it is EMPTY-ONLY and server-guarded. `mlt_delete_empty_member()` (079, SECURITY DEFINER, catalog-driven over every live FK into `members`, telemetry denylist, pre-clears NO-ACTION telemetry, refuses ANY formation footprint; **#355**). Proven on a throwaway PG16 with the real `auth_member_id()` pattern (5 behavioral tests) before shipping; surgical BASE→patcher→WANT. The refresh half was ALREADY shipped (`saveNewMember` calls `loadMyMembers()` after add). Typos are self-fixed by the disciple renaming in MMT (`editField('name')` → `members.name`), not by delete.

**#209 + migration 080 — the "Iba pa" fix.** A Pastor screenshot showed `violates check constraint lcg_checkins_reason_chk`: the S75 "Iba pa" reason submits `reason_code='other'` but migration 054's CHECK never learned `'other'`. NOT a cache/iPhone-install issue (the opposite — the user was on the NEW build). 080 widened the CHECK; a concrete recurrence of #290/#336 (**#357**).

**#210 — the weekly attendance gate RESTORED.** The Pastor noticed the Sunday/Wednesday log reminder was gone. `runWeeklyAttendanceGate` (Inv #100) had been SILENTLY DROPPED in a May-28 whole-file upload (`d463aaa`) and absent ~2 months — the invariants doc still described it (**#356**). Restored from last-good `10971c8`, stripped of the old announcement-wall coupling (the inbox owns announcements now), reconciled to today's file, boot call wired once-per-open in `loadMyMembers`, L2+ via `_liveLeaderLevel()`, fail-soft with the "I'll do this later" escape. Service-window resolver smoke-tested 7/7. (Its code comment mislabels the restore "Session 77" — harmless off-by-one; this is Session 76.)

**#210 delivery near-miss (#358).** The ~20 KB base64 gate-block homoglyph-corrupted in transit (16 Cyrillic look-alikes for Latin base64 letters); CC HALTED at `base64.b64decode` and refused to guess. Reshipped as a sha-verified whole-file UPLOAD — located BY its WANT sha, BASE+WANT gated (#338/#339) — byte-clean first try.

**#211 — SW cache bump.** `multiply-sw.js` `CACHE_VERSION` `v2-2026-07-17` → `v3-2026-07-20` so all installed PWAs purge the old shell + re-precache fresh after the merges. HTML/JS is network-first, so online users already get fresh; the bump refreshes the OFFLINE shell + forces a clean purge (**#359**).

**All merged, live on `main`, on-device green** (delete + gate + check-in fix all tested). Invariants #355–#359 → **count 359.**

## ✅ COMPLETED JULY 19, 2026 — SESSION 75 (The Giving Journey gate arc — L1 lockstep + “theirs to close” at any level + cumulative per-member rung gate · an "Iba pa" check-in reason · the lone CRLF blob renormalized · Meeting Prep source generalized to devotional | BTLI | paste | upload · 8 PRs + migration 078 · 7 invariants)

**PRs #198-#205 + migration 078 - `multiply_shared.js` untouched - invariants #348-#354 -> count 354.**

**The Giving Journey gate arc (#198-#199).** The Pastor noticed an L0 member showing all 6 giving rungs on his MLT companion card. Root cause: the L1 gate (#341) lived only in MMT; the leader-side card had no level check and had silently diverged. **#198** L1-gated `_refreshGivingCompanions` to mirror MMT (a dormant-L0 disciple is hidden, not deleted, and reappears at L1 -- #348) AND fixed the trap the same gate created: a member who opted in while ungated then dropped to L0 had her whole giving section blanked and could never reach her own off-switch. Now MMT hides only the invitation *door* at L0; an already-open covenant always renders so it stays the member's to close at any level (#349) -- a deliberate asymmetry (leader companions active journeys; member always controls their own covenant). `GIVING_JOURNEY.md` line 109 was trued up to "visible from L1 onward" as #341 had already mandated. Juana's specific orphaned covenant was closed with one human-gated SQL (`status='closed'`). **#199** then added the Pastor's refinement: the full 6-rung path stays visible to the member as aspiration, but on the LCL's card only rungs at/below each disciple's own level are *checkable* -- higher rungs render locked ("Opens at Level N"), per-member, with a defense-in-depth guard in `_gjAffirm` (#350).

**The "Iba pa" check-in reason (#200).** The LCG Pulse leader check-in gained an 8th "Iba pa" (Others) reason above the Detail box. Unlike the seven fixed reasons (which submit on tap), Others requires the free-text -- an empty "other" tells the pastor nothing -- so `_submitOther()` focuses the box and prompts when empty. `CHECKIN_LABELS` in the Pulse report learned the new code so the reply pill renders (#351).

**Migration 078 + repo hygiene (#201-#202).** **078** added `source_kind` ('devotional'|'btli'|'custom', default 'devotional', CHECK) + `source_title` + `source_text` to `lc_meeting_guides` (additive, rerunnable, self-verifying; applied in Supabase, committed via #201). **#202** renormalized the one tracked file carrying a CRLF blob -- `lessons/btli101_xrw5fg/btli1_l3_participant.html` (561 CRLF lines) -- back to LF, restoring Invariant #3. CC caught it (a stop-hook flagged the dirty file) and correctly refused to fold it into the migration PR. The key lesson: codeload/`raw`/`git show` all apply `.gitattributes` eol normalization and so are BLIND to a CRLF blob -- it was located via a clone + `git ls-files --eol`, fixed from the raw bytes via `git cat-file blob`, proven eol-only via `git diff --ignore-space-at-eol`, and back-checked via clone (not codeload) (#352).

**Meeting Prep Bring-your-own-material (#203-#205).** Meeting Prep's source was hard-wired to the daily devotional. The BYO-AI prompt skeleton (RULES + 1-hour flow + EN/TL fences) is actually source-agnostic -- only the SOURCE block varies -- so the arc generalized it. **#203** added a Step-1 source toggle (Devotional (by date) | Bring your own material); the custom path takes a Title + pasted Material and builds the two guides via a new `mpBuildPromptCustom` under a hard Scripture-anchor guardrail: because an article/story isn't Scripture, the AI must anchor the session in a named Bible passage (the material illuminates the Word, never replaces it). Save stamps `source_kind`/`source_title`/`source_text` (#354). **#204** added a "Start from a BTLI lesson" pre-fill, populated from the published BTLI catalog -- `pipeline_lessons`, leader-readable via RLS (`church_id IS NULL OR own`), `track='BTLI'`, deduped by overlay precedence (#353) -- seeding Title + aim + scripture as an editable springboard (the LCG-weekly text isn't in the catalog, only in lesson HTML), stamped `source_kind='btli'`. **#205** added `.txt`/`.md` upload (client-side FileReader, capped 500 KB, nothing sent to a server) into the same material box. Four sources now: **devotional - BTLI - paste - upload**, all client-side, and the My-lessons/Library title lookups surface `source_title` for any non-devotional guide.

**Process:** every surgical PR ran BASE->patcher->WANT through CC, back-checked byte-for-byte vs WANT before merge; a CC tab was once pointed at the wrong repo (`eastbridge-op`) and the BASE gate halted it cleanly; migration 078 was human-gated + committed as LF. Invariants #348-#354 -> **count 354.**

## ✅ COMPLETED JULY 17, 2026 — SESSION 74 (The AI-tailored salvation Debrief Guide, completed end-to-end — the LCL's guide now speaks in the member's own words · + the `.nojekyll` deploy cure and SW auto-purge · 6 PRs · ZERO migrations · 6 invariants)

**PRs #191–#196 · no migrations · `multiply_shared.js` untouched · invariants #342–#347 → count 347.**

**The arc — the Debrief Guide learns to speak in the member's own words.** The LCL's salvation Debrief Guide (`facilitator_debrief_guide.html`, shared by MD + MLT) began the session as a static A–H scaffold with `[bracket]` fill-in cues; it ended tailoring itself to each disciple's actual diagnostic answers, with no raw brackets left except the Outcome-section verdicts the LCL fills live.

**#191 (MLT)** — surfaced the debrief where the LCLs actually are: Zone 2 relabeled from a misleading "Stable & Growing 🔵" to **"Uncertain 🟡"** (the pastoral blind spot), Debrief + Intervention buttons shown for any zone ≥ 2, and both opened as in-app modals via `openDebrief`.

**#192** — **AI-tailored debrief**: a Standard↔Tailored 🎯 pill; the existing BYO-AI panel now emits a `tailored` map, and the AI resolves each tagged ask-box's `[bracket]` from the member's own salvation `open_answers` — under a hard no-verdict guardrail (the AI never rules on salvation, never invents quotes).

**#193** — parser hardening (curly quotes, trailing commas, unescaped inner double-quotes normalized), the Past-Debriefs modal viewport-anchored in embed mode, and the header renamed **"AI Assisted Debrief."**

**#194** — the revert bug: standard text had been stashed in a `data-std` DOM attribute where quotes corrupted the round-trip; moved it to a JS map (`_stdAskText`); tailored mode now persists in `ai_parsed_json._guideMode` and auto-re-applies on resume.

**#195 (the completeness pass + first full patcher delivery)** — the Pastor caught that the tailoring was **uneven**: A5 and B2 had rich analysis but their guide questions stayed generic, and `[their B1 score]` showed a raw bracket. Root cause (Inv #345): `diagnostic_results` stores only **section aggregates**, never the per-question B1/habit scale scores — so those brackets were un-fillable by anyone. The fix realigned the guide to the data we actually have: **A5** now references the member's basis-of-acceptance answer (the single most diagnostic question, tagged `a-basis`); the **B** section repoints from the un-stored B1 score to the stored **B2** free-text answer (`b-basis`); the **fruit box** fills `[highest/lowest-rated fruit]` from the nine `section_fruit_*` scores; **E habits** reworded to drop the un-stored `[lowest habit]/[score]`; and **`[Name]`** fills the member's first name at load via `_fillPlaceholder` (leaf-only, so it never flattens child markup — Inv #346). Folded in the fresh-visibility fix: a fresh AI parse now auto-applies the tailored guide and scrolls to it (no more stranding the LCL on the analyses panel), with a scroll on every toggle. This was the **first full BASE→patcher→WANT delivery through CC** (Inv #347) — patcher proven in `/tmp`, BASE `297002d6` / WANT `599448af`, `node --check` clean, fill logic + determinism unit-tested, back-checked byte-identical before merge.

**#196 (SW auto-purge + the deploy cure)** — after #195 merged, the fix wouldn't appear on-device. The data proved it wasn't the browser: `main` had the new file, but the published **`github-pages` deployment** was an older commit whose file was still old (Inv #343). The **"pages build and deployment"** Action had gone **red** — the build log showed `GET api.github.com … 503`: GitHub Pages runs **Jekyll** by default, and its metadata plugin's API call failed during the session-long 503 storm (Inv #342). Re-running the workflow published the fix once the storm cleared; then #196 shipped the permanent measures — a **`.nojekyll`** file (MULTIPLY is pure static — no `_config`/layouts/Gemfile — so Jekyll was pure overhead and a single point of failure) and a **CACHE_VERSION bump** to `multiply-shell-v2-2026-07-17` so the service worker purges stale caches on next load and LCLs never hit the "Clear site data" dance (Inv #344).

**On-device: all green** — `[Name]` filled, A5 and B2 tailored from their own answers, fruit names rendered, and the fresh parse lands straight on the tailored guide. The whole debrief tailoring arc is closed. Invariants #342–#347 → **count 347.**

## ✅ COMPLETED JULY 16, 2026 — SESSION 73 (The Giving Journey shipped end-to-end — covenant table + 6 rungs → member door → companion card → L1-gate + affirm-as-formation · MMT empty-state study card · 6 PRs + migration 077 · ?v=11 untouched · 8 invariants)

**PRs #184, #186–#189 + migration 077 · `multiply_shared.js` untouched (?v=11) · invariants #334–#341 → count 341.**

**The Giving Journey (Landas ng Pagtitiwala) — the doctrine that money is a matter of the heart, made into code.** A five-part arc on a member-consent model: giving is opt-in, companioned, and never measured. **Migration 077** created `giving_covenant` (member-insert-only RLS, `member_id`≠`partner_id`, one-open-per-member partial-unique index, NO finance columns — consent + companionship only, #330) and seeded **6 base rungs L1–L4** (`category='formation'` — the CHECK had to be widened, #336; `celebrate_publicly=false`, `trackable=false`, `meta.track='giving'`): First Fruits & A Rhythm of Giving (L1), Proportional Giving & The Tithe (L2 — the Pastor chose "Kita/income %" over "Biyaya/grace" so the percentage is taught honestly from the start), Sacrificial Giving (L3), A Generous Life (L4), each in formation language. **#186 (MMT)** — the always-visible invitation *door*; the six rungs render only when an open covenant exists; opt-in is a "double yes" naming the discipler as companion, and closing it is the member's alone. `renderPathway` filters `track=giving` out of the normal metro AND its done-count so non-participants never see it (absence is not a verdict, #334). **#187 (MLT)** — the companion card on the leader Home, showing only the disciples who named *this* leader as partner (RLS `partner_id=auth_member_id`), invisible when none, no church-wide view even for the pastor (#332). **#188 (MMT)** — L1-gate the door (the formation track begins at L1, not L0, #341) + affirmed rungs read as formation ("becoming true in you"), never a checkmark or count (#335). **#189 (MLT)** — the companion card is now interactive: expand a disciple, affirm/un-affirm their rungs in a one-on-one, writing to `pathway_progress` via the proven `_mltPwToggle` pattern (church_id trigger-stamped, completed_at defaulted, delete keyed by member+level+rung, discipler-only, #337). The Pastor **pushed back on pure self-attest** — the LCL does the checking, one-on-one, like every other rung, because the discipleship template is Jesus ("whatever I have commanded you"): leaders pass on what they received.

**#184 (MMT, the session-opener)** — the "This Week's Study" card now ALWAYS renders with a bilingual empty-state ("No study shared this week yet / Wala pang aralin ngayong linggo") instead of hiding when empty, so pastors-in-training stop thinking the feature is missing.

**Non-code:** the Agape pastor "not seeing the study card" was diagnosed as a **content gap, not a bug** — `lc_meeting_guides` RLS shows a guide only if you authored it, or it's published by your discipler, or published + shared-to-library + you're a facilitator; Agape had zero published guides. Fix = an Agape LCL authors + publishes + ticks "share to Library." Working as designed.

**Process (a new device, three near-misses, zero damage):** CC was caught on a stale S44 clone, then the *wrong repo* (`eastbridge-op`), then a stale post-device-switch session — each halted by the BASE/WANT gate + CC's read-first discipline (#338); the upload lands at a separate `/root/.claude/uploads` path, targeted by a `UP` variable proven by WANT, not a glob (#339); codeload tarballs lag like raw and were SHA-checked after every merge (#340). Migration 077 run in prod (verified clean); all five PRs back-checked byte-identical before merge (#318). Invariants #334–#341 → **count 341.**

## ✅ COMPLETED JULY 14, 2026 — SESSION 72 (MMT reading typography + Easy Read · MMT "Add my own" prayer item · the Giving Journey doctrine, repo + manual · Preaching Calendar embedded everywhere · 5 PRs · ZERO migrations · 7 invariants)

**Session 72 — the session that wrote a doctrine and fixed a font a member thought was blinding him.** 5 PRs, **zero migrations**, one `shared.js ?v=10→?v=11` bump handled clean across all 21 consumers. Every branch back-checked byte-for-byte (`cmp`-identical to the proven copy) before merge (#318).

**#178 — MMT reading typography (#326, #327, #328).** A member reported "wavy capital letters" in the devotional and feared for his eyesight. The audit corrected the handoff twice (#327): the A−/A+ slider **already existed**, and there was **no uppercase in the reading flow** (all 31 rules are chrome). The real defect: **25 reading surfaces** set in Fraunces (a DISPLAY face, WONK axis) — and **twelve were inline in JS template strings**, invisible to a CSS grep (#328): the sermon body, three quiz stems, the quiz reflection textarea, lesson prose, the Library transcript, encouragement blocks, plus the daily verse card, the reflection textarea, EOLO/heads-up fields. Fix: three tokens — `--font-read` (Literata), `--font-display` (Fraunces stays on h1–h3/names/avatars), `--font-easy` (Atkinson Hyperlegible) — and `body.easy-read` re-points `--font-read`, flipping stylesheet *and* inline styles in one class. Scripture + prayer lose their italic (italic + high contrast + a run of capitals like "LORD" was the worst case). **Easy Read** = a third bilingual FAB pill, `multiply_mmt_easyread`, Atkinson lazy-injected on first toggle. Fraunces declarations 58→34. **Tested on-device.** **PASTORAL: the door-frame check remains open** — metamorphopsia is a retinal symptom; the font fix must not mask it.

**#179 — MMT "Add my own" personal prayer item.** The only way to add to the personal prayer list was to save someone else's request. `source:'personal'` was already in the Wave 1 CHECK constraint and the render path already drew these items (Sarili/Personal tag, Mark-answered button) — the button + compose modal were simply never built. Pure MMT front-end: an "✍️ Add my own" button, a private compose modal (title + optional details, "stays on your list only"), one `source:'personal'` insert. No migration. **Tested on-device.**

**#180 — The Giving Journey doctrine (#330–#333).** A request to "help disciples grow in generosity" became, through the pastor's own pushback, a full pastoral doctrine (see `GIVING_JOURNEY.md`). The heart-issue framing (#330), the discipler-owns-the-path/disciple-owns-the-yes resolution (#331), absence-is-not-a-verdict + the finance wall (#332), and the two-homes commit (#333): `GIVING_JOURNEY.md` (repo, builder voice) + a new "The Giving Journey" chapter under a "The heart of MULTIPLY" nav group in `md_manual.html` (pastoral voice, plumbing stripped) + one pointer line in `GRACE_PATHWAY.md`. **Transport lesson (#329):** the new-file heredoc drifted 2 bytes; CC caught the SHA mismatch (held, did not commit), re-shipped byte-exact via base64 decode.

**#181 — MLT Preaching Calendar embedded (Quick Action).** It was the lone Quick Action using `window.open('_blank')` — a new tab escaping the app frame. Repointed to `_openLessonViewerModal` (the same in-frame sandboxed-iframe modal the four reports + manual use). No `?embed=1` needed: `preaching_calendar.html` reads identity from `sessionStorage`, which a same-origin iframe shares, so the JWT-backed swap path works embedded. One anchor. **Tested on-device.**

**#182 — Preaching Calendar embedded EVERYWHERE + dead back button removed (`shared.js ?v=11`).** The pastor caught what #181 missed: the shared "📅 View Calendar" **banner** button (in `multiply_shared.js`, rendered in BOTH MMT and MLT) still opened a new tab. Added an `opts.onViewCalendar` callback (mirrors the existing `onRequestSwap` pattern); MLT wires it to `_openLessonViewerModal`, MMT to its own `_mmtOpenLessonViewerModal` — same mechanism, per-tool name. `window.open` fallback kept for `preaching_calendar.html` itself (renders the banner, has no modal). Also removed the calendar's internal `← Back` button + its dead `goBack()` (`window.close()` is a no-op in an iframe) — the modal's own back arrow is the exit. `shared.js ?v=10→?v=11` across all 21 consumers (SW cache lockstep, #310). 22 files: 4 real edits + 18 pure ?v-bumps. **Merged.** `CLAUDE.md` prose still said "currently v=10" — trued to v=11 this session.

**PARKED — the covenant-gate Giving Journey (fully designed, build-ready, awaiting the pastor's Tagalog on 6 rung titles):** a `giving_covenant` table (one row/member, member-insert-only RLS, names one partner, holds consent + companionship — **no income, no percentage column**), a render gate in MMT `renderPathway` + the MLT mirror (giving rungs appear only when an open covenant exists, on both the member's and the partner's view), six base rungs (`category='formation'`, `celebrate_publicly=false`, `church_id=NULL`, bilingual) seeded once for every tenant, and a "＋ Begin the Giving Journey" opt-in on the pathway card (the member's thumb; the LCL can never flip it). One migration + MMT + MLT edits when built. **STILL OPEN (non-code):** the door-frame / metamorphopsia check with the brother.

**Invariants #327–#333 → count 333.**

## ✅ COMPLETED JULY 11, 2026 — SESSION 69 (SVI Wave 5b-2 per-church metric windows + Option B multi-ministry bonus scorer · 3 PRs + migrations 071/072 · 6 invariants)

**3 PRs, each back-checked byte-for-byte vs Claude's proven `/tmp` output before merge.**

**5b-2a — per-church windows engine (PR #157, migration 071).** New `public.svi_metric_overrides` (`church_id`, `metric_key`, `compute_config_override jsonb`, `is_active`, `UNIQUE(church_id, metric_key)`) lets a church tune any count/rate metric's `lookback_days` without touching the GLOBAL `svi_metrics` catalog (#032). RLS = the 4-policy `auth_church_id()` pattern + a `set_church_id_from_jwt` BEFORE INSERT trigger (client sends no `church_id`, #266); a regular composite unique so `onConflict` COULD target it, but the client still does manual insert-or-update-by-id for robustness (#303). The EF resolves each member's window as `override.lookback_days ?? global`, keeping the bulk prefetch single-pass by fetching the WIDEST window once then bounding each member by their OWN church's window (`effLookback`/`widestLookback`); only `count_rows`/`rate_rows` honor per-church windows — `sql_query` metrics compute their own (#304). **Latent bug caught (#305):** `allLite` selected `id,pipeline_level,discipler_id` but NOT `church_id` → `liteById.get(id)?.church_id` always undefined → the `events_held` prayer denominator keyed every held-date to `""` and dropped them → prayer n/a for ALL members since Wave 5b-1. The one-line fix (add `church_id`) revived prayer as a live weight-10 dimension — caught by spot-checking the dry-run against Gerry's REAL attendance (548 present / 22 held dates), not synthetic. Proven: prayer scores returned (Airish 0.333→4, Agustin 0.067→0, Abraham 0→0 as a real care-flag), per-level weighting honored (L0 Aby unaffected — prayer not in `level_0`).

**5b-2b — MD SVI Windows UI (PR #158).** Pastor-only "🗓️ SVI Windows" Settings section (mirrors the SVI Weights editor): one days-input per window-able metric (`count_rows`/`rate_rows` with a `lookback_days`), pre-filled `override ?? global`. Save logic — input ≠ default → insert/update the override; input = default → DELETE it (revert to global, never store a redundant row); JWT'd `db` + RLS + autostamp scope to the pastor's church; reuses `_sviInvokeCompute()` + `loadCareRadar()`; Save & Recompute applies now (#306). Per-church window PROVEN end-to-end (Rosehill prayer window→28d moved one member 60→100).

**Option B — multi-ministry bonus scorer (PR #159, migration 072).** Reward multi-ministry service WITHOUT lowering anyone: grading the existing `service_ministry_role` boolean into a count (Option A) would have dropped role-only members (ministry_role but no named ministry) 10→0. Option B keeps the boolean untouched and ADDS `service_multi_ministry` — n/a (null) for 0–1 ministries, 7 for two, 10 for three — so only the 38 multi-servers rise, nobody falls (#307). Enabled by a new GENERIC `count_fields` compute_type (counts non-empty configured member columns — `ministry`/`ministry2`/`ministry3` — returns null below `min_count`, CHECK-widened first #290/#302); seeded at weight 0 (zero effect until the pastor weights it — no surprise shift). Data checked first: 38 members serve 2+ ministries (8 two, 30 three), and the 30 triple-servers verified as real distinct ministries, not placeholders. Migration 072 run (`all_ok=PASS`), EF redeployed, metric weighted.

**Process (#308).** A FIND/REPLACE anchor emitted by SLICING a region out of the proven EF carried a phantom trailing blank line — the slice already ended in `\n` and the emit template wrapped it as `\n{slice}\n`, adding a second → the FIND matched ZERO times against the file (which had one blank between functions). CC correctly stopped, reported loudly (#298), and re-anchored on the next stable line; the merged result was byte-identical to intent. The verify-by-reapply guard checks the OUTCOME but not the emission artifact (it re-applies the in-memory strings, not the wrapped prompt text). Fix: emit slices verbatim — never re-wrap a newline-terminated slice. Invariants #303–#308 → **count 308.**

## ✅ COMPLETED JULY 10, 2026 — SESSION 68 (SVI Wave 5b-1: the rate engine — `rate_rows` compute_type · rostered vs events_held denominators · #290 CHECK-widen · 1 PR + migration 070 · 3 invariants)

**PR #153** + **migration 070** (already run, `all_ok=PASS`, committed per #242). Wave 5a was count-first; **5b-1 adds the `rate_rows` compute_type** to `compute-svi-weekly` — a denominator prefetch + `computeRateRows` handler returning a 0..1 rate, fed to the ALREADY-existing `rate_to_score` (scoring half pre-existed; only numerator÷denominator was missing, #300). **Two denominator modes (#301):** **`rostered`** (SS/BTLI/Batch) = member's own present+absent → present/(present+absent), so **an absence lowers the rate** (Gerry's rule) and 0-logged = not-enrolled = null (n/a, #281 preserved); **`events_held`** (Prayer, church-wide) = distinct church-held dates → present/held, held=0 = null, present=0 while held>0 = 0 (counted). Both n/a paths route through `null_if_zero`. **#290 in practice (#302):** `svi_metrics_compute_type_check` allow-list did NOT include `rate_rows` — migration 070 DROP+re-ADD the CHECK first. Converted the 4 attendance metrics in place (defaults: rate ¾+→10, ≥40%→7, ≥15%→4, tunable in 5b-2); the other 6 stay counts. EF redeployed (Verify-JWT OFF), recomputed. **Result on live data: dormant lessened, thriving increased.** Delivery: readable `<<<FIND>>>/<<<REPLACE>>>` anchor blocks (#298); back-check byte-for-byte clean (index.ts + 070 identical to proven copies, scope = exactly 2 files). Invariants #300–#302 → **count 302.**

**Resolved (PR #155):** care-detail now renders rate metrics as "attended X% of …" — `attended 75% of prayer meetings held` (events_held) / `attended 60% of your Batch sessions` (rostered), MD+MLT lockstep. Optional "75% · 6 of 8" transparency deferred (needs the EF to store numerator+denominator) — a candidate to fold into 5b-2.

## ✅ COMPLETED JULY 10, 2026 — SESSION 67 (Meeting Prep arc A→B→C · MMT reads Tagalog · Library own-shared · LC Encouragement · MLT manual embed · base64→str_replace delivery · 8 PRs + migrations 068/069 · 8 invariants)

**8 PRs, each back-checked byte-for-byte vs current `main` (and vs Claude’s proven `/tmp` output where Claude authored) before merge.** **LC Encouragement (#145)** — the LCG Pulse “🌱 Building Rhythm” tier gets a one-way pastoral nudge (`kind='encouragement'`, `status='sent'`, editable `lcg_encouragement_greeting`); **migration 068** extended the `lcg_leader_checkins` CHECK to three kinds (tri-kind, route by `kind`, #289/#292; CHECK-DDL-superset again #290). **MLT manual embed (#146)** — the User’s Manual opens in `_openLessonViewerModal(...?embed=1, label, true)` + Samsung off-canvas fix, not a new tab (#293). **Meeting Prep — the full arc (`lc_leader_tool.html`):** **#147 Phase A** made `openMeetingPrep` a browsable “My lessons” list + read-only modal (EN/Tagalog pills, forward-compat to `*_md_tl`); create→publish moved behind “+ New lesson”/“Edit.” *(Accidentally cut off a stale local `main` missing #146; the back-check caught it → rebased → fetch-first rule #294.)* **#148 Phase B** + **migration 069** added the church **Library** (other leaders’ `published + shared_to_library`, church-scoped by a leader-read RLS branch), **Share-to-my-LCG** which CLONES a library guide into a row the borrower owns (`source_guide_id` provenance, date defaults today, confirm-replace, #295), an opt-in “share to Library” checkbox, the `source_guide_id`/`shared_to_library`/`facilitator_md_tl`/`participant_md_tl` columns, and `auth_is_leader()` — a SECURITY-DEFINER `is_facilitator` helper mirroring `auth_discipler_id()`, preferred over an inline EXISTS to avoid members-RLS recursion (#296). **#149 Phase C** added a group-level **celebration opener** to the top of the timed facilitator flow (0:00–0:03 name-the-wins; personal Positive/Negative stays 0:03–0:08), switched the AI prompt to **English + Tagalog** in four fences, taught `mpParse` to split four sections while staying backward-compatible with the old two (#297), added a revealed-on-demand Tagalog editor writing `*_md_tl`, and made “+ New lesson” open blank at today. **MMT reads the Tagalog guide (#150)** — Phase C taught the leader tool to SAVE `participant_md_tl` but `member_tool.html` still rendered `participant_md`; the fix reads the right source column (`_mtgGuideBodyHtml` picks `participant_md_tl` when `document.body.lang-tl` and non-empty, else EN) and re-renders the open guide sheet on `setLang()` — because the CSS `.tl-text`/`.en-text` toggle can’t swap rendered-markdown content (#299). **The bilingual meeting-guide loop is now closed end-to-end: leader authors EN+TL → member reads in their language, flipping live.** **Library own-shared (#151)** — the Library now lists your OWN shared guides too (dropped the self-exclusion; own cards tagged “· you”; opening your own offers Edit, not clone-to-self, via a per-guide `_mine` flag). **Process shift (#298):** an 18 KB base64+sha patcher drifted in chat transit — decoded and `ast`-parsed clean but the sha256 no longer matched, and CC’s `sha256sum -c` gate correctly refused it. Switched delivery to readable `str_replace` **anchor blocks** (`<<<FIND>>>/<<<REPLACE>>>/<<<END>>>` in one copyable block, `ast`-extracted from the proven patcher); Phase C and every build after reshipped and back-checked **byte-identical on the first try.** Claude still proves each edit on a `/tmp` copy (node --check + jsdom) as the back-check reference; `str_replace` match-exactly-once is the drift guard. Both migrations **068 & 069 run** in Supabase. Invariants #292–#299 → **count 299.**

## ✅ COMPLETED JULY 8, 2026 — SESSION 66 (SVI manual pair · full in-app embed sweep — viewers + editors · Celebrate feature end-to-end — send→Amen→LC feed + perfect-Wednesday badge · 8 PRs + migrations 066/067 · 5 invariants)

**8 PRs, every one back-checked byte-for-byte before merge.** **SVI docs:** #132 pastor-manual (`md_manual.html` Ch.2) brought SVI to Wave-5 reality — the 10 count-first signals table, a "How the number is built" methods section (score-rules→weighted-blend→n/a→Insufficient→trend), per-church/per-level weights, care-detail units, and the six→seven dimension fix; #133 leader-manual (`mlt_manual.html` Ch.5) shepherding playbook (four states, trend, detail-drilldown, Taglish check-ins, celebrate-not-compare, Luke 6:40/Gal 5:22-23). Split by audience (#287). **Embed sweep:** #134 the two viewers (Pastor's Manual, Leadership Pipeline) + #135 the six editors (attendance/devotional/quiz admin + transfer/bulk/preaching) — all MD/Settings pages now open in the in-app `_openLessonViewerModal(...?embed=1, label, true)` modal; each adds the embed-class script + `html.embed` fixed→absolute Samsung CSS; Tier-2 editors hide own back-chrome + guard `goBack` with `_exitTool()` → `window.parent._closeLessonViewer()` instead of navigating the iframe (#288). **Celebrate (full arc):** #138 P1 — "Send celebration in-app" + log + champion chip in the LCG Pulse Report, kind-aware `loadCheckins`; **migration 066** added `kind` (checkin|celebration) and extended the status CHECK to allow `'sent'` (my miss — `status:'sent'` would have violated `lcg_checkins_status_chk`; CC caught it → verify CHECK DDL not just column defaults, #290). #140 P2a — leader receives it in MLT (banner + 🙏 Amen sets `responded_at`, the publish gate). #141 P2b — the 🏆 group badge in the LC celebrations feed; **migration 067** added an additive member-read RLS policy gated on `auth_discipler_id()` (the SECURITY-DEFINER "my LC leader" helper, 047). #142 P2c — the 🙏 Wednesday Prayer Meeting perfect-attendance badge (mirrors Sunday; `mostRecentWeekday(3)` + `event_type='Prayer Meeting'`). `lcg_leader_checkins` is now dual-kind (#289); the LC celebrations feed is a computed group-badge assembler, auto-wins pull/compute not materialized (#291). The chain: **pastor Sends → leader Amens → 🏆 lights up for the whole LC.** **Resolved along the way:** the "Ministry Meeting" Heads-Up dropdown = config-not-bug (add via MD → Heads-Up Settings, a separate list from the MLT ministry-attendance select); back-checked + green-lit CC's devotional file-upload branch. **DB reminder:** run migration `067` in Supabase (the P2b badge stays dark until then; `066` already run). Invariants #287–#291 → count 291.

## ✅ COMPLETED JULY 6, 2026 — SESSION 58 (the per-church Leadership Pipeline — Full-Unify catalog · data-driven + L5-editable poster · church-stamp + interleave-order · True North onboard · Pulse Building-Rhythm · noopener loop fix · 8 invariants)

**Path A — Full Unify (migrations `059`/`060`).** `059` added `pathway_rungs.meta jsonb`. `060` migrated the entire hand-authored static poster into the catalog: a `trackable boolean` splits the member-progress spine (37 rungs, `trackable=true`) from the rich reference layer; 167 reference chips seeded from the static `leadership_pipeline.html` (`trackable=false`), and the 11 spine rungs with a matching write-up got their `meta` attached — **204 base rungs** total (auto-generated by parsing the static file, parse-validated with pglast + executed against a throwaway Postgres, rerun-safe). One catalog now feeds both the diagram and the tracker (#265).

**Data-driven poster (PR #108).** `leadership_pipeline.html` rebuilt from static (2,494 lines) to data-driven (653 lines): reads `pathway_rungs` (base `church_id NULL` + own-church overlay, RLS-scoped), overlay-wins-by-`level|rung_key`, `published:false` tombstoned; preserves the parchment CSS, 5-column level frame, badge cells, filter bar, modal shell; chip detail modal renders from `meta`. Verified via jsdom (overlay-shadow + tombstone-hide + modal-fill).

**L5 Edit Mode (PR #111).** Overlay CRUD on the poster: add · customize-base · remove/tombstone · restore · edit-own · delete · publish-toggle · trackable-toggle. Tenancy is server-authoritative — migration `061` attaches the **base-safe** `set_church_id_from_jwt()` trigger to `pathway_rungs` (stamps only when NULL; base rows stay NULL), so the client sends **no `church_id`** (#266); RLS enforces L5-own-church. customize/tombstone are **manual insert-or-update** (load `OVERLAY_BY_KEY`), NOT `upsert(onConflict)` — the overlay unique is a PARTIAL index PostgREST can't target (#267, caught by CC's back-check). Levels locked. Non-editor path unchanged (jsdom test 1).

**Interleave reorder foundation (migration `063`).** `pathway_section_order` (one row/church, `order_map jsonb`, RLS L5-own + stamp trigger) enables full base+overlay interleave reorder keyed by `rung_key` — chosen over `churches.settings` because `churches` is SELECT-only under RLS (#269); reuses the pathway_rungs RLS+trigger pattern, no EF (#270). **Step 3b (PR #112) — show-hidden-own-overlays (closes CC's Note-1 foot-gun) + ↑/↓ interleave reorder — was IN FLIGHT at session close.**

**True North onboarded (migration `062`).** 2nd tenant church for overlay-isolation testing: slug `truenorth`, church_id `03ce9f8d-292c-449e-8a4e-c164cbf4d29c`, L5 pastor Timothy (NULL pin → first-login `set_pin`). Mirrors the `051` onboarding template.

**LCG Pulse — Building Rhythm (PR #107).** New **🌱 Building Rhythm** card section (met this week but not every week) closes the Champions/Heartbeat coverage gap (e.g. Rosana Cordero); partition proven exhaustive + exclusive; solo-LCG (flock ≤1) no longer marked weak; all sections alphabetical by LCL (#271).

**noopener redirect-loop fix (direct `f2abd2f`).** The pipeline launcher used `window.open(...,'noopener')`, which opens a session-less tab (sessionStorage not copied) → `gateOrRedirect` loop. Removed `,'noopener'` to match the working pulse launch (#268). Surfaced when Pastor Timothy first opened the pipeline.

**Process note (#272).** `059`/`061` were applied to the DB but not committed to `migrations/`, which broke a CC repo-grep back-check — commit every migration immediately; trust the ledger + `schema.json` over a grep.

**Still open (pipeline roadmap):** merge #112 (3b); **Step 4 — ✨ AI-research chip modal** (the headline BYO-AI feature: click an empty chip → ready-to-paste prompt with the church frame baked in → paste any AI's JSON back → enrich `meta`); pathway v2 auto-wiring; Five Looks accountability; Flock Map in MD; EGR→evangelism redesign; BTLI L12 (UNLAD-L2 scan). Invariants #265–#272 → **count 272.**

## ✅ COMPLETED JULY 4, 2026 — SESSION 57 (Leadership Pathway v1 — per-level rungs, MMT read + MLT marking, church-isolated · BTLI L2 deck rebuild + generic BTLI_SLIDE_SPEC · 5 invariants)

**Pathway v1 SHIPPED** (migration `057` + PRs #101/#104/#105/#106): `pathway_rungs` (catalog, partial-unique base+overlay #253) + `pathway_progress` (per-member, LCL-marked). 37 base rungs seeded L0–L5 (Usbong/BTLI/Leading-Yourself/Glorious-Hope/APIIS/EOLO/assessments/devo). RLS: member reads own, LCL marks own disciples via `discipler_of()` (#254), L3+ coach church-wide, L5 pastor any — self-verify green (37·6·true·true·4·4). **MMT Journey** `renderPathway` read surface (ladder + clickable L0–L5 metro, bilingual, "My pathway" + footprints); **MLT** `renderMltPathway`/`_mltPwToggle` LCL marking. Ladder→footprints identity across both tools (#264). Auto-vs-manual schema-ready; v1 manual-only (#260).
**Design decided (parked for build):** SVI = care lens not advancement gate; Flock Map vitality×progression quadrant (#261). Per-church pipeline = shared level frame + per-church rung overlay via `published` (#262). MLT "nudge" prompt for healthy-but-stuck disciples.
**BTLI L2 "Bible Meditation" deck** (side task, Sunday teaching): rebuilt to 23 slides — 5 dedicated dark "TANONG" question slides each tagged Participant Guide Question, per-slide timer badges (55-min tempo), generous facilitator notes on every slide (Presenter View), "Participant Guide" wording. Optional live-countdown HTML companion delivered on request (not part of the standard). Codified as **`BTLI_SLIDE_SPEC.md`** (repo root, generic for all BTLI lessons, uploaded direct to main #264, #263).
**Next-up queue:** (1) per-church pipeline authoring surface; (2) pathway v2 auto-wiring; (3) Five Looks accountability build; (4) MLT "nudge" prompt; (5) Flock Map in MD; (6) #253/#218 catalog partial-index audit; (7) DROP `_backup_*` + v_* pass; (8) members INSERT hardening + pin `050` search_path + members SELECT privacy; (9) EGR evangelism redesign; (10) BTLI L12 (UNLAD-L2 scan). Invariants #260–#264 → **count 264.**

## ✅ COMPLETED JULY 2, 2026 — SESSION 55 (LCG shepherd's check-in loop end-to-end · tenancy-unique sweep 052/053 + repo-hygiene consolidation · security lockdown 055 · L0 pipeline restructure wired into MD · 8 invariants)

**Migrations `052`–`056`, PRs #87–#100.**

**Devotional-admin save fix (root cause).** "Saved 0, 27 failed" in `devotional_admin.html` for a non-Rosehill church = a single-church-era GLOBAL `UNIQUE(entry_date)` on `devotionals` (23505 / 409 Conflict): the church saw "0 entries" (RLS-scoped SELECT) while every INSERT collided with ANOTHER church's rows for the same date. Diagnosed by ruling out anon/RLS first (a live-JWT check via in-browser alert — role `authenticated`, valid `church_id`), then reading the constraint from `schema.json`, never guessing. Fix `052` → `UNIQUE(church_id, entry_date)` (applies #218).

**Tenancy-unique sweep + repo hygiene.** A sibling audit found the same tenant-blind unique on `wednesday_preaching(preach_date)`, `ministries(name)`, `preachers(sequence)` → `053` composites them all (pastor-confirmed per-church). The four Model-3 catalog uniques (`cohort_programs`, `btli_quizzes`, `usbong_quizzes`, `library_chapters`) stay parked for the partial-index treatment (#253/#218). The 052 rollout drifted onto `main` as TWO conflicting files → consolidated to one `migrations/052_devotionals_composite_unique.sql`; all migrations relocated to the `migrations/` folder convention (#252).

**Security.** Supabase Security Advisor flagged an RLS-off backup table → `055` locked `_backup_devo_attendance_cleanup_20260620` + `_backup_members_diag_zone_2026_05_06` (RLS-on-no-policy) (#255). Drop-when-confirmed + a `v_*` view pass remain optional follow-ups.

**LCG Shepherd's Check-in Loop (the session's spine).** From a real pastoral pain — *"my heart bleeds for the new believers when an LCL isn't meeting; how do I encourage + learn why?"* — a fully in-app, shepherd-not-spy loop (#258): `054 lcg_leader_checkins` (tenant + per-member RLS via `auth_member_id()` in leader sessions #254, + `church_id` auto-stamp) → **Pulse "📨 Send in-app check-in"** persist-on-send (PR #93) → **MLT 💛 banner + 7-reason reply screen** flips `status→answered` (#95) → **message single-source** to the editable LC Check-in Message-Greetings template, stored as `fullText` in `056`'s `message` column (#96/#97, #257) → **Pulse ack reason-chip** (#98) → **clickable chip → reply-detail modal** with the free-text note (#99) → **grid-break hotfix** (a `<div${click}` welded tag, #100/#256).

**L0 pipeline restructure.** Added the pre-pipeline **L0 "New Believer · Follow Jesus"** to `leadership_pipeline.html` (Stott + Putman in, PDL retired, NBO/Usbong/assessments/milestones moved down, L1 compensations), fixed a PRE-EXISTING script-breaking apostrophe bug (so the diagram's modals + filter tabs finally work), and wired the diagram into **MD Settings → Pastor Tools** for all churches (PR #91) (#259).

**Design deliverables (chat/artifacts).** The **Five Looks** weekly accountability instrument (`accountability_five_looks_v1.md`); the accountability program skeleton (three rings, grace guardrails, Six Sources mapping); and the LCL check-in outreach strategy (targeted-announcement vs in-app loop).

**Process.** Every PR back-checked byte-for-byte vs the branch + `node --check` re-run locally before merge (#215/#105); CC patches proven on a local copy first (#241); migrations self-verified rerun-until-true (#210) and committed to `migrations/` (#242/#252). New shorthand **`gmt`** = the proper session title enumerating builds/PRs/migrations. Invariants #252–#259 → **count 259.**

## ✅ COMPLETED JULY 1, 2026 — SESSION 54 (two onboardings · members-RLS hotfix · pastor-tools lockdown · two manual chapters · MMT Change-my-PIN + change_pin EF · 6 invariants)

> PRs #78–#86 + migrations `049`–`051`. Invariants #246–#251 → **count 251**. A consolidation session: two churches onboarded (11 total), a real **security hole** in `members` RLS closed and proven, a client-side pastor-tools lockdown ahead of L2 users, two Pastor's-Manual chapters, and a member-facing **Change my PIN** flow that surfaced the EF-contract + EF-deploy guardrails.

### **Church onboardings — CEF (#10) + GGCF (#11) (migrations `049`/`051`, PRs #78/#83)**
Christ's Eminence Fellowship (slug `cef`, L5 pastor Arnaldo J Arnaldo, `049`) and Glorious Grace Christian Fellowship (slug `ggcf`, L5 pastor Geraldine Minguez, `051`) onboarded — both NULL pin → first-login `set_pin`, mirroring the `042`/`049` pattern, re-stamp-not-delete (#235). Eleven churches now live.

### **🔒 SECURITY — `members` RLS self-escalation hotfix (`050`, PR #81, Inv #249)**
ROOT CAUSE: `members` policies were tenant-scoped only with NO level check — any authenticated member could DELETE every member in their church AND self-promote via UPDATE (`pipeline_level=5` / `is_platform_admin=true`) → re-login as pastor. FIX: DELETE now requires `auth_church_id()` match AND `auth_level() >= 5`; a BEFORE UPDATE trigger (`members_guard_privilege_cols`) blocks non-pastor changes to `is_platform_admin` / `pipeline_level` / `is_facilitator` / `facilitator_role`, bypassing only for service-role (`auth_church_id()` NULL) or L5. **Validated 3/3 in-browser as a live L2** (normal update OK; self-promote BLOCKED 400; delete BLOCKED 0 rows). Console RLS tests run in the dashboard frame via `MultiplyShared.getDB()`; the SQL editor bypasses RLS as owner.

### **Pastor-tools UI lockdown (PR #82)**
Ahead of L2 users onboarding across churches, `multiply_dashboard.html` extended `initSettingsPanel()`'s hide-list to cover the Pastor Tools / Instrument Versions / Data Management sections, and added `isPastor()` guards to `clearAll`/`clearSample`/`loadSample` (which had no client guard). Defence-in-depth over the server-side RLS.

### **MMT cosmetics — card unification + wrap fix + cleanup (PRs #79/#80 + direct-to-main)**
Home CTA cards unified to one shape/size while keeping per-card accent colors (#79); attendance self-attest buttons stopped overflowing (`flex-wrap` + hint-on-own-line, #80); the milestone next-step icon swapped ladder→footprints (tofu-box on Gerry's device); Home eyebrow section-labels removed + Thanksgiving CTA renamed “Provision Notebook/Thanksgiving” + prayer CTA de-jammed.

### **Pastor's Manual — two chapters (PRs #84/#85)**
**Chapter 3 “Loading the Daily Devotional”** (#84) — Calendar + Bulk-Paste workflow, per-church + date-keyed, auto-appears as “today's devotional” (no publish step). **SVI now documents Prayer as the live 7th dimension** (#85, Inv #250) — heading/intro/list/diagram/glossary all updated to seven (Gather · Word · Prayer · Fellowship · Mission · Growth · Service). Confirmed live before documenting: migration `009` run + `compute-svi-weekly` computing it (4 prayer metrics active, 14 profiles weighted, **458 recent snapshots scoring prayer**) — the stale “W4 silent-SVI-prayer held” note is retired.

### **MMT “Change my PIN” + `change_pin` EF action (PR #86, Inv #246/#247/#248)**
A Profile → PREFERENCES **🔑 Change my PIN** modal (Current / New / Confirm, bilingual, masked numeric). The build surfaced three guardrails: the `auth-login`/`token-login` EFs are now IN the repo (`supabase/functions/…`) — **read the EF source before building** (#246); `set_pin` is first-login-only (`pin_already_set` 409), so a new **`change_pin`** action (`{member_id, current_pin, new_pin}` → bcrypt-verify current server-side → overwrite, no session minted) backs the modal (#247); and the EF redeploy is a **separate human-gated dashboard step** (Verify-JWT OFF) that must land with/before the merge — it fails safe (generic error) until deployed (#248). CC's pre-apply contract check caught a dead-on-arrival first attempt that assumed `set_pin` would overwrite.

### **Process**
`m.md` now **drops the two full canonical files** (`HANDOFF.md` + `MULTIPLY_INVARIANTS.md`) for the Pastor to upload manually — superseding the Session-52 net-additions/CC-splice flow (**Inv #251**).

### **Open threads (carry-overs)**
`050`-style **INSERT hardening** (members INSERT still open — a console-savvy member could INSERT an `is_platform_admin=true` row; also pin `search_path` on the `050` trigger function); **sibling-table RLS audit** (other per-church tables likely share the tenant-only-no-level gap; the broad members UPDATE also lets a member edit another member's row — Phase 2); members **SELECT privacy** review; embed-timing resync for Meeting Prep (#169); interactive arc **PR-3/PR-4** + **v2 EOLO**; overlay-authoring surface + #218 catalog-unique audit; sample-guide seeder; BTLI **L12** (blocked on UNLAD-L2 scan).

---

## ✅ COMPLETED JUNE 30, 2026 — SESSION 53 (BYO-AI Meeting Prep arc — devotional→AI→guides + interactive responses · Ministry Recommender close-out · Pastor's Manual + MLT manual chapter · 10 invariants)

> PRs #65–#77 + migrations `043`–`048` (`046` committed at close). Invariants #236–#245 → **count 245**. The headline build: a **Bring-Your-Own-AI Meeting Prep** workstream that turns a daily devotional into a Facilitator + Participant guide via any AI the leader trusts, with members recording revisitable I-will/reach/notes — the **"I Will → Look Back" loop made real**. Around it: the Ministry Recommender arc closed out, a new **Pastor's Manual**, and a **Meeting Prep chapter** for the MLT manual.

### **Ministry Recommender — end to end (migrations `043`–`046`, PRs #65–#68)**
Replaced the hardcoded per-church ministry array with a DB-driven, archetype-linked model (**Inv #244**): a shared `ministry_archetypes` library (Model 3 — 17 base archetypes + a Rosehill `interior_design` overlay; partial unique indexes split base/overlay per #218; scoring jsonb `{adv,risk,profile}`) joined to each church's own `ministries` via a new `ministries.archetype_key`. Recommender reads the church's own ministries ⋈ archetype scoring, overlay-wins-by-key, with graceful untagged/empty states (#65). MD Settings got an async archetype dropdown (base + own overlay) on the ministries manager (#67). RLS-isolated + Agape-proven. `046` renamed **Agape Christian Fellowship → Agape Christian Church** (dynamic branding repaints everywhere; committed at this session's close).

### **Pastor's Manual (PR #69) + MLT manual Chapter 4 (PR #77)**
New **`md_manual.html`** — a friendly pastor/admin guide: why-MULTIPLY intro (the **shepherd-not-spy** covenant), getting started (3 tools, login, PWA + Samsung fix), and full chapters on the **Ministry Recommender** + **SVI**, with 6 render-verified SVG diagrams; launcher added in MD Settings. The MLT manual gained **Chapter 4 · "Meeting Prep & the LC Meeting"** — the one-hour format with a **named-systems rationale table** (Four Ws/Neighbour·Comiskey, sermon-based/North Point·Life.Church, 3/3-DBS/Watson·T4T, Up·In·Out/Breen, HOST/Saddleback) and the four places we strengthened, step-by-step Meeting Prep, and 2 diagrams (v1.2). Manual chrome is reusable + brand-free; all diagrams render-verified (**Inv #245**).

### **Meeting Prep — BYO-AI guides (migration `047`, PRs #70–#72, fix #75, tweak #76)**
`047 lc_meeting_guides` + the `auth_discipler_id()` helper (**Inv #236/#239**). MLT **Meeting Prep** screen: pick a devotional (any date) → `mpBuildPrompt` builds a fixed prompt → run in any AI → paste → **fenced-contract parse + manual fallback** (**Inv #237**) → review/edit → publish; guides shaped to the **1-hour meeting** (**Inv #238**). MMT **"This Week's Study"** card reads the published Participant's Guide via the member-read RLS path (#72). **Revisit fix (#75):** local date not UTC (**Inv #243**), resume-most-recent-guide on open, clean empty-state on date-switch, visible load-error toast. **Prompt tweak (#76):** dropped the I-will/reach prose prompts now that MMT provides real fields.

### **Interactive guides (migration `048`, PRs #73–#74)**
`048 lc_meeting_responses` + the `discipler_of()` helper. MMT response fields **i_will / reach_person / notes** with quiet autosave (mirrors gratitude), **private-by-default + opt-in share-to-leader** (**Inv #240**), and reload-prior-answers on reopen — the revisit. Locked design (all 6 decisions): LCL-only review · fenced+fallback · bound to the 1-hour meeting · fixed prompt v1 · per-LCL v1 / shared library v2 · last-wins.

### **Process**
**Commit-each-migration-immediately** adopted to proactively close the #212 dashboard-vs-repo lag (**Inv #242**) — `047`/`048` each committed right after running, before the dependent feature. After CC caught a heredoc-delimiter-gluing trap in a generated `.sh`, **CC prompts are now single self-contained Python patches** (one delimiter, anchors as literals, embedded patch proven against a copy first — **Inv #241**). `046` committed at session close.

### **Open threads**
**Embed-timing resync** for the unified shell — the Pastor's 06/29 guide doesn't reappear in Meeting Prep when opened via `index.html`; the row is real and owned by him (`lcl_id = 547ebda6`), the devotional loads (church_id JWT ready) but the guide query may run before the app-session `leaderId` lands via postMessage (#169). Parked on a standalone-tool confirmation test. Interactive arc remaining: **PR-3** (My Studies history + "Last week you said… how did it go?" banner), **PR-4** (LCL follow-up view of disciples' shared I-wills), **v2 EOLO wiring**. Carry-overs: overlay-authoring surface + the #218 catalog-unique audit, the sample-guide seeder (cold-start devotionals), BTLI **L12** (blocked on the UNLAD-L2 scan).

## ✅ COMPLETED JUNE 29, 2026 — SESSION 52 (Gratitude Journal + sheet · I'm-here sheet · attendance recorder fix + full 'Unknown' backfill · 7-church pastor cohort · GP-batch own-scope · 8 invariants)

> PRs #56–#62 + a direct-to-main grammar push + 5 human-gated migrations (`038`–`042`). Invariants #228–#235 → **count 235**. The member-facing Home surface gained a daily **Gratitude Journal** and was unified onto one CTA+sheet pattern; the attendance **recorder-identity** bug was fixed at source AND every historical 'Unknown' row attributed; and MULTIPLY went genuinely multi-church with a **7-pastor onboarding cohort**.

### **Gratitude Journal (PRs #56–#58 + grammar push)**
New `gratitude_entries` table (`038` — mirrors `devotional_reflections` + a `shared_to_lc` bool; member-own RLS via `auth_member_id()` + same-church read of shared rows; `church_id` auto-stamped; NO unique → multiple entries/day). MMT Home card with quiet autosave + a "🎉 share as a win in LC celebrations" checkbox, and a 🌾 gratitude win-type in `loadCelebrationFeed` (#56). Collapsed into the CTA+sheet pattern with a "My Thanksgiving" list (view + share-toggle + delete) (#57, + a CSS de-scope fix — **Inv #229**). Leader-feed fix so a pastor's OWN shared thanks surfaces via the leader-inclusive id set (**Inv #230**, #58). Past-tense label fix "gave thanks" (**Inv #231**, direct-to-main).

### **I'm-here sheet (PR #59)**
The self-attest card collapsed into the same CTA+sheet (CTA always shown with a "now" pill, empty-state inside the sheet, picker/status/history relocated with their ids preserved so `renderAttestCard` is untouched — **Inv #228/#229**).

### **Attendance recorder fix + full backfill (PR #60, migrations `039`–`041`)**
Root cause: batch + quick-mark saves stamped `logged_by` from the frozen `currentLeader` shim → **'Unknown'**. Fixed with `_liveLeaderIdName()` at all 3 write paths (**Inv #232**). Then attributed all 288 historical Unknown rows: 279 LC-scoped → subject's LCL (`039`), 8 GP/IYM-PID → pastor (`040`), 1 removed-member orphan → Amy by attendance id (`041`) — **0 Unknown** left (**Inv #233**).

### **7-church pastor cohort (`042`)**
Pre-seeded 7 tenant churches (`saturate`, `hpcinc`, `alc`, `mcf`, `fcc`, `obcc`, `ciccl`) + their L5 pastors with NULL pin (first-login `set_pin`); Joey Sison re-stamped Rosehill→Saturate (kept history + PIN, dodged the FK-delete block); Jerome + 5 fresh. Skipped Rosehill + Agape (already onboarded). (**Inv #235**.)

### **GP-batch own-scope (PR #62)**
The General-Purpose-Batch attendance picker now shows only the leader's own batches (`owner_id === live leader`); programs stay visible for creation; the full list stays in My Batches / Settings (**Inv #234**).

### **Repo bookkeeping + process**
Migrations `038`–`041` committed to the repo via PR #61 (so `refresh-schema.yml` picks up `gratitude_entries`); `042` applied — commit to `migrations/` when convenient. **Process change:** `m.md` now produces NET ADDITIONS only (Claude pulls the repo copies, emits new invariant blocks + a session section + Jumpstart update + Last-updated prepend), which CC splices in — Claude no longer regenerates whole docs.

---

## ✅ COMPLETED JUNE 26, 2026 — SESSION 50 (de-brand arc — dynamic church names · self-refreshing schema.json · 5 invariants)

> PRs #37–#46 merged (#215 back-check clean). Invariants #222–#226 → **count 226**. Two arcs: **(A)** every user-visible "Rosehill"/"Pastor Gerry" is gone — church identity now paints at runtime from `churches.name`; **(B)** an authoritative structure-only `schema.json` auto-commits to repo root via a GitHub Action, so Claude reads ground truth instead of guessing columns.

**THE ARC:** with the multi-tenancy DB work complete (S49), the app still HARDCODED "Rosehill Christian Church"/"Pastor Gerry" in chrome, signatures, PDFs, CSVs, and lesson copy — a multi-tenant platform that names one tenant everywhere. Genericize all of it (dynamic where a church name belongs, "MULTIPLY"/"your pastor" where the publisher/role belongs), then stand up a self-refreshing schema so column-shape is never guessed again.

### **A1. Dynamic church name — MD header (`paintChurchName`, PR #37)**
MD header (title, brand-church, drawer — 3 hardcoded spots) now reads `churches.name` via the JWT'd client (own-row-only via `035`) and paints at boot + the #169 embed re-sync. Live-confirmed: Agape header shows "Agape Christian Fellowship."

### **A2. `multiply_brand.js` — standalone branding module (PR #38, Inv #222)**
A UMD module (`?v=1`, sibling of `multiply_close.js`, NOT in shared.js) that fetches `churches.name,slug` once and auto-paints any `[data-church-tmpl]` element by substituting `{church}`/`{CHURCH}`/`{slug}`/`{SLUG}`; exposes `MultiplyBrand.churchName()`/`church()` promises. **Anti-leak:** the static fallback carries NO church name — only "MULTIPLY" — and upgrades ONLY when the JWT resolves, so a pre-auth/wrong-tenant render can never flash another church's name. Wired into 5 DOM pages: `attendance_admin.html`, `lc_attendance_report.html`, `lc_leader_tool.html`, `preaching_calendar.html`, `member_tool.html` (Guest-of banner).

### **A3. "Pastor Gerry" → "your pastor" (PR #39)**
8 visible spots across `member_login.html`, `leader_login.html`, `multiply_dashboard.html`, `mlt_manual.html`. Code comments naming Pastor Gerry left intact.

### **A4. JS-string surfaces (PR #41) + static chrome (PR #42) + in-lesson strings (PR #44)**
- **PR #41 (Bucket A pt2):** dynamic name on JS-built strings using each page's existing JWT'd client (no brand.js on ESM pages): `bulk_send_links.html` WhatsApp signature (`God bless! — ${churchName}`), the 2 diagnostic PDF headers (`salvation_assurance_diagnostic.html` / `spiritual_gifts_diagnostic.html`, via `getDB()` in the async `downloadPDFReport`), `multiply_member_dashboard.html` brand span (brand.js) + title + CSV. All fall back to "MULTIPLY".
- **PR #42 (Bucket B):** 24 one-line chrome swaps Rosehill→MULTIPLY across 17 static curriculum/manual pages (BTLI 101 + BTLI 1 L5/L6, Usbong L6–L10, `ministry_intake_form.html`, `mlt_manual.html`). No DB client — these carry the global BASE curriculum where **MULTIPLY = publisher** (Model 3).
- **MD guest-text + PR #44:** "Non-Rosehill MMT user"→"Non-member"; "Guest of Rosehill" copy genericized; CSV `rosehill_multiply.csv`→`multiply_pipeline.csv`; 4 in-lesson visible strings (`profile_results_viewer` "Rosehill's", `btli1_l7` Taglish lines + placeholder, `btli1_l7_facilitator`) genericized.

**Milestone:** ZERO user-visible "Rosehill" remains anywhere — all residual occurrences are code comments, a data key, or the internal `strengths_save_debugger.html` tool.

### **A5. #36 regression — caught + fixed (PR #40, Inv #226)**
S50's earlier housekeeping (#36) removed the obsolete Access-Control Settings section. That section was a `.ss`, so `#panel-settings .ss:nth-child(3)` (used by 4 JS routines) silently shifted off the ministries manager and onto **Pastor Tools** — the ministries code clobbered Pastor Tools, and the real ministries section rendered a raw unsubstituted `${ROSEHILL_MINISTRIES_PLACEHOLDER}`. Fix: gave the section a stable `id="ministriesSettingsSection"`, repointed all 4 JS refs, dropped the placeholder, relabeled "⛪ Ministries." Pastor Tools restored. → **positional `:nth-child()` is never a stable handle for JS-driven sections (Inv #226).**

### **B1. `schema-dump` EF + `refresh-schema.yml` Action (PRs #45/#46, Inv #223/#224/#225)**
GitHub runners reach `supabase.co`; Claude/CC bash cannot — so a runner is the bridge. The workflow curls the `schema-dump` EF (`?schemas=public`, header `x-schema-secret`), validates `table_count ≥ 1`, **strips volatile `generated_at` + per-table `est_rows`** (byte-identical re-dumps → clean diffs, no cron spam, no row counts pushed — honors #124), and commits a **structure-only `schema.json`** to repo root only on real change. Triggers: push to `migrations/**` or `supabase/functions/**`, weekly cron (Mon 03:00 Manila), manual `workflow_dispatch`.

**Debug arc (the 3 ops invariants):**
- **404 → deploy naming:** the EF had been dashboard-created as "Schema Dump V2" (slug `swift-task`); the repo folder `supabase/functions/schema-dump/` already held the same 265-line code → created a function named exactly `schema-dump` so repo-folder = slug = workflow URL (#197).
- **401 → Verify-JWT gate (Inv #224):** a dashboard-editor function defaults **Verify-JWT ON**; the runner's `x-schema-secret`-only call was rejected at the gateway BEFORE the function's own secret check ran — while the dashboard "Test" panel returned 200 (it auto-attaches a login token). Fix: toggled **Verify JWT off** in the function Settings.
- **push-rejected → stale-SHA re-run (Inv #225):** the commit step succeeded but `git push` failed `! [rejected] main -> main (fetch first)` because **"Re-run jobs"** replays the original pinned commit, and `main` had advanced via the day's merges. Fix: trigger a FRESH **"Run workflow"** run (checks out current `main`) → **GREEN**.

**Verified:** `schema.json` live at repo root — 64 tables, ~460 KB, `generated_at` + `est_rows` both stripped, real tables present. Claude now pulls `schema.json` at session start as ground truth.

### **Still open / flagged:**
- **`ministry_recommender.html`** — 17 `rosehill:` keys are per-church gift-recommendation PROFILE DATA, not visible labels → a real multi-tenant DATA-architecture decision (own profiles / shared default / inherit base; church→profile resolution), parked for its own discussion.
- **Secret hygiene:** `s5e9a4u9p1008b` appeared in a screenshot during debug; risk near-zero (structure-only dump), but rotate when convenient — set the same fresh string on the function (`SCHEMA_DUMP_SECRET`) AND the GitHub repo secret.

---

## ✅ COMPLETED JUNE 26, 2026 — SESSION 49 (Phase B: shared-content catalog publish-lane EF + RLS-enable — MULTI-TENANCY COMPLETE · 3 invariants)

> PR #34 merged (#215 back-check clean) + migrations `036`/`037` applied to Supabase and committed. Invariants #219–#221 → **count 221**. **The multi-tenancy arc is COMPLETE** — every per-church table (config, `churches`, and the 7 shared-content catalog tables) is RLS-isolated.

**THE ARC:** the last tenancy item from S48 — flip RLS on the 7 Model-3 catalog tables — which first required a publish lane so base/canonical curriculum authoring (`church_id IS NULL`) survives RLS-on (the `033` tenant policies make base rows unwritable by any church JWT).

### **1. `catalog-publish` Edge Function — service-role BASE-only publish lane (Inv #219)**
Verifies the caller's HS256 JWT (`JWT_SECRET`, the legacy secret — same verify set as `auth-login`/`token-login`), then gates on **`members.is_platform_admin = true`** for the token's `sub` (service-role lookup) — a tenant JWT (even L5, even the L5 Agape test member) gets `403 not_platform_admin`. Contract `{ table, op, id?, payload?, onConflict? }`; four locks: (1) `table` in the 7-table allowlist; (2) `op` in insert/update/upsert/delete; (3) insert/upsert **force `church_id=NULL`** (#211 sibling); (4) update/delete scoped `.is('church_id', null)` → BASE only, never a tenant overlay. Deployed dashboard-side `--no-verify-jwt` (the EF is the sole gate). Committed at `supabase/functions/catalog-publish/index.ts` (PR #34). Smoke-tested ×2 in-browser.

### **2. `036` — `members.is_platform_admin` (platform author decoupled from church, Inv #220)**
Per-member boolean, NOT a hardcoded Rosehill `church_id` (the rejected alternative would give a tenant pastor god-mode over every church's catalog). Seeded TRUE for the platform author ONLY (Gerry `547ebda6`); verify returned exactly 1 row.

### **3. Client swap — `MultiplyShared.catalogWrite()` + 23 call sites (PR #34)**
One helper in `multiply_shared.js` (returns the supabase `{ data, error }` shape; base lane only) replaces direct `db.from(<catalog>).insert/update/upsert/delete` at **23 sites**: `multiply_dashboard.html` (18), `btli_quiz_admin.html` (4 — toggle/patch/delete/bulk-upsert), `lesson_quiz_editor.html` (1 — the dynamic-`tbl` BTLI+Usbong editor). `?v=8→?v=9` across all 19 loaders. #215 back-checked from chat (codeload tarball diff vs `origin/main`): only intended files, no reverts, 0 remaining direct base-writes. All 6 author surfaces browser-verified with RLS still OFF before the flip (bulk-upsert proven via a throwaway-row probe through the helper).

### **4. `037` — RLS ENABLED on all 7 catalog tables (the last tenancy item)**
Pre-flight (#213) confirmed every table `rls_enabled=false / n_policies=4 / allow_all_stubs=0` with the standard tenant set — no dormant stub to activate. `037` enabled RLS on all 7; self-verify green ×7 (`true / 4 / 0`). Pre-flight also confirmed the quiz-player pages run **authenticated** (they `.insert()` into the already-RLS'd `*_quiz_attempts`), so no anon read path breaks.

### **5. Agape leak-test (RLS live) — the definitive proof**
Two throwaway overlays planted (one Rosehill, one Agape); probed as the Agape test member (`4c79ce89`, PIN 1234): **sees own Agape overlay OK · blind to Rosehill's OK · base visible (2) OK · EF base-write `403 not_platform_admin` OK.** Overlays cleaned up.

### **6. Near-miss — dynamic `db.from(tbl)` (Inv #221)**
A literal `grep "from('usbong_quizzes')"` showed only `.select()` — but `lesson_quiz_editor.html` writes BOTH usbong + btli through `db.from(TABLE_MAP[STATE.table].table)`. Caught before the flip; usbong added to the EF allowlist (the EF gained `upsert` in the same revision, for `btli_quiz_admin`'s `onConflict` bulk save).

**Doc-gap retired:** GRACE_PATHWAY / MULTIPLY_PIPELINE_DIAGRAM / BTLI1_LESSON_MAP all serve 200 from the repo (committed S48) — project-knowledge fallback no longer needed.

---

## ✅ COMPLETED JUNE 26, 2026 — SESSION 48 (shared-content catalog tenancy structure · Agape SVI seed + composite-unique repair · churches own-row RLS · 2 invariants)

> Migrations `033`/`034`/`035` applied to Supabase + committed (PRs #32/#33, #215 back-check clean both). Invariants #217–#218 → **count 218**. **All per-church CONFIG + `churches` now isolated** — only the 7 shared-content catalog tables await **Phase-B** RLS-enable (blocked on the publish-lane EF, not a decision).

**THE ARC:** close the last tenancy gap — the 8 no-`church_id` shared-content tables — starting with the curriculum-edit-ownership DECISION, then isolate `churches`, and seed Agape's SVI weights along the way.

### **1. Curriculum-ownership DECISION → Model 3 (shared base + per-church overlay)**
The 8 tables split: **`churches`** is the tenant registry itself (handled separately, §3); the other **7** are curriculum content (`btli_quizzes`, `cohort_programs`, `library_chapters/quizzes/resources`, `pipeline_lessons`, `usbong_quizzes`). Three models weighed — (1) shared read-only catalog, (2) per-church copies, (3) shared base + per-church overlay. **Pastor chose 3** (elegant + scalable, #197): canonical curriculum is global (`church_id IS NULL`, readable by all — MULTIPLY is a *publisher* of the BTLI/Usbong pipeline), each church can ADD its own rows and edit only those, and **cold-start stays free** (a new church inherits the base with zero seed). Model 2 (the N-church-migration-per-edit trap) explicitly avoided.

### **2. `033` — Model 3 catalog tenancy (STRUCTURE only, RLS deferred)**
Preflight (#213, all-schema locator after a wrong-project false alarm) confirmed all 7 tables: `id uuid` PKs (no `system_settings`-style collision), no `church_id`, RLS off, zero policies, zero stubs. FK graph confirmed live (corrected schema.json): `btli_quizzes.lesson_id`/`usbong_quizzes.lesson_id → pipeline_lessons`; library chain `resources → chapters → quizzes` (CASCADE); `library_quizzes` carries an XOR CHECK. `033` adds nullable `church_id uuid REFERENCES churches(id) ON DELETE CASCADE` + the 4 tenant policies per table (**SELECT = `church_id IS NULL OR church_id = auth_church_id()`** (base-or-mine); **INSERT/UPDATE/DELETE = `church_id = auth_church_id()`** (mine-only) — base rows are NOT writable by any tenant JWT). **RLS kept DISABLED** by design: enabling now would lock out today's in-app "author as Rosehill" path, which must first move to a privileged **publish-lane EF** (Phase B). Verified green (has_church_id=true, n_policies=4, rls_enabled=false ×7).

### **3. `035` — `churches` registry own-row-only RLS (ENABLED, leak-tested)**
Evidence-first (#56/#97): grepped ALL 8 client surfaces (login, MD dashboard `multiply_dashboard.html`, MLT, MMT, shell `index.html`, the 3 shared JS) — **zero** `churches` table access anywhere; church identity flows entirely via the JWT (`multiply_tenancy.js` `churchId()`), and the login/onboarding EFs run service-role (bypass RLS). So own-row-only is safe with nothing to break: SELECT `id = auth_church_id()` for `authenticated`, no write policy (service-role/owner-only), **RLS ENABLED** (no in-app authoring path to defer for, unlike the catalog). **Leak-tested in-browser** with the Agape member JWT (member `4c79ce89`, PIN 1234) via alert-popup: `HTTP 200 — 1 row` (Agape only), Rosehill invisible. The SQL editor would have lied (owner bypasses RLS) — hence the in-browser proof.

### **4. `034` — Agape SVI seed + a two-landmine composite-unique repair (Inv #218)**
Agape had **0** `svi_weight_profiles` (Rosehill: 7 = default + level_0..5); post-isolation its editor would render empty. The seed (copy-as-template, genericized `level_5` note to drop Pastor's name, #29-spirit) first failed `23505 duplicate key … profile_key_key` on `level_0`: `svi_weight_profiles_profile_key_key` was a **global** `UNIQUE(profile_key)` — a single-tenant assumption invisible until a 2nd church reused a key. The full constraint+index dump (#213 extended to indexes) surfaced a **second landmine**: `uq_svi_profile_active_level` = `UNIQUE(applies_to_level) WHERE is_active` (also global). `034`: drop both → rebuild as `UNIQUE(church_id, profile_key)` + `UNIQUE(church_id, applies_to_level) WHERE is_active`, then seed. Verified: **both churches at 7**, identical key sets. This is the SVI sibling of the `system_settings` `single_row`/`DEFAULT 1` footgun → **Invariant #218**.

### **5. Process — ledger discipline + the session-naming convention (Inv #217)**
`033`/`034` bodies were run stamp-less, so a one-shot ledger backfill (`schema_migrations` `033`/`034`, #212) caught the DB up to the committed files; `035` baked its stamp into the run version (no backfill). PRs #32/#33 both #215-clean (cut from current `origin/main`, back-checked — exactly the stale-base guard that bit S47). Separately, Pastor set the **session-naming convention** (→ **Inv #217**): the read-in/reconcile phase is unnumbered ("Pre-flight (post-S## reconcile)"); the number is claimed only at the first real build, and the title is build-named — because claiming it at the backward-looking read-in made an opening session look like a duplicate of the one that just closed.

---

## ✅ COMPLETED JUNE 25, 2026 — SESSION 47 (system_settings per-church · MD Settings render-fix · svi_weight_profiles RLS · 4 process/RLS invariants)

> Migrations `030`/`031`/`032` applied to Supabase + committed. PRs #28 (system_settings), #29 (Settings render-fix), #30 (svi_weight_profiles RLS) merged. Invariants #213–#216 → **count 216**. **All per-church CONFIG tables now isolated** — only the 8 shared-content catalog tables remain.

**THE ARC:** close the last per-church CONFIG tables — `system_settings` (Rosehill-global app config) and `svi_weight_profiles` (per-church SVI tuning) — and fix a Settings render regression surfaced along the way.

### **1. `system_settings` per-church isolation (`030`/`031`, PR #28 — Option C)**
NOT a "meta blob": one global row (`id=1`) with **7 JSONB columns** (`weights/thresholds/zones/interventions/pipeline/fats` + `meta`). Live config is `meta` ONLY (4 greeting templates, absence reasons/events, attendance event types, instrument-redesign dates, idle minutes); the 6 siblings are **VESTIGIAL** (superseded by `svi_weight_profiles` at the SVI Service split #117/#119) — left PARKED. **Option C (fold into MD):** MD already owned 100% of the live `meta` editors via JWT'd `getDB()`; the standalone **anon `system_settings.html` edited only dead columns + a redundant PIN gate → RETIRED** (`git rm`). `030`: `id`→sequence (was `default 1`, would collide on the 2nd church's insert), **dropped the `single_row` singleton CHECK guard** (Inv #213 — preflight only listed policies, missed it; seed failed `23514` until dropped), `UNIQUE(church_id)`, dropped two LIVE `"anyone can read/write system_settings"` public stubs (Inv #186 — `policy_count=6` not 4 caught them), 4 tenant policies → verified `t / 4 / 0`. `031`: Agape seeded a copy-as-template `meta` row (id=4; sequence burned 2,3 on rejected attempts — distinct + church-scoped is what matters). App (PR #28): **11 reads** dropped `.eq('id',1)` (RLS picks the my-church row); **4 writes** → `.upsert(…,{onConflict:'church_id'})` (cold-start self-provision). **Browser-proven** (Agape JWT → only id=4, Rosehill invisible) + write smoke-test (greeting Save→reload persists).

### **2. MD Settings render-fix (`029`, PR #29)**
Four pastor-only Settings sub-sections (Ministries / Diagnostic / Heads-Up / Greetings) vanished. **NOT data** (`ss_rows=1`), **NOT #28** (those edits never touched gates/DOM). Root cause: **hide-only boot gates raced `LeaderScope` readiness** in the unified-shell iframe — the leader session arrives via postMessage AFTER the gates' 0/500/1500ms timers, so `!isPastor()` was briefly true → gate hid the section → no "else show" ever revealed it (Inv #214). Fix: `initSettingsPanel()` re-asserts the section loaders + **symmetric** visibility (`display = isPastor ? '' : 'none'`) on Settings open, wired to both nav buttons via `_navTo('settings', initSettingsPanel)`.

### **3. `svi_weight_profiles` per-church RLS (`032`, PR #30)**
Carried `church_id` since `001` (backfilled Rosehill) but was **never given tenant RLS**: read = `public USING(true)` (cross-church leak — Agape would read Rosehill's 7 weight profiles) and write = `service_role`-only (silently **blocking** MD's client-side `saveSviWeights()` — leader JWT is `authenticated`, not `service_role`; Inv #216). `032`: dropped both, 4 tenant policies on `auth_church_id()` → verified `t / 4 / 0 / 0`. `svi_metrics` left **GLOBAL** (KPI catalog, no `church_id`; public-read + service_role-write correct by design — untouched).

### **4. Process lesson — stale-base branch (Inv #215)**
CC's first render-fix branch was cut from a **pre-#28 base** and, diffed against current `main`, silently **reverted the whole system_settings arc** (re-added the deleted `system_settings.html`, deleted migrations `030`/`031`, restored `.eq('id',1)` reads + `.update().eq('id',1)` writes) — while CC reported a clean "+22/−2" against its own stale base. Caught ONLY by back-checking the diff vs `origin/main`. Recovery: hard-sync to `origin/main` → rebuild on clean base → base-sanity guards in the recovery prompt.

### **Still open / flagged:**
- **Agape has 0 `svi_weight_profiles`** → after isolation its SVI editor is empty + engine has no Agape tuning. Follow-up `033`: seed Agape a copy of Rosehill's 7 (copy-as-template) OR confirm engine default. Recommend seed for parity when Agape SVI goes live.
- 6 vestigial `system_settings` columns stay **PARKED** (dead since the SVI split; drop later in a backed-up cleanup).
- The 8 shared-content catalog tables remain the last tenancy gap (needs the curriculum-edit-ownership decision).

---

## ✅ COMPLETED JUNE 24, 2026 — SESSION 46 (schema_migrations ledger + reconcile-on-open · RLS-coverage sweep: 21 tables newly enforced · leader_sessions EF church_id stamp · FIRST tenant church onboarded — Agape)

Deployed files pulled from the repo + re-read before editing (Inv #56); SQL human-gated (Claude proposes, Pastor runs in Supabase — FLAT per Inv #209, **rerun-until-true** per new Inv #210); isolation via in-browser `alert()`-popup with the Agape token, never the SQL editor. Every CC branch back-checked before merge. **Invariants #210–#212 added → count 212.** Shipped to `main` (PRs #18–#26).

**THE ARC:** make the dashboard-run-not-committed gap self-detecting (the ledger), drive the Inv #208 RLS-coverage sweep across the remaining ~22 church-stamped tables, then onboard the first real tenant church.

### **1. `schema_migrations` ledger + reconcile-on-open (`023`, Inv #212)**
A GLOBAL metadata table (no `church_id`, deliberately OUTSIDE the tenancy sweep) every migration self-stamps via a trailing `INSERT … ON CONFLICT (version) DO UPDATE`. Backfilled history 001–022 (006 absent = phantom; 021 stamped provisionally `(file pending)`). RLS-on with one `SELECT TO authenticated USING(true)` read policy (non-sensitive; keeps the linter quiet; room for a future in-app health widget); writes owner/service-role only. **Reconcile-on-open** = session-open ritual: `SELECT string_agg(version,',' ORDER BY version) FROM schema_migrations`, paste it, Claude diffs vs the `migrations/` folder (excluding `_rollback`/`_verify`/`_diagnostic`) → buckets {in-sync · applied-not-committed · committed-not-applied}. v2 north-star = a GitHub Action (same query+diff, relocated). It caught + closed its own first gap (021) end-to-end.

### **2. RLS-coverage sweep — 21 tables newly enforced (Inv #208 progress)**
Audit-first each batch (RLS state + `pg_policies` stub scan + accessor grep), then standard pattern (backfill NULL→Rosehill → drop stubs → 4 `TO authenticated` policies on `auth_church_id()` → ENABLE), proven by structural verify (`true`/`4`) + no-lockout + in-browser Agape-token isolation (every table → 0).
- **`024` tier-a (6):** `ministries`, `ministry_roles`, `devotional_reflections`, `diagnostic_results`, `attendance` (lone anon reader = a debug tool, slated for deletion), `profile_tokens` (token-login EF is service-role → bypasses RLS). **CORRECTS the S45 "tier-a = latent/quick" framing:** every policy on these was an allow-all STUB (Inv #186), not a pre-written tenant policy — all needed the full pattern.
- **`025` devotionals (Model A, per-church):** all 52 were NULL=shared; chose per-church (each church owns its guide) over shared-NULL — backfill 52→Rosehill, **re-attached the autostamp trigger** (reversing 014's exclusion), 4 tenant policies. Cold-start empty-state to be solved by a church-OWNED "Load sample guide" seeder (queued), NOT shared rows (which would reopen a write hole).
- **`021` catch-up:** the S42 assessment RLS (`member_profiles`+`gifts_diagnostic`) was dashboard-only; reconstructed the file from live policy names (idempotent no-op) → ledger fully green.
- **`026` tier-b (13):** `attendance_self_attest_log`, `discipler_change_log`, `library_chapter_progress`, `library_progress`, `library_quiz_attempts`, `ministry_rosters`, `ministry_roster_members`, `prayer_intercessions`, `prayer_list_items`, `prayer_list_opens`, `prayer_reminders`, `prayer_requests`, `transfers`. All had `church_id`, 0 stubs, autostamp attached, JWT accessors. (prayer_* cross-member writes are within-church → church-scoped RLS passes them; the old "SECURITY DEFINER RPC" worry is moot.)
- **`027` leader_sessions (+ EF patch, Inv #211):** `auth-login` inserts `leader_sessions` as service-role (no JWT) so the autostamp trigger left `church_id` NULL → invisible to the JWT client. Patched the EF to stamp `church_id` explicitly (PR #23, redeployed) BEFORE enabling, then backfill + 4 policies + ENABLE.

### **3. Self-verifying migrations + rerun-until-true (Inv #210)**
The Supabase editor sometimes runs only PART of a pasted block (or a flat run partial-applies). Every migration now ENDS with a trailing self-verify SELECT; the safe loop is **run → read column 2 → rerun the same idempotent block until it reads the expected value** (true/4/0). The trailing SELECT is KEPT in the committed file (harmless read; self-checks every future run; matches the form run, Inv #209).

### **4. FIRST tenant church onboarded — Agape (`028`/`029`)**
Every per-church table is now RLS-locked + isolation-proven, so Agape is go. Moved the **Millete cluster** (pastor Jojo + 14, incl. Cristy) Rosehill→Agape by explicit id (Inv #166; `028`), then flipped guest→full member (`029`). **Jojo starts at L3** (leader tools, NOT MD Pastor-only Settings — so the `system_settings` cross-tenant config caveat is moot until promotion). Provisioning note: the Rosehill (Pastor) session can no longer see/create Agape members — RLS working as designed; Agape's own pastor adds people, or seed via dashboard.

### **Still open (all NON-blocking for Agape):**
- **`system_settings` isolation — NEXT (mini-project, not just a migration):** 1 global row (`id=1`, `meta` blob) of Rosehill-specific config (greetings, assessment cutoff dates, absence reasons/events, idle timeout); code hardcodes `.eq('id',1)`. Plan: per-church rows → drop the `id=1` assumption (RLS picks "my church's row") → JWT-wire the anon `system_settings.html` admin page → seed Agape a config row (copy-as-template). Needs `SELECT * FROM system_settings` to design.
- **8 shared-content tables (no `church_id`):** `btli_quizzes`, `churches`, `cohort_programs`, `library_chapters`, `library_quizzes`, `library_resources`, `pipeline_lessons`, `usbong_quizzes` — global shared curriculum (read-side already correct). Open: anon-WRITE hardening (pre-public-signup) + a Pastor DECISION — may every church's pastor edit the shared MULTIPLY curriculum, or only the platform owner? (shapes the write policy).
- Sample-guide seeder (church-owned starter devotionals + Load/Clear) — cold-start empty-state.
- Confirm SVI weights (`svi_metrics`/`svi_weight_profiles`) are church-isolated (didn't appear in the RLS-off audit → likely already RLS-on; verify).
- Carried: stray `strengths_profile (11).html` + `multiply_shared (7).js` delete · 2 own-anon debug tools · GRACE/PIPELINE/BTLI docs still 404 at repo (read project-knowledge) · assessment revisions (DISC, Love Languages, Enneagram) · BTLI L12.

---

## ✅ COMPLETED JUNE 23, 2026 — SESSION 45 (logout JWT-hygiene · RLS version-of-record catch-up · SECURITY DEFINER view audit · multi-tenancy NOT-complete finding)

Deployed files pulled from the repo + re-read before editing (Inv #56); `node --check` + browser-smoke per change; SQL human-gated (Claude proposes, Pastor runs in Supabase). **Deployed `multiply_shared.js` `?v=7→?v=8`; `multiply_tenancy.js` `?v=2`.** **Invariants #205–#209 added → count 209.** Shipped to `main` (PRs #13, #15, #14, + `022`).

**THE ARC:** clean up the loose security + version-control threads after the S44 "tenancy complete" declaration — and, in auditing, discover that declaration was premature.

### **1. JWT-clear-on-logout (PRs #13 + #15, Inv #205)**
The logout functions cleared their own session key but left `multiply_jwt` (and, on the unified shell, the sibling session key) behind. Fix: one private `_clearSessionState()` helper in `multiply_shared.js` removes **all three** keys — `multiply_leader_session`, `multiply_member_session`, `multiply_jwt` — and BOTH `logoutLeader` + `logoutMember` route through it (a unified-shell leader holds both sessions + one shared JWT, so logout from any role tears the whole presence down; fresh login re-mints). The **gate-fail** paths (`gateOrRedirect`/`gateMemberOrRedirect`) were deliberately left untouched — they fire on expiry, not logout, and must not strand a still-valid sibling session's JWT on a standalone tool. `leader_login.html`'s `logoutAndRestart` (legacy page, no shared.js) inlines the same three `removeItem`s (#15). Shared.js `?v=7→?v=8` across all **19** consumers. **Browser-proven on the live site:** after leader logout (incl. the legacy `leader_login` path) Session Storage is **completely empty**; logins + tools still work (fresh JWT re-mints, RLS happy).

### **2. Migrations 015–020 committed as version-of-record (PR #14)**
The RLS rollout (`015` canary → `020` members) was **dashboard-run, never committed** — same gap pattern the EFs had pre-#202. CC created all six byte-exact (`015`=33, `016`=51, `017`=63, `018`=11, `019`=39, `020`=28 lines), zero hardcoded UUIDs (slug-subquery backfill). Back-checked: 017 byte-identical, structural policy/ENABLE counts match each migration's table set. **Noted:** `020` is non-idempotent (drops the 2 members stubs but not its own 4 policies — fine, it ran once); `017` does NOT carry the `absence_notices` allow-all stub-drops (those were a live-DB one-off remediation, not canonical schema — correct).

### **3. SECURITY DEFINER view audit → `migrations/022` (Inv #206/#207/#209)**
`SELECT prosecdef` → **zero** SECURITY DEFINER functions in `public` (clean). `pg_class.reloptions` → **6 owner-views bypassing RLS**. View-defs traced to base tables: the 4 quiz-grade views (`v_btli_course_grade`, `v_btli_member_best`, `v_usbong_course_grade`, `v_usbong_lesson_grade`) read `btli_quiz_attempts`/`usbong_quiz_attempts` — per-church (`church_id uuid`) but **RLS-OFF, 0 policies** (the real leak); `v_pending_transfers`→`members` (RLS-on), `v_svi_latest`→`svi_snapshots` (RLS-on), both no client consumer. All consumers use `getDB()` (JWT) — safe to enable. `migrations/022` (run **flat** after the wrapped first run silently rolled back, Inv #209): enable RLS + 4 policies on both quiz-attempt tables (backfill 65 btli / 83 usbong NULLs → Rosehill) + `ALTER VIEW … SET (security_invoker=true)` on all 6. **Live-verified:** both tables `rls=true`/`policy_count=4`/`null_church=0`; 6 views `security_invoker=true`. **Browser-smoked:** Quiz Scores report renders church-scoped (209 Rosehill members); member quiz-submit saves + scores. Committed version-of-record (flat form) — PR off `claude/rls-022-quiz-attempts-view-invoker`.

### **4. BIG FINDING — multi-tenancy is NOT complete (Inv #208)**
The S45 `pg_class` audit (Part 2 of the SECURITY DEFINER recon) found **~22 church-stamped tables with RLS OFF** — the S44 "every per-church table / wall complete" claim was **premature**. Tier-a (latent — policies written, never ENABLEd): `attendance` (4!), `profile_tokens` (3), `devotionals`/`devotional_reflections`/`ministries`/`ministry_roles`/`system_settings` (2 each), `diagnostic_results` (1). Tier-b (unstarted — `church_id`, 0 policies): `transfers`, all 5 `prayer_*`, the `library_*` set, `ministry_roster*`, `attendance_self_attest_log`, `discipler_change_log`, `leader_sessions`. **No active leak today** (Rosehill is the only church with real data) — exposure activates at **Agape onboarding**. This is the next big arc: the **RLS-coverage sweep**, fronted by a `schema_migrations` ledger + reconcile-on-open to make the dashboard-run-not-committed gap self-detecting.

---

## ✅ COMPLETED JUNE 23, 2026 — SESSION 44 (login rewire EXECUTED · `members` RLS ENABLED — last table · tenancy COMPLETE)

Deployed files pulled from the repo + re-read before editing (Inv #56); `node --check` + browser-smoke per change; SQL human-gated (Claude proposes, Pastor runs in Supabase). **Deployed `multiply_shared.js` unchanged at `?v=7`; `multiply_tenancy.js` `?v=2`.** **Invariant #204 added → count 204.** Shipped to `main` (`6ab9aa0` EF · `72012c0` pages).

**THE ARC:** execute Inv #201's blueprint, then flip `members` RLS — the final per-church table. Done end-to-end and isolation-proven; multi-tenancy is now COMPLETE.

### **1. `auth-login` extended — `min_level` + `stamp:'leader'` (`6ab9aa0`, deployed)**
`search` gained an optional `min_level` — `.gte('pipeline_level', …)` applied BEFORE `limit(8)` (the leader picker passes `2`, so a common-name match can't fill all 8 slots with sub-leaders and hide a real leader). `login`/`set_pin` honor `stamp:'leader'` → a `stampLogin()` helper writes `leader_last_login` + inserts a `leader_sessions` row (folded in from the old client-side writes); non-leader logins stamp `member_last_login`. **Backward-compatible** (no `stamp`/`min_level` = prior behavior exactly), so it deployed safely BEFORE the pages. Back-checked by diff vs the session-start version: only the three spec changes, nothing else. CC coded; Pastor redeployed via dashboard; existing-member login re-confirmed.

### **2. Both login pages rewired — ZERO client-side `members` access (`72012c0`)**
`member_login.html` + `leader_login.html` are now thin clients over `auth-login`: dropped the supabase-js + bcryptjs `<script>`s + `window.bcrypt` shim, `createClient` (`SB_KEY` kept ONLY as the EF `apikey` header), `loadMembers`/`loadLeaders`, `verifyPin`/`hashPin`/`_mintTenancyJwt`. New `authLogin(body)` POSTs to the EF and throws `Error` with `.code = data.error` (`'network'` fallback). Search → debounced (250ms) `search` (member: no `min_level`; leader: `min_level:2`); pick branches on `has_pin`; verify → `login`, setup → `set_pin`, leader both carry `stamp:'leader'`. `loginSuccess(ef)` builds the session from `ef.profile` + carries `lc_group`/`discipler_name` from the picked search row (the profile lacks them), stores session + `multiply_jwt` in one place using the EF's `expires_at`. Member → `_safeNextUrl()||MMT`; leader → `showDestinationChooser()` (no redirect); leader `logoutAndRestart` dropped its anon `leader_sessions` write. **De-branded** (generic-branding rule): “Rosehill” removed from both (step-sub + footers → “MULTIPLY Pipeline”). Grep-clean (zero `members`/`createClient`/bcrypt/`leader_sessions`/`rosehill`); `node --check` OK. **All four login paths smoke-proven** (member returning + first-login `set_pin`, leader returning + first-login).

### **3. Unified-shell single-door login model confirmed (Inv #204)**
`index.html` sends ALL levels through `member_login`; `ensureBothSessions()` mints the leader-session counterpart for L≥2; MD/MLT (`?embed=1`) get it via the postMessage handshake. **`leader_login.html` is now a legacy/bookmark fallback** (rewired + RLS-safe; not deleted — retirement deferred). One church-scoped JWT serves every tool under RLS regardless of role; the leader session is only a client-side gate shim. (Caught from the Pastor's own observation that L0 Juana and L5 Gerry both appear in the member picker.)

### **4. `members` RLS ENABLED — the LAST per-church table (`migrations/020_members_rls.sql`)**
Pre-flight audit found **two PUBLIC allow-all stubs** (`“Allow all operations”` ALL + `“anon can update members”` UPDATE — both `USING(true)/CHECK(true)`, Inv #186), 15 NULL-`church_id` rows (13 guests + 2 real Rosehill members; 1 Agape already stamped), and the `trg_set_church_id` BEFORE-INSERT trigger present (from `migrations/014`). Migration: DROP both stubs → backfill NULL → Rosehill → 4 `TO authenticated` policies on `auth_church_id()` → ENABLE. Structural verify GREEN: `policy_count=4`, `null_church=0`, `rls_enabled=true`, `stamp_trigger=1`. `auth-login`/`token-login` (service role) bypass RLS, so login survived the flip. **No-lockout smoke** passed (re-login → MMT/MD/MLT load, member add+edit work — Inv #187). **Isolation proven in-browser** — raw `fetch` with anon `apikey` + Bearer `multiply_jwt` (no library; `alert()`-displayed to dodge the console level-filter that hides `console.log`): **L5 Rosehill → 236 rows, all `724f4826…`, zero Agape**; **Agape test member → 1 row, only `46d78009…`, zero Rosehill**. **Every per-church table now enforces tenancy. The wall is complete.** 🔒

### **5. Migration repo-commit gap noted**
`migrations/015`–`019` (the RLS rollout) + `020` (this one) were **dashboard-run, not committed** to the repo — same pattern as the pre-#202 EF gap. Queued next-up: commit `015`–`020` so the RLS migrations are version-of-record.

---

## ✅ COMPLETED JUNE 23, 2026 — SESSION 43 (`members`-RLS prep: own-anon readers closed · `auth-login` consolidated + EFs committed · login rewire designed)

Deployed files pulled from the repo + re-read before editing (Inv #56); `node --check` per change; SQL human-gated. **Deployed `multiply_shared.js` unchanged at `?v=7`; `multiply_tenancy.js` `?v=2`.** **Invariants #198–#203 added -> count 203.** All shipped to `main` (`5e21d25`).

**THE ARC:** clear the way for `members` RLS — the LAST table. Two client-side anon `members` readers blocked it; the login pages were the real wall (anon pre-auth picker + client-side PIN hash). Closed the two readers, made `auth-login` the server-side gate, committed the EFs, and fully designed the login rewire.

### **1. `member_self_edit.html` wired to `MultiplyTenancy` (Inv #198, `80ddcb5` → merged `main`)**
Dropped its own anon `createClient` + esm.sh module; now loads UMD supabase + `multiply_tenancy.js?v=2` (classic), resolves via `bootstrap()` (cold `?token=` → `token-login` mint; in-app `?id=` → existing JWT), reads/writes `members` through JWT-attached `getDB()`. Dropped the per-save `profile_tokens` `used_count` bump (the EF owns "used"); one read-only `profile_tokens` lookup stays for the expiry/saved banner. **Smoke-proven cold:** `token-login` 200, `members` GET + PATCH both carry `Authorization: Bearer`, 200.

### **2. `auth-login` consolidated — `search` / `login` / `set_pin` (Inv #199, deployed + verified)**
One EF, three anon actions on a body `action` field: `search` {q} → login-picker directory (safe cols + `has_pin`, never the hash); `login` (default, backward-compatible) → bcrypt-verify + mint 8h JWT + last-login stamp; `set_pin` → first-login claim (409 if already set) + auto-mint. Deployed via dashboard; **backward-compat confirmed** (current member login unchanged).

### **3. `bulk_send_links.html` JWT-wired (Inv #200, on `main`)**
C3b inline Bearer (read `sessionStorage.multiply_jwt` → attach) — leader-launched admin tool, no `multiply_shared.js`. Under `members` RLS the anon fallback empties the list (implicitly leader-gates it). Backlog flagged: hardcoded "Rosehill" in the WhatsApp message (Agape multi-tenancy) + `profile_tokens` insert missing `church_id`.

### **4. Both Edge Functions committed to the repo (Inv #202, `5e21d25`)**
`supabase/functions/auth-login/index.ts` + `supabase/functions/token-login/index.ts` — the dashboard-only gap (Inv #190) is CLOSED. Repo = version-control-of-record; deploy stays a dashboard step (keep in sync by hand).

### **5. Login rewire DESIGNED — not shipped (Inv #201)**
`members` RLS REQUIRES server-side login: the picker's pre-auth anon `members` read + client-side bcrypt are incompatible with RLS. Blueprint (reviewed S43): rewire `member_login` + `leader_login` to `search`/`login`/`set_pin` → zero client-side `members` access, drop `loadMembers`/`verifyPin`/`hashPin`/anon writes + the supabase-js & bcryptjs libs; `loginSuccess(efData)` stores session + the EF's JWT (one call does verify+mint+last-login). `leader_login` deltas: `selectedLeader`, leader session, Step-4 destination chooser (no redirect), and a `stamp:'leader'` flag → ~3-line `auth-login` add for `leader_last_login`. **Preaching-calendar `members` anon-read confirmed ALREADY closed** behind the S41 JWT gate (struck from pending).

### **6. Audit + workflow**
Full repo clone-audit confirmed every other `members` accessor is JWT-attached (`getDB()`/C3b). Standing rules: Claude pulls all deployed files from the repo itself; **"sync"** = CC reset-to-`origin/main` at machine switch (Inv #203, after a stale-orphan-history reconcile this session); CC prompts shaped for CC **web** (relative paths, no `cd`).

---

## ✅ COMPLETED JUNE 23, 2026 — SESSION 42 (assessment-token flow + `member_profiles`/`gifts_diagnostic` RLS — 16 tables RLS-live)

Deployed files re-read before editing (Inv #56); `node --check` + browser smoke per change; SQL human-gated. **Deployed `multiply_shared.js` unchanged at `?v=7`; new `multiply_tenancy.js` at `?v=2`.** **Invariants #190–#197 added -> count 197.**

**THE ARC:** unblock + ship RLS on the two assessment tables. The 7 assessment instruments + `member_self_edit.html` were **own-anon** (their own anon `createClient`, no JWT) and assessments were shared **cold via raw `?id=`** — both a blocker for `member_profiles`/`gifts_diagnostic` RLS AND a pre-existing security hole. Resolved by a token->JWT path, then RLS, then cold-share tokenization.

### **1. `token-login` Edge Function (deployed via dashboard, Inv #190)**
Mints a short-lived (+2h) church-scoped HS256 JWT from a `profile_tokens` token: `{token}` -> service-role validate (`profile_tokens` exists + unexpired) -> resolve `members.church_id` -> sign with the SAME `JWT_SECRET`/claim shape as `auth-login` (`verify_jwt` OFF). Returns `{token, church_id, member_id, expires_at}`; bumps `used_count`. Curl-proven for Rosehill, then later for Agape (isolation). **Not in the repo** (dashboard-only, like `auth-login` — commit gap).

### **2. `multiply_tenancy.js?v=2` standalone module (Inv #191/#192)**
Classic-script UMD sibling of `multiply_close.js`; `window.MultiplyTenancy` exposes `tokenLogin()`, `bootstrap()` (the single cold/in-app member-resolution helper), JWT-attached `getDB()`, and `readJwt`/`currentMemberId`/`churchId`/`hasValidSession`. Shipped v1 (`452aae4`) then v2 with `bootstrap()` (`3778005`). NOT folded into `multiply_shared.js` (avoids the 19-consumer `?v=` lockstep, Inv #11).

### **3. 7 assessment instruments wired (canary -> fan-out)**
`disc_profile.html` canary first (`9aa0507`): module load + `getDB()` delegation + cold/in-app resolution. **A resume regression was caught here** (Inv #193): the init `await`ed `autoLinkFromUrl()` before `buildQuestions()`, so the autosave IIFE (`start()`, DOMContentLoaded) attached `saveProgress` listeners before the inputs existed -> no draft -> no resume banner. Fixed by **render-first** (`buildQuestions()` synchronous, then `await autoLinkFromUrl()`, `0c0945f`). Then the centralized `bootstrap()` retrofit, then the other 6 (`89b4b04`: love_language, enneagram, strengths, spiritual_gifts, conflict_style, salvation — each adapted to its structure; static-question pages satisfy render-first automatically). Proven: cold `?token=` submit -> `member_profiles` row with `church_id`=Rosehill; in-app `?id=` carries the login JWT (console-verified).

### **4. `ministry_recommender.html` JWT-attached (C3b, `84dd173`)**
The one production own-anon reader of both tables (ESM page). Mirrored `profile_results_viewer.html`'s C3b inline `_readTenancyJwt()` Bearer-attach. (Non-blocking own-anon left: 2 debug tools + the stray `strengths_profile (11).html`.)

### **5. RLS on `member_profiles` + `gifts_diagnostic` (dashboard migration, 16 tables total)**
Pre-RLS audit found the **`"Enable all for anon"` allow-all stub on BOTH tables** (Inv #186 hazard). Migration (atomic `BEGIN/COMMIT`): DROP the anon stubs -> backfill 28 NULL `church_id` -> Rosehill (slug subquery) -> 4 `TO authenticated` policies on `church_id = public.auth_church_id()` -> ENABLE. Structural verify: `policy_count=4` each, `null_church=0` each, `rls_enabled=true` each. App-smoke (re-login -> MD read, recommender, cold + in-app writes) all green. **Isolation proven** via `token-login` JWTs: Rosehill -> rows (all `church_id` 724f4826…), Agape -> `[]` for both tables. A live `?id=`-cold anon insert was correctly **rejected by RLS** ("new row violates row-level security policy") — proof the stub-drop worked (Inv #194).

### **6. Phase 2d — cold assessment sharing -> `?token=` (`9f61fec`) + footgun removal (`3780f67`)**
RLS broke cold `?id=` assessment links. Refactored each tool's token helper into **`getOrCreateMemberToken(memberId)`** (reuse a non-expired `profile_tokens` row; mint only if none — one token/member across self-edit + all assessments, decision #1). MLT `sendLink_` + MD `copyProfileLink`/`copyGiftsLink`/`copyDiagLink` now emit `<file>?token=<tok>` (drop `&name=`); **in-app Take/Open paths + the getters stay `?id=`** (Inv #195). Removed the MD salvation-tab raw-`?id=` URL **text preview** footgun -> hint + tokenized Send button only (Inv #196).

### **7. Standing rule set (Inv #197)**
Pastor directive: when recommending, default to the **most elegant + scalable** option and justify on those grounds — never "easiest / less work." Ease is a tiebreaker at most.

---

## ✅ COMPLETED JUNE 21, 2026 — SESSION 41 (RLS rollout: 3 safe-now batches / 13 tables · allow-all stub purge · preaching-calendar JWT gate · MLT My Profile · MD embed-badge fix · member-self-edit Close + de-brand)

Deployed files re-read before editing (Inv #56); `node --check` + browser smoke per change; SQL human-gated (proposed -> Pastor RUN in Supabase; isolation tested via curl token, never the SQL editor). **Deployed `multiply_shared.js` unchanged at `?v=7`.** **Invariants #186-#189 added -> count 189.**

**THE ARC:** the RLS rollout moved from a single canary to **systematic table-by-table tenancy**. Three safe-now batches (13 tables) were audited (CC read-only: JWT-path grep + new `pg_policies` allow-all scan), migrated (slug-subquery backfill -> 4 `TO authenticated` policies on `auth_church_id()` -> ENABLE -> one-line DISABLE rollback), and **proven** (structural verify `policy_count=4` exactly + `null_church=0`; curl isolation Agape `[]` vs Rosehill rows; app-load smoke). A blocking anon path on the preaching calendar was closed first (Option A JWT gate) so its tables could ship.

### **1. RLS batch 1 - cohort/lesson family (`migrations/016`, RUN + proven)**
`cohorts`, `cohort_members`, `cohort_lesson_unlocks`, `pipeline_lesson_grants`. CC audit: all paths gated `getDB()`, no own-anon. Isolation proven on `cohort_members` (Agape `[]` vs Rosehill 5 rows). (Inv #187)

### **2. RLS batch 2 - communication family (`migrations/017`, RUN + proven) - caught a real leak (Inv #186)**
`announcements`, `announcement_acks`, `announcement_retraction_dismissals`, `absence_notices`, `notifications`. CC audit clean (incl. `facilitator_debrief_guide.html` = C3b patched-anon for `notifications`). **The structural verify showed `absence_notices` at `policy_count=6`, not 4** -> inspection found **two dormant allow-all stubs** (`absence_notices_read` = SELECT `public` `USING(true)`; `absence_notices_write` = ALL `public` `USING(true)`+`WITH CHECK(true)`) that `ENABLE` had activated, OR-combining to defeat isolation. **Dropped both** -> back to 4 -> Agape `[]` confirmed (the hole was closed). Lesson codified as **Inv #186** (drop stale `public`/`anon` allow-all before ENABLE; verify EXACTLY 4; pre-RLS audit now scans `pg_policies`).

### **3. RLS batch 3 - preaching family (`migrations/018` sermons + `019` preaching, RUN + proven)**
`018` `sermons` (0 rows; structural verify only). `019` `preachers`, `wednesday_preaching`, `preaching_swap_requests` - gated behind the calendar JWT fix below. CC audit (`?me=` path inspected per Inv #149), CHECK B clean (no existing policies). Isolation proven (Agape `[]`); no-lockout confirmed by the live calendar loading for the Rosehill session (curl ROSE confirmation was blocked by a local `HTTP 000` network/curl issue - not RLS, Inv #187).

### **4. Preaching-calendar Option-A JWT gate (`841a8bf`, Inv #188)**
`preaching_calendar.html` had an un-gated `?me=` logged-out path (bookmark/external/PWA/`noopener`) where `getDB()` runs anon - which RLS would break. Fix: inline `_hasValidTenancyJwt()` + boot gate -> no JWT renders a "Please log in" panel with **zero** DB calls; valid JWT loads normally. `submitSwap()` hardened to require the JWT (no bare `?me=` writes). No `multiply_shared.js` change (`?v=` stays 7). The two `?me=` link builders (MLT `585`, shared `1137`) are same-origin `window.open` and carry the JWT -> unaffected.

### **5. MLT 'My Profile' feature (`80b4b1c`)**
Header **My Profile** button (gold, replacing the 🔄 switch-leader button - `switchLeader()` kept defined but dead) -> new `screenMyProfile` with two tabs: **My Info** (read-only summary + "Edit my info" -> `member_self_edit.html` via `createSelfEditToken`, new tab) and **My Assessments** (grid over the existing `ASSESSMENTS`; status via `hasFormalAssessment` with a direct `gifts_diagnostic` fallback when the leader is out of loaded scope; `takeMyAssessment`/`viewMyAssessment` launch with the owner’s own id; Salvation shows no View by design). Reuses existing catalogs only; no MD modal ported; no share modal. `?v=` stays 7.

### **6. MD embed-badge fix (`1fb9311`, Inv #169)**
In the unified shell (`?embed=1`) the Pastor’s **My Profile button + leader badge never appeared** - `paintLeaderBadge` was an **anonymous boot IIFE** that ran while `LEADER={}` (session not yet arrived), hid `#scopeBtnMyProfile`, and was never re-run by the embed re-sync. Converted to a **named hoisted function**, re-called in the re-sync `.then()`, and the leader-present branch now restores `display:flex`. `?v=` stays 7.

### **7. member_self_edit.html - Close + de-brand (`f32200d`, Inv #29)**
Added the canonical `multiply_close.js?v=1` Close module (returns to launcher: `window.close` -> `history.back` -> referrer -> home) and replaced the "Rosehill Christian Church" `<title>` + footer with generic **"MULTIPLY Pipeline"**. Only personal fields are editable (name disabled; no level/discipler/zone) so no self-promotion risk.

### **8. Workflow**
All CC prompts now delivered as **single pastable fenced code blocks** (Inv #189; recorded in memory).

---

## ✅ COMPLETED JUNE 20, 2026 — SESSION 40 (Tenancy Phase 2 LIVE end-to-end: custom HS256 JWT auth · church_id auto-stamp trigger · FIRST RLS tenant boundary proven · MLT attendance fold · Quiz Scores report)

Deployed files re-read before editing (Inv #56); `node --check` + browser smoke per change; SQL human-gated (proposed → Pastor RUN in Supabase). **Deployed `multiply_shared.js` is now `?v=7` (19 consumers, lockstep).** **Invariants #180–#185 added → count 185.**

**THE ARC:** Multi-tenant Phase 2 was stood up end-to-end and the **first live church boundary is proven**. The chain: the `auth-login` EF mints a church-scoped JWT → the client attaches it on every DB call → a BEFORE-INSERT trigger auto-stamps `church_id` → RLS enforces isolation. Canary on `debrief_records`: **Agape token → `[]`, Rosehill token → 5 rows** on the same query.

### **1. Auth = Path 1 — custom HS256 JWT signed with the LEGACY shared secret (Inv #180)**
The project signs its GoTrue tokens with **ECC P-256 (asymmetric)** now; that private key is **not exportable**, so we cannot custom-sign with it. The **Legacy HS256 shared secret is "previous" but still in the verify set** — so we mint our own HS256 JWT with it. **Never revoke the legacy key** while this is live. Fallback if Supabase ever drops legacy verification = the GoTrue custom-access-token-hook (Path 2, rejected as too heavy for now).

### **2. `auth-login` Edge Function — DEPLOYED, verify_jwt OFF (Inv #181)** — `supabase/functions/auth-login/index.ts`
It's the login endpoint, so **Verify JWT = OFF** (no token yet at call time; it does its own PIN auth). Reads the member via the **service-role key**, verifies the PIN with **`npm:bcryptjs@2.4.3` `compareSync`** (byte-compatible with the browser's bcryptjs 2.4.3 `$2a$` hashes), then mints `{sub, role:'authenticated', aud:'authenticated', church_id, leader_level, name, is_test, is_guest, iat, exp(+8h)}` signed (djwt HS256) with EF secret **`JWT_SECRET`** (= the project's Legacy JWT Secret). Uniform `invalid_login` 401 on any failure; CORS `*`. **HS256-acceptance proven:** the minted token is accepted by the Data API (a `select` returned rows under the `authenticated` role).

### **3. JWT-claim SQL helpers + `church_id` auto-stamp trigger (Inv #182)** — `migrations/012`, `migrations/014` (RUN)
- `012` — `public.auth_church_id()` / `auth_member_id()` / `auth_level()` read `request.jwt.claims` (NULL-safe → anon behavior). In the SQL editor they return NULL/0 (no JWT there) = correct.
- `014` — `set_church_id_from_jwt()` **BEFORE INSERT** trigger sets `church_id` from the claim **only when the row left it NULL**; attached **dynamically (DO-loop)** to every per-church table that has a `church_id` column **EXCEPT `devotionals`** (NULL = shared) and `_backup%`. Verify showed **45 tables**. Sanity-confirmed: a logged-in attendance insert auto-stamped Rosehill. Do NOT edit per-page insert sites for `church_id` — the trigger owns it.

### **4. Client carries the JWT for RLS (Inv #183)** — `c7ac579`, `e39fc27`, `0f3ab90`, `91b927d`
- **C1 (`c7ac579`, login pages):** `_mintTenancyJwt()` calls the EF after PIN success and stores `{token, church_id, expires_at, member_id}` in `sessionStorage['multiply_jwt']`. Best-effort / non-fatal; inert (stored, not yet enforced).
- **C2 (`e39fc27`, `multiply_shared.js`):** `getDB()` now attaches `Authorization: Bearer <token>` via `_readTenancyJwt()` (NULL on absent/expired/malformed → anon). The 4 gate functions untouched. **`?v=6 → ?v=7` lockstep across all 19 consumers** (verified: exactly 19 repo HTML reference `multiply_shared.js`, all at `v=7`, zero stragglers — Inv #184).
- **Mixed-client finding:** MLT/MMT already used `getDB()`; **MD and 10 standalone pages built their OWN anon `createClient`** — any anon path would be locked out by RLS. **C3a (`0f3ab90`):** MD swapped its own client → `MultiplyShared.getDB()`. **C3b-1 (`91b927d`):** inline JWT-attach for 3 logged-in-context pages — `facilitator_debrief_guide.html`, `multiply_member_dashboard.html`, `profile_results_viewer.html`. Still own-anon (pending before their RLS): the 3 assessment instruments (token-flow), the 2 login pages (anon pre-auth — special), 2 debug tools (ignore).

### **5. FIRST RLS canary — `debrief_records` — isolation PROVEN (Inv #185)** — `migrations/013`, `migrations/015` (RUN)
- `013` seeded a 2nd church: **Agape Christian Fellowship** (`slug='agape'`).
- `015` — backfill `church_id IS NULL → Rosehill`; 4 policies (SELECT/INSERT/UPDATE/DELETE) `TO authenticated USING/WITH CHECK (church_id = public.auth_church_id())`; `ENABLE ROW LEVEL SECURITY`. Verify: `relrowsecurity=true`, 4 policies, `null_church=0`.
- **Proof:** an EF login as a throwaway Agape test member (token `church_id=Agape`) → `select debrief_records` → **`[]`** (zero rows). Rosehill (Pastor) token → **5 rows**. Same query, different token = full church isolation. **Test A reads PASS** (the debrief guide renders Rosehill records under RLS). **Test A writes PASS — a new debrief was created in-app AND revisited to verify it actually persisted** (INSERT/UPDATE policies validated). The first live tenant boundary is real. 🎉

### **Non-tenancy ships**
- **MLT 'My Attendance' fold (`bcf984d`)** — `lc_leader_tool.html`: the L5 self-attest card collapses by default; tappable header + chevron; remembers the choice in `localStorage['mlt_attest_open']`.
- **Quiz Scores report (`32efab2`, new `quiz_scores_report.html` + MD/MLT launchers)** — Usbong + BTLI best-of-retakes per member, scoped by viewer (pastor = all-real excl. test+guest; else self+downline, own-kind); views `v_btli_member_best` / `v_usbong_lesson_grade` / `v_*_course_grade` (fallback to `*_quiz_attempts` MAX on 404); enrollment via `cohort_members→cohorts→cohort_programs` (no `!inner`, Inv #38); "Unenrolled" per program; `multiply_close.js?v=1` Close.
- **Quiz Scores polish (`a53090b`)** — collapsed cell "{grade}% of {N}"; 5-col detail (Lesson · Title · Best · Attempts · Status, P% passing); a "How to read this report" panel (the rollup-% confusion fix — "9% of 11" = course-completion, not a quiz score).

### **Key IDs**
- Rosehill `church_id` = `724f4826-0f83-4ce8-bfc0-83698b79ce6f`
- Agape `church_id` = `46d78009-8b7d-4c2f-9053-bc99aff13b24`
- Agape Test member = `4c79ce89-0c43-4b4e-b916-88b2b92fa5c4` (`is_test_member`, PIN `1234`, church=Agape) — **permanent isolation-smoke account**
- Pastor Gerry member = `547ebda6-5126-436e-9890-709f50588ced`

### **Note — CC "Unverified" signing reports = known false positive**
Local SSH-signature verify can't run (`gpg.ssh.allowedSignersFile` unset + 0-byte key) → CC shows "Unverified"; GitHub validates against the registered key and shows the commits **Verified**. No action ever needed.

---

## ✅ COMPLETED JUNE 20, 2026 — SESSION 39 (assessment Close/return polish · shared multiply_close.js module · MMT lands back on Discover · enneagram parity · devotional ≥10-word gate · celebration feed floor-6/LC-leader/self-anchor · MMT font-zoom fix)

Deployed files re-read before editing (Inv #56); `node --check` + browser smoke per change. **Deployed `multiply_shared.js` is `?v=6`** (older `?v=4` mentions below are historical). **Invariants #173–#179 added → count 179.**

### **1. Devotional streak/SVI require a ≥10-word reflection (Inv #173)** — `member_tool.html` + `migrations/011`
Bare reader-close no longer writes a devotional `attendance` row; credit only on a ≥10-word reflection (exit-reminder modal when short, text still saved). `migrations/011` (diagnostic+cleanup+rollback) RUN by the Pastor (today-inclusive); backup `_backup_devo_attendance_cleanup_20260620`; deleted = 0-word reads, lowest kept = 18 words.

### **2. LC Celebration feed — floor-6 + LC-leader modeling + self-anchor (Inv #174–#175)** — `member_tool.html`
Streak shown continuously while ≥6 (badge "6+"); LC leader's own streak always-on (👑 LC LEADER, ignores `share_with_lc`); no-discipler pastoral staff with a flock anchor the feed to self, normal LCLs keep their upline LC.

### **3. MMT font-increase compression fix (Inv #176)** — `member_tool.html` (CSS only)
Inside-zoom centered padding changed to `100vw/var(--fs)` at the four sites; native chrome unchanged.

### **4. Shared Close module + sweep (Inv #177)** — new `multiply_close.js?v=1` + 7 assessment files
Sibling module self-injects ✕ Close, returns without closing the launcher (window.close→same-host history.back→referrer→home). Added to the 6 missing instruments; strengths converted from inline to module. MD = new tab (window.open), MMT = same-frame.

### **5. MMT lands back on Discover (Inv #178)** — `member_tool.html`
`goTo` persists `mmt_current_screen`; `openAssessment` stamps `mmt_return_screen`; `pageshow` one-shot restore → Discover. Covers reload + bfcache; fresh login still Home.

### **6. Enneagram parity — lang bottom-right + font slider (Inv #179)** — `enneagram_profile.html`
Lang toggle moved top-right→bottom-right floating (cleared the ✕ Close); font slider added (`body.style.zoom`, 5 steps, key `multiply_enneagram_fontscale`; safe — max-width-centered). All 7 instruments now have Close+font+language parity.

---

## ✅ COMPLETED JUNE 19, 2026 — SESSION 38 (SVI embed-scoping fixes · transparent care-detail · LC-meeting scoring · Preaching Auto-fill anchored to schedule-end)

Deployed files re-read before editing (Inv #56); reviewed post-ship via `raw.githubusercontent.com`. **Invariants #169–#172 added → count 172.** ✅ The EF 150s blocker is **RESOLVED** — the `svi-ef-bulk-prefetch` refactor shipped + the Pastor redeployed `compute-svi-weekly`; the full 231-member compute now runs in **seconds** and **LC-meeting scoring (Inv #171) is LIVE** (dry-run parity verified — expected, no drift; scores shifted accordingly, e.g. May Ann < 70.67).

### **1. Embed-mode SVI scoping — three fixes, one root cause (Inv #169)** — `multiply_dashboard.html`
- **Root cause (embed-timing bug class):** the shell loads MD as an iframe (`?embed=1`) and the session arrives **async via postMessage AFTER** `const LEADER = window.LEADER || {}` (~1480) binds → `LEADER` stays `{}` (leaderLevel `undefined`, isPastor false) at script-load. Confirmed live in the iframe console: `LEADER.lvl=undefined | isPastor=true | sess.lvl=5 | embed=true`. Standalone is fine (the gate runs synchronously first).
- **Fix (one re-sync, three consumers):** after `const LEADER`, an embed-only re-sync — `if (MultiplyShared.isEmbedMode?.()){ MultiplyShared.embedSessionReady(3000).then(()=>{ const s=MultiplyShared.getValidSession(); if(s) Object.assign(LEADER,s); revealPastorTab(); _refreshMyMinistryNav(); /* scope-count setter */ }); }`. It mutates `LEADER` **in place** once the handshake lands, then re-runs the boot IIFEs (which had been converted from anonymous IIFEs to **named hoisted functions** so they can be re-called).
  - **(a) SVI report scope** — `_crScopedIds` (uses `(LEADER.leaderLevel||0)>=3`) was scoping the Pastor to disciples-only ('your disciples · 0'). Now shows **231 church-wide**.
  - **(b) '🔓 All Members' tab** (`revealPastorTab`, id `scopeBtnAll`) was hidden for the Pastor in embed. Now revealed.
  - **(c) MMT greeting / My-Ministry nav** refreshed in the same `.then()`.
- **Pastor confirmed all three fixed.**

### **2. Care-detail modal — full score transparency** — `multiply_dashboard.html` (`openCareDetail`)
- Each **dimension** (word/gather/growth/prayer/mission/service/fellowship) shows a bar + nested **per-metric** rows `score × weight = contribution` (+ raw), labelled from `svi_metrics` (display_name/category/sort_order via `_crMetricMeta`), grouped by `metric_scores[].category`.
- **Score-computation panel:** `Σ(score×weight)=N` · `Σweight=N` · `Total=(Σsw÷Σw)×10 = total_score ✓` · Zone line vs profile thresholds (verified e.g. May Ann 735÷104×10 = 70.67 ✓).
- **How-each-number-is-derived section** (`explainMetric`): data-driven from `compute_type`/`compute_config`/`score_rules` — window dates (weekStart − lookback), raw meaning per compute_type, the triggered score tier — in plain language. CC's 9/9 harness reproduced the worked examples (Devotional 44%→6×20=120, Sunday 100%→10×15=150, Diagnostic 42 d→10×10=100, EOLO 3→8×15=120).

### **3. LC-meeting-based SVI scoring (Inv #171)** — `index.ts` + `migrations/010_lc_meeting_scoring.sql` (commit `e00c435`)
- **EF (reviewed clean; now LIVE — see item 6):** member-load gained `discipler_id`; `lcKeyOf(m)` = L2+ keyed by own id else `discipler_id`; a `meetingsHeld` Map (`Map<lcKey, Set<date>>`) is precomputed once from the unfiltered member map × 8-week LC-Meeting **present** attendance, passed into `computeMemberSnapshot`.
  - `service_lc_led`: **L<2 → {raw:null, score:null}** (not counted); else raw = `meetingsHeld.get(member.id).size` → `applyScoreRules`.
  - `gather_lc` / `fellowship_lc_rate`: if the member's LC **didn't meet** that window → `score = compute_config.no_meeting_floor_score` (??4) + note `no_meeting_held`; a **real low score is kept** if the LC met but the member was absent.
- **Migration 010 (Pastor RAN + verified):** `service_lc_led` `score_rules` → `linear_threshold` tiers (7→10, 5→7, 3→4, 1→2, 0→0); `gather_lc` & `fellowship_lc_rate` got `no_meeting_floor_score:4` merged into `compute_config`. (`service_lc_led` still carries a vestigial `computed:lc_meetings_led` flag — harmless; the EF special-cases it by metric_key.)
- **Decisions:** source = member LC-meeting attendance (not the LCL's); applies to **L2+**; fellowship softening = partial floor 4 (not full 0); 'meeting held' = ≥1 group member present. Footnote 'About LC meetings' added to the modal.

### **4. MMT greeting — time-of-day aware** — `member_tool.html` (Pastor confirmed)
- `#greetingTxt` was hardcoded 'Magandang umaga,'. Added `_setGreeting()` keyed on the **Manila** hour (`Intl.DateTimeFormat timeZone:'Asia/Manila'`): 0–4 & 18–23 → gabi, 5–11 → umaga, 12–17 → hapon; sets both `en-text`/`tl-text`; called at boot.

### **5. Preaching Auto-fill — window anchored to schedule-end (Inv #172)** — `preaching_admin.html` (`autofillNext12`, reviewed clean post-ship)
- **Was:** rolling `today → today+84d` (so the new dates added shrank as the schedule grew). **Now:** queries the **latest** assigned `preach_date`, sets `startDate = later of (lastAssigned, today)` (never backfills the past), `windowEnd = startDate + 84d`. Each Auto-fill press always extends a full 12 weeks **past the end** of the current schedule.
- Rotation **pointers** (Wed '2 then mainstay', Sun 'last-of-month rotates') come from DB history `< winStart`, so continuity holds regardless of the anchor. **Gaps before the last-assigned date are intentionally skipped** (Pastor agreed — forward-extend only). The recurring-pattern past-lookup now uses `startStr` (not `todayStr`). The confirm popup now prepends **'From … to …'** so the span is visible.

### **6. SVI Edge Function — `svi-ef-bulk-prefetch` refactor SHIPPED + DEPLOYED (resolves Inv #170)** — `index.ts` (compute-svi-weekly)
- The full 231-member compute was blowing Supabase's **150s wall-clock limit** (~2,500 sequential per-member queries) → no snapshots written → the LC-meeting scoring (Inv #171) never took effect. **Fix:** every source is now bulk-prefetched **once**, fully paginated (`fetchAllPaged` + `.range()` past the 1000-row cap) into per-member Maps (attendance 8w, devotional 4w, interventions 26w, latest diagnostic/member, prayer ×4 count_rows, prior snapshots @ −28d) + a derived `meetingsHeld` Map. `computeMemberSnapshot` is now **synchronous** and reads only from `prefetch` — **zero `await supabase.from()` in the per-member loop** (verified on `main`); the only DB I/O is setup (before the loop) + the final `svi_snapshots` write.
- **Pastor redeployed** the EF → the full compute now finishes in **seconds**. **Dry-run parity = expected results, no drift** (Inv #112). With the EF running, **LC-meeting scoring (Inv #171) is now LIVE** — scores shifted as designed (e.g. **May Ann dropped below 70.67**).

---

## ✅ COMPLETED JUNE 14, 2026 — SESSION 36 (lc_group decommission Tiers 1–4 · Prayer bugfixes + Wave 2 · devotional-nag · MLT sorting)

All `node --check` clean + truth-tables per commit; no `multiply_shared.js` change anywhere → `?v=4` held throughout. **Invariants #166–#168 added → count 168.**

### **1. `lc_group` decommission (Inv #166)** — the session's spine
`lc_group`/`member_lc_group`/`event_lc_group` are **decorative**; canonical grouping/matching/filtering/counting = `discipler_id` → LCL (or `facilitator_id` for transfer state).
- **Tier 1 — attendance reports re-attributed by member `discipler_id` + three-state heartbeat (Inv #167):** **LACR** (`lc_attendance_report.html`, `e23554f`) and **LCG Pulse** (`lcg_pulse_report.html`, `bca7871`). Self-attest rows (event_lc_group NULL) were dropped → groups falsely "Awaiting"; now each row is credited via the attended member's `discipler_id`. States: **Logged** (leader-source) / **Self-Attested · Awaiting Confirmation (N/M)** / **Awaiting Your Heartbeat**. Pulse also gained **test+guest exclusion** (Inv #15 — was test-only).
- **Tier 2 — audit + diagnostic:** repo-wide classified sweep (chat report) + read-only [diag_lc_group_views.sql](diag_lc_group_views.sql) (`ba8d647`). The 3 SQL views (`v_member_attendance_rates`/`v_attendance_summary`/`v_consecutive_absences`) have **zero repo/Edge consumers** (dormant); defs live-only — awaiting Pastor paste-back.
- **Tier 3 — announcement LCG targeting (`e889be1`):** MD + MLT pickers emit one `lcl:<leaderId>` entry per LC Leader; matchers (MD/MLT/MMT) key on `lcl:<discipler_id>` / `lcl:<own id>`; legacy text-name drafts fall back. Fixes null/stale-`lc_group` flocks (e.g. Roce's) missing announcements; MLT chip + size calc updated.
- **Tier 4 — absence-notice + transfer-state + LC-family (`dc15c8f`, `0dfc641`):** MLT heads-up feed → **own disciples only** (live `discipler_id`, no church-wide bleed — the Pastor's reported symptom); transfer-pending detection → `pending_facilitator_id !== facilitator_id` (MLT 5 sites + transfer_management 2 sites); MMT `loadLcDetails`/`loadCelebrationFeed` → always `discipler_id` (dropped the `.eq('lc_group')` branch).
- **Tier 4B — PROPOSED human-gated SQL (NOT run; rollback-first, idempotent), `c3d262d`:** `migrations/004` rewrites `promote_pending_lc_transfers()` to promote by `facilitator_id`/`discipler_id` (old fn moved only text); `005` drops the 6 dormant lc_group indexes (full rollback); `006` drops the 3 dormant views (⛔ blocked — paste captured defs into `006_rollback` first).
- **Tier 4C — DEFERRED:** `index.ts` (compute-svi-weekly) writes `lc_group` into `svi_snapshots` as a passenger column (`v_svi_latest` keys on `member_id` — decorative). Changing → `discipler_id` needs `supabase functions deploy`; defer unless full purity wanted.

### **2. Prayer / Panalangin — Wave 1 bugfixes + Wave 2 (Inv #165)** — `member_tool.html`, no SQL, `?v=4`
- **Wave 1 bugfixes:** requester-only "Mark answered" + read-only badge for savers (`2133d79`); answer **propagates to savers** + save-on-already-answered stamp (`2133d79`); **no active action on a non-open request** (`84b134d`); **single** answered indicator — dropped the duplicate pill (`486c3eb`); **My-List is a layered overlay** + main sheet closes only via Close button (`9877dbe`, `938ad3f`).
- **Wave 2 SHIPPED** (`7daca68`): 🙏 intercession "N praying" (`prayer_intercessions`, distinct-member count, no leaderboard); requester **pull aggregates** ("saved by N · N praying", no inbox); **praise → celebration feed** (computed win, anonymity preserved); **soft care flag** (`care_flag` + `prayer_care` notification to discipler + same-kind pastors, anonymity exception for shepherding); **My-List category filter**. **Phase-2 RLS:** cross-member writes → `SECURITY DEFINER` RPCs (logged). **W3/W4 HELD.**

### **3. Devotional reflection nag — existence-gated (Inv #168)** — `member_tool.html` (`c0f41aa`)
Was firing on devo-less days (Sundays/gaps). Now fires only when a `devotionals` row exists for today AND the member hasn't reflected today; own existence query (parallel-safe), device-local date (Inv #12), fail-soft.

### **4. MLT sorting + UX**
Batch roster participants A→Z (`ce4c88f`); **My Batches** list grouped by program with clear sticky headings, batches A→Z within (`3836edd`).

### **5. Repo hygiene** (`deff939`): recovered `index.ts` (compute-svi-weekly) after a schema-dump clobber → preserved the schema-dump fn at `supabase/functions/schema-dump/index.ts`; committed `CC_SETUP.md`; dropped stray `ROLES.md` (eastbridge-op) + spent `.patch` files.

---

## ✅ COMPLETED JUNE 14, 2026 — SESSION 35 (Prayer / Panalangin Wave 1 — MMT prayer-request board)

Built on a **read-only laptop cloud session** (push blocked → delivered as `git format-patch`, applied + pushed from the desktop). On `main` (`1b3f2c8` → `2ca3411`). `node --check` clean; 22/22 truth-tables per commit. No `multiply_shared.js` change → `?v=4`. **Inv #165 added → count 165.**

### **1. SQL — 4 per-church tables** (`migrations/003_prayer_wave1.sql`, SHIPPED `1b3f2c8`; **run + verified in Supabase**)
- `prayer_requests` (audience model), `prayer_list_items` (saved-to-list, partial `UNIQUE(member_id,request_id) WHERE request_id NOT NULL`), `prayer_list_opens` (logs My-List opens), `intercessions` (exists for Waves 2–4, unused in W1). Each idempotent, nullable `church_id` + FK + indexes (Inv #153–#156), each ends with `DISABLE ROW LEVEL SECURITY` (Inv #5/#10). **`verify_003` JSON `all_ok:true`.** `003_rollback.sql` on standby (NOT run, Inv #157). `003_verify.sql` folds all checks into one JSON row for the mobile editor.
- **Audience model:** `lcg` (requester's LC group via LCL linkage, Inv #1/#46) · `individuals` (`audience_member_ids uuid[]`, GIN-indexed) · `all` (everyone, still kind-bubbled) · `discipler_pastor` (discipler + any L5 pastor).

### **2. MMT surface** — `member_tool.html` (SHIPPED `c9bf718`, adjustments `6551d65`)
- **Home:** prominent **Prayer / Panalangin** CTA card near the top (light relevance badge; **NO points/score/badge UI**, Pastor's standing intent).
- **Compose modal:** 4 audiences, anonymity **off by default**, category, kind-scoped picklist. **Adjustment `6551d65`:** `individuals` picker opens to the **full same-kind church roster** (reach outside the requester's LCG, per spec; `share_with_lc` opt-outs + self excluded) — replaced the prior circle-only scope. `discipler_pastor` pastor detection now keys on **`pipeline_level >= 5`** (mirrors `_isSuperuser` L5; MMT has none), degrading to **discipler-only** in a guest/test bubble.
- **Feed + lists:** feed of visible requests, **Save-to-list**, **mark-answered** (praise note); **My Prayer List** logs a `prayer_list_opens` row on each open.
- **Kind bubble (Inv #164):** `requesterKind === viewerKind` across audiences/picklist/feed, viewer = the member's own `members` row (Inv #162, never the session shim). Helpers **local to the prayer block**; **Phase B:** centralize `sameKind` in `multiply_shared.js`. Fail-closed; no `!inner` (Inv #38); avatars never blank (#93/#133); bilingual EN+TL; no URL leaks.

### **Waves HELD until Pastor's go** (see PENDING)
- **W2** encouragement: prayed-tap "N praying", praise→celebration feed, notify-on-save, soft care-flag, category filter. **W3** reminders + EOLO seeding + auto-expiry (`expires_at`). **W4** silent SVI prayer category (`count_rows` — invisible to members; gamification stays hidden, SVI factors it). The `intercessions` / `prayer_list_items` / `prayer_list_opens` tables exist for these.

---

## ✅ COMPLETED JUNE 12, 2026 — SESSION 34 (kind-mirroring Phase A — Pastor sees test/guest members)

`multiply_dashboard.html` + `lc_leader_tool.html` (SHIPPED `fc586a1`). `node --check` clean; 28/28 truth-table. No `multiply_shared.js` change → `?v=4`. **Inv #164 added → count 164.**

- **Rule (Inv #164):** each kind (real / test / guest) is a self-contained bubble. **`memberKind`** (test wins over guest) vs **`viewerKind`** read from the viewer's own `members` row (never the session shim, Inv #162). **Operations** (`_scopedMembers`, disciples trees, unassigned pool, all member pickers, attendance lists, All Members): a **real** viewer (Pastor) sees **every** kind with a **kind badge** (🧪 Test / 👤 Guest); test/guest viewers see own-kind only. **Statistics** (`liveMembers` chokepoint, scope counts, KPIs, pipeline/zone/EOLO/alert distributions, MLT home stats, dup-check pool): `memberKind === viewerKind` only — kinds never cross-count.
- **This is what makes the Pastor SEE test users in MD.** Generalizes #6/#58/#160/#162. Report HTMLs keep the strict Inv #58 predicate **until Phase B** (which also centralizes the helpers in `multiply_shared.js` → `?v=` bump).

---

## ✅ COMPLETED JUNE 5, 2026 — SESSION 33 (guest/test inheritance · self-attest past-date · viewer Close button)

Deployed files re-read before editing (Inv #56); `node --check` + truth-tables (Inv #105). No `multiply_shared.js` change → all consumers stay `?v=4`.

### **1. Members created by a guest/test user inherit that flag** (Inv #162) — `multiply_dashboard.html` + `lc_leader_tool.html` (SHIPPED `4c48e83`)
- **Problem (Pastor):** guests (invited pastor-friends) add their own members; those defaulted to Rosehill → contaminated stats, forcing manual deduction. **Fix:** a new member inherits the **creator's** `is_external_user` / `is_test_member`. **MD Add Member** — NEW members only (`eid` falsy): `effIsExternal/effIsTest = checkbox OR (isNew && creatorFlag)`, creator read from the global `members` row for `LEADER.leaderId`; a guest can't create a Rosehill member even by unchecking the box (intended containment); edits keep form/lock logic. **MLT `saveNewMember`** — `currentLeader` is a session shim without the flags, so the creator's row is read from the DB; payload now carries both flags; fail-soft. **6/6** truth-table.
- **Forward-only** (Inv #162): pre-existing guest-added members aren't retroactively flagged — a one-time **backfill SQL** (flag members whose discipler is a guest/test) is **offered, not yet run**. **Phase 2:** consolidate both insert paths into one `newMemberDefaults(creator)` that also sets `church_id`.

### **2. Self-attest PAST-date correction** (Inv #163, extends #152) — `member_tool.html` (SHIPPED `b4b792f`)
- **Option A:** correcting an existing **leader-marked absence** is now allowed at **ANY age** — `_doSaveAttest` runs the **dup check first**, so the absent→present flip returns before the window; `SELF_ATTEST_WINDOW_DAYS` (=7) now gates **brand-new claims only** (just before insert). The picker (`openAttestDatePicker`) gained a native `<input type="date">` (max=today) so any past Sunday/Wednesday is reachable; recent-day quick-picks no longer disabled past the window (save logic decides). **6/6** truth-table.

### **3. profile_results_viewer — always-visible ✕ Close** (applies Inv #12) — `profile_results_viewer.html` (SHIPPED `58944e9`)
- The viewer opens in its own tab (`window.open(_blank)`); tablet/PWA users had **no way back** and got stuck. Added a fixed top-right **✕ Close** (on every view) using the standard self-close: `window.close()` → returns to the exact firing tab (scroll/state preserved); fallbacks = same-host referrer (the firing page) → history → safe home (cross-host referrer ignored). Self-contained file (no shared.js). **5/5** fallback harness.

---

## ✅ COMPLETED JUNE 5, 2026 — SESSION 32 (three quick fixes: MLT attachment download · MD roster alphabetize · MD guests visible)

Deployed files re-read before editing (Inv #56); `node --check` + truth-tables (Inv #105). All `multiply_dashboard.html` / `lc_leader_tool.html` — **no `multiply_shared.js` change → `?v=4` unchanged.**

### **1. MLT "My Lessons" — office/binary attachments download on first click** (Inv #161) — `lc_leader_tool.html` (SHIPPED `28722e2`)
- `_openLessonAttachment` always opened the in-app **iframe viewer**; a `.pptx` can't render there → a **blank page** whose "↗ New tab" link did the real download (two steps). Now `_isDownloadAttachment` routes `.pptx/.ppt/.docx/.xlsx/.odp/.ods/.odt/.key/.zip` to a first-click **download** (`_downloadAttachment`): same-origin (decks live in the repo `lessons/**.pptx` → GitHub Pages) via `<a download>` (no tab); cross-origin via `window.open`. HTML/PDF still preview in the viewer. MMT/MD have no such viewer. **12/12** truth-table.

### **2. MD batch roster alphabetized** — `multiply_dashboard.html` (SHIPPED `aa48d90`, `611a79a`)
- `_coRenderRoster` sorted by status→ROLE; now active-first (then graduated, then withdrew) and **alphabetical by name within each status group** (`aa48d90`). The "Pick a member" add-dropdown is also sorted A→Z (`611a79a`). Pure ordering change.

### **3. MD DISCIPLES tab keeps GUESTS visible** (Inv #160) — `multiply_dashboard.html` (SHIPPED `bccdff6`)
- **Bug:** marking a member as a guest (`is_external_user`) made them vanish from their discipler's MD profile DISCIPLES tab. Root cause: `_disciplesDirect` / `_disciplesTree` / `_getUnassignedMembers` lumped guests with **test fixtures** under `_shouldHideTest` (`m.is_test_member || m.is_external_user`). But guests are **real** pastor-friends test-driving — their relationships are operational and must stay visible; only **statistical reports** exclude guests (their own rule, Inv #6). Fix: those 3 operational filters now hide **test fixtures only**. **Left as-is, flagged:** the main-screen scope-count badges (`updateScopeCounts` 1538) + Pastor "All" via `liveMembers()` still exclude guests (anti-inflation headline counts) — pending pastoral review. **5/5** truth-table.

### **⏸ PAUSED mid-session — self-attest past-date correction** (not started; design decision pending — see PENDING)
- The S29 absent→present self-correction works, but only within `SELF_ATTEST_WINDOW_DAYS = 7` (`member_tool.html`), so **old** leader-marked absences can't be flipped. A date picker already exists (last 14 days) but disables anything >7d. The flip logic (`_doSaveAttest`) is date-agnostic — the only blocker is the window. **Config map:** one constant (`:7282`) enforced at 3 points (`memberSelfAttest` :7800, `openAttestDatePicker` :7858/:7893, `_doSaveAttest` :7941); one code path gates ALL plain events. Awaiting the window-policy call (below) before building.

---

## ✅ COMPLETED JUNE 5, 2026 — SESSION 31 (Usbong quiz visible to batch staff, not just L0)

Continuation after the S30 `m.md`. **Bug:** in MMT, a teacher discipling an Usbong batch saw the **lessons** but not the **quizzes**. Deployed files re-read before editing (Inv #56); `node --check` + truth-table verified (Inv #105).

### **Usbong quiz section gate: L0 → "L0 OR live-batch staff"** (Inv #159) — `member_tool.html` (SHIPPED `076d89b`)
- **Trace:** `renderUsbongQuizzes` hid the Pre-Pipeline quiz section whenever the member's own `pipeline_level !== 0` (the Session-5 L0-only gate) and **returned before** calling `eligibilityForMany`. So **Juan dela Cruz** (L2, **teacher** of "Juan Usbong Disciples") was hidden out — even though `MultiplyShared.usbong.eligibilityFor` already grants him via the staff bypass (`multiply_shared.js:1888`). Live SQL confirmed his teacher enrollment is correctly tagged (`usbong_course_code='Usbong 1'`, cohort `active`, program active) → the **shared eligibility gate was NOT the cause**; the **section-display gate** was. Lessons show because lesson visibility is role/enrollment-based, not level-gated.
- **Fix:** added `_isUsbongStaff(memberId)` (mirrors shared.js `_usbongFetchEnrollments`: active staff `cohort_members` → live cohort `active`/`forming` → active program with a `usbong_course_code`; fails closed) and changed the gate to `if (lvl !== 0 && !(await _isUsbongStaff(...))) hide`. **No `multiply_shared.js` change → consumers stay `?v=4`.**
- **MLT:** swept `lc_leader_tool.html` — its only quiz code is the **attendance picker**; there is **no member-facing quiz-taking section** and no L0 gate, so nothing to change there for this bug.
- **Verified:** `node --check` clean; **11/11** truth-table (Juan teacher→show; exclusions: BTLI-only apprentice, participant, non-live cohort, untagged program, no enrollment; L0 always shows).

---

## ✅ COMPLETED JUNE 5, 2026 — SESSION 30 (Usbong quiz blank-stem fix — tolerant reader + data normalization)

Continuation after the Session-29 `m.md`. **Bug:** Usbong quiz showed **questions blank but choices present**. Root cause = the #125 drift pattern, but on the question **STEM** (the option fix never covered it). Deployed files re-read before editing (Inv #56); `node --check` + truth-table verified (Inv #105).

### **1. Tolerant `_qText` stem accessor** (Inv #158) — `usbong_quiz_player.html` + `btli_quiz_player.html` (SHIPPED `0e5fa20`)
- The players read the stem from raw `q.question_en`/`q.question_tl` (no fallback) at BOTH the render (~633) and answer-review (~905) sites; options already used the tolerant `_optText`, which is why **choices rendered but the stem was blank** (`escapeHtml(null)`→`''`). Live SQL confirmed **Usbong 1 lessons 6–10** store the stem under **`stem_en`/`stem_tl`** (`has_question_en_key=false`); lessons 1–5 are canonical `question_en`.
- Added `_qText(q,lang)` mirroring `_optText`: `question_en ?? stem_en ?? en ?? question` (+ `tl`), applied at all **4 sites** (both forks × render+review). Reader-only, no data touched. **Verified:** `node --check` clean on both; **11/11** truth-table (incl. the exact `stem_en` lesson-6 case + `question_en` precedence). No `multiply_shared.js` change.

### **2. Migration 002 — normalize the data to canonical** (Inv #153/#157) — `migrations/002_normalize_usbong_stems.sql` (+ `002_rollback.sql`) (`d93da27`; **run + verified by the Pastor**)
- The tolerant reader unblocks the players, but `lesson_quiz_editor.html` reads `question_en` raw (lines 827–828) → L6–10 stems were also blank there. **Read-only diagnostic confirmed** every L6–10 question has non-empty `stem_en`/`stem_tl` (8×5 = 40, none missing) — a clean key-rename, no re-authoring. The diagnostic's full-question dump also showed options use bare `{en,tl,correct}` — **left as-is** (tolerated by the players' `_optText` and by the editor's own load/save `label_en⇄en` remap), so the migration touches **only the stem keys**.
- `002` renames `stem_en→question_en` / `stem_tl→question_tl` for **Usbong 1 L6–10**, idempotent (`stem_en`-only CASE + `IS DISTINCT FROM` guard), scoped (lessons 1–5 untouched), ending with the #125 verify-SELECT. **Verify result: all 5 lessons `all_have_question_en=true`, `any_stem_en_left=false`.** `002_rollback.sql` reverses it (question→stem, L6–10). SQL human-gated (Pastor ran it in Supabase; CC holds no service-role key).

### **Net:** Usbong quiz blank-question bug fixed on **both layers** — tolerant readers (drift can never blank a stem again) + normalized data (the editor shows them). Smoke-test: Usbong 1 L6 in the player + `lesson_quiz_editor.html` now show the stems.

---

## ✅ COMPLETED JUNE 5, 2026 — SESSION 29 (self-attest present-aware fix · tenancy Phase 1 foundation · attendance_admin LC-Leader filter + mode toggle)

All logic `node --check`-verified with truth-table harnesses (Inv #105); deployed files re-read before editing (Inv #56/#126). SQL is human-gated — the Pastor ran it in Supabase (Inv #153).

### **1. Self-attest present-aware fix** (Inv #152) — `member_tool.html` (SHIPPED `9306ebc`)
- **Bug:** the "I'M HERE AT SERVICE" pill disable was **present-blind** — `byLeader` fired on ANY leader-logged (non-`self_attest`) row for this week's occurrence, **including `present=false`**. The Pastor logs on-site and marks online members **absent** before they self-report → an online member (e.g. **Dennis Nolasco** `76c7cdd4…`, whose target-date rows were confirmed `present=false, lcl_logged` via live SQL) was **locked out** of self-attesting and shown a misleading "✓ by leader."
- **Diff A (render, ~7470/7561):** added `present` to the `_leaderLoggedSlots` select; `byLeader` now requires `present===true`; new `byLeaderAbsent` → the pill stays **enabled** with an honest **"marked absent — tap if you were here" / "naka-absent — i-tap kung dumalo"** hint (no false ✓).
- **Diff B (save, ~7960):** a tap on a leader-marked-absent slot **corrects** the row → `present=true, source='self_attest', logged_by=member`, updated **by `id`** (safe: **UNIQUE(member_id,event_type,event_date)** confirmed live as `attendance_member_id_event_type_event_date_key`). It then surfaces in the leader's **Pending Confirmations** (✓ Confirm / ✕ Dispute).
- **Verified:** `node --check` clean; **12/12** truth-table. Confirmed the confirm path is real, and that self-attest `present=true` **counts immediately** (no `confirmed_by` gate in reports — confirmation is a trust overlay, not a counting gate). **No `multiply_shared.js` change → stays `?v=4`.**

### **2. Devotional streak — WORKING AS DESIGNED (non-bug; draft rolled back)**
- Reported as a bug ("1-day streak" despite 4 days), investigated, confirmed correct. The streak reads **`attendance`** (`event_type='devotional'`, `present=true`) via `loadDevoStreak`/`computeStreak` in **local Manila** time (UTC boundary ruled out). A **catch-up** entry (the 6/2 devotional done on 6/3) legitimately breaks consecutiveness → **"1-day" is correct**. The exploratory draft was **rolled back via `git restore`** (nothing shipped). **Do not re-investigate.**

### **3. Tenancy Phase 1 — foundation DONE (multi-tenant Option A)** (Inv #153–#157)
- **`migrations/001_add_tenancy.sql`** (`baa0250`) **run by the Pastor in Supabase and verified**: created **`churches`** (RLS disabled), seeded **Rosehill** (`slug='rosehill'`), added **nullable `church_id` + index + FK** to the **40 PER-CHURCH** tables (backfilled to Rosehill) and to **`devotionals`** (no backfill; **NULL = shared**).
  - **Verify 6b:** all **41** `has_church_id=true`. **6c:** all 40 backfilled; `devotionals` **52 NULL / 0 set** (correct); sentinel row 1.
  - **Expected NULL trickle (Inv #155):** 8 high-traffic tables showed a small `null_ct` (announcement_acks 10, attendance 10, leader_sessions 9, devotional_reflections 7, usbong_quiz_attempts 2, cohort_members/member_profiles/profile_tokens 1) = live writes that landed **after** the backfill (app doesn't set `church_id` yet). **Expected, harmless, NOT a failure** (a real failure = whole-table `set_ct=0`).
- **`migrations/001_rollback.sql`** (`d24a6a3`) = emergency undo (41 `DROP COLUMN` + `DROP TABLE churches`, derived from 001, idempotent, **NOT run**). **Free tier → no backups**, so the rollback script is the net (Inv #157).
- Established the versioned **`migrations/`** folder; **SQL stays human-gated** (Pastor runs in Supabase; CC holds no service-role key — Inv #153).

### **4. attendance_admin — LC-Leader filter + edit-modal mode toggle** (SHIPPED `b93d752`) — `attendance_admin.html`
- **Filter:** replaced the free-text "LC Group" search with an **"LC Leader" dropdown** populated from members' **discipler** (LCL, Inv #46) — `lc_group` is mostly null but every member has an LCL; `_loadLclIndex()` builds a `member_id→{LCL id,name}` map and `applyFilters` matches each row by its member's LCL (+ a "No LC leader" option).
- **Edit modal:** added a **Face-to-face / Online** select bound to the existing `attendance.attendance_mode` column (the list already rendered its 🌐 badge; the modal just couldn't set it). Save still stamps `source='pastor_admin'`.
- **Verified:** `node --check` clean; **9/9** truth-table for the LCL filter predicate. **No `multiply_shared.js` change → stays `?v=4`.**

### **Deploy notes for Session 29:** `member_tool.html` + `attendance_admin.html` are live (Pages auto-deploys); hard-refresh / relaunch PWA; phone smoke-test (Inv #31). SQL (`001_add_tenancy.sql`) already run + verified by the Pastor; `001_rollback.sql` is on standby, NOT run.

---

## ✅ OPERATIONAL — JUNE 4, 2026 (Claude Code migration + repo housekeeping)

No platform-code / feature changes — operational + housekeeping only.
- **Added `CLAUDE.md` project memory** for Claude Code (`005dbdb`).
- **Added `.gitattributes`** — `* text=auto eol=lf`, explicit `eol=lf` for `html`/`js`/`ts`/`json`/`md`/`svg`/`css`/`sql`, and `binary` for images/`pptx`/`pdf`/fonts; `git add --renormalize` confirmed the index was already **pure LF** (`39289bb`). See Inv #151.
- **Completed the `?v=4` lockstep** — bumped **ALL** shared.js consumers to `?v=4`: the **4 reports** (`v=2→4`) **plus 8 previously-UNVERSIONED** admin/quiz tools (`attendance_admin`, `btli_quiz_admin`, `btli_quiz_player`, `devotional_admin`, `lesson_quiz_editor`, `preaching_admin`, `transfer_management`, `usbong_quiz_player`) that carried a latent stale-cache risk; left the **self-inlined** `profile_results_viewer.html` alone (it does not load shared.js) (`cc328c4`).
- **Repo cleanup** — removed stale BTLI 101 folders `lessons/btli101` + `lessons/btli101_s1_hrwm68` (`616c072`), and two mistakenly-uploaded dupe files `member_tool (14).html` + `multiply_dashboard0.html` (`8f16665`).

---

## ✅ COMPLETED JUNE 3, 2026 — SESSION 28 (preaching swap + alert overhaul; attendance-save fix; MD bulk-enroll)

All logic `node --check`-verified and jsdom-tested (Inv #105); deployed files re-read before editing (Inv #56/#126). No SQL migrations this session.

### **1. MD batch bulk-enroll** (Inv #145) — `multiply_dashboard.html`
- Cohorts → 👥 Roster modal gained a **"⚡ Bulk add"** button. Intermediate modal: filter **Everyone / By Level** (multi-level checkboxes) **/ By LC Leader** (members whose `discipler_id` = the chosen facilitator; leader not auto-included) → a **name-checklist** (default checked; already-enrolled shown disabled with "✓ in batch"; test members + guests excluded) → role select (default participant) → **➕ Enroll N** = one `cohort_members` array insert. No `canEnroll`/Zone-1 gate (Pastor override, Inv #103); pacing still via Model-X lesson unlocks (#84). Cohort member SELECT widened with `discipler_id, is_facilitator, facilitator_role, is_test_member, is_external_user`. 19-point jsdom test green.

### **2. Preaching weekday-derivation + combined banner** (Inv #146, #147) — `multiply_shared.js`
- `wednesday_preaching` has no reliable `service_type` column, so the old code defaulted everything to "Wednesday." Now the service day (Sun=0…Wed=3) is derived from `new Date(preach_date+'T00:00:00').getDay()`; `service_type` is only an optional override. Sunday=morning, else=evening phrasing.
- `renderReminderBanner` now fetches `getUpcomingAllForMember(memberId, 7)` (dropped `.limit(1)` which hid a same-week second engagement). 1 live → single banner; 2+ → ONE **"Preaching This Week"** banner colored by the nearest, each engagement a row with its own 🔄 Swap, single ✕ dismisses all (`dismiss-all`). 7-day window is intentional. 15-point jsdom test green.

### **3. Consolidated swap-request modal** (Inv #148) — `multiply_shared.js` + MMT/MLT/calendar
- New single source `MultiplyShared.preaching.openSwapRequest(assignmentId, preachDate, requesterMemberId, {bilingual, onSent})` lives beside the banner. **"Swap from" picker** (own upcoming, shown only when 2+; default = tapped) + **"Swap with"** filtered ±6 weeks of the selected FROM (re-filters on FROM change). MMT (`MEMBER.memberId`, bilingual), MLT (`LEADER.leaderId`), and calendar (`onSent` → toast+reload) are now **thin wrappers**; the old page-local duplicates were removed. 20-point jsdom test green.

### **4. Calendar identity fix** (Inv #149) — `preaching_calendar.html` (+ openers)
- Root cause of "no Swap from the calendar": it read **`localStorage`** while the app stores sessions in **`sessionStorage`** → `isMine` always false → no "You", no swap buttons (Pastor's Sundays showed only "Mainstay"). Confirmed via console: both `leaderId`/`memberId` came back `undefined` (a session-less tab / wrong store). Fixed to read the shared session store, match a cell as "mine" against **either** the leader or member id, and accept a **`?me=<id>` URL fallback**. MLT's "Preaching Calendar" card and the banner's "View Calendar" now pass `?me=` (critical for a PWA that opens an external browser where `sessionStorage` doesn't carry). Unit test green.

### **5. Attendance save: batched + immediate feedback** (Inv #150) — `lc_leader_tool.html`
- LC `saveAttendance` had no in-progress button state and did **2 sequential awaited round-trips per member** (40–60 serial calls for a 20–30 roster ⇒ 15–30 s on mobile) — users assumed it had saved and left → no data. Now: **"⏳ Saving…"** disabled button on tap (double-tap-guarded, "✅ Saved!" on finish), and a **batched write** — ONE SELECT for existing rows, ONE bulk INSERT for new rows, parallel UPDATEs for the rest. Preserves the Pre-Pipeline present-only skip (#129) and event_name keying; still avoids DELETE (RLS); failure restores the button to allow retry. `saveMinistryAttendance` already used bulk upsert (untouched). 8-point jsdom test green.

### **Deploy order for Session 28:** no SQL. Push the 5 changed files **together** (all `multiply_shared.js?v=4`): `multiply_shared.js`, `lc_leader_tool.html`, `member_tool.html`, `multiply_dashboard.html`, `preaching_calendar.html`. Then **bump the remaining shared.js consumers to `?v=4`** (the four report HTMLs) so the browser caches a single shared.js. Hard-refresh / relaunch PWA; phone smoke-test (Inv #31). Smoke: MD Roster "⚡ Bulk add" by level/LCL with the checklist; the preaching banner shows a combined Wed+Sun when both are within a week; the swap modal shows a "Swap from" picker for a 2-engagement week; the calendar (opened from MLT and from the MMT banner) shows "You" + 🔄 on the Pastor's cells; attendance Save shows "⏳ Saving…" and finishes in ~1–2 s.

---

## ✅ COMPLETED JUNE 1, 2026 — SESSION 27 (fail-closed parity; ?v=2 standardization; faces everywhere; Love Language + Enneagram rebalance; Strengths review/fix; LC-Group field; superuser)

Fail-closed parity on both MMT render-layer eligibility catches (Inv #137). `multiply_shared.js?v=2` standardized across all 7 HTML with a bump-together convention (#138). Faces on every remaining avatar surface — MD modal, MMT celebration+LC, MLT listings (#139, extends #133). Love Language rebalanced (Physical Touch de-loaded to literal touch; Gifts dignified; scoring key byte-preserved) and Enneagram rebalanced (de-glow Type 1; dignify 8/7/9; 63 statements; key preserved), both de-branded (#140); `members.enneagram_type` is INTEGER (#141). Strengths reframed to a "Strengths Reflection" with disclaimer + Gallup link and a NEUTRAL (shuffle) tie-break replacing the alphabetical bias (#142). Late MD: an **LC-Group-name field** (Pastor/superuser + facilitator only, anti-drift guard — #143; fixes the LACR mislabel where a leader's `lc_group` held a person's name) and a **superuser predicate** `_isSuperuser()` = Pastor OR Level-5-with-no-LCL that exempts top-of-tree leaders from the self-edit lock (#144).

## ✅ EARLIER SESSIONS (condensed — full detail in git history + invariants)
- **S26:** Profile photos end-to-end (resize ≤512px JPEG → public `avatars` bucket, `add_member_photo_and_avatars_bucket.sql` run ONCE; DB holds only `photo_url`; initials base + photo overlay, #133/#134); celebration-feed expansion (streaks/perfect-attendance/birthdays, LCG-scoped); MLT batch rename (#132)/delete (#135)/Visible-to-others Pastor-only (#136).
- **S25:** Per-person Usbong unlock by attendance (#128), present-only attendance (#129), per-lesson roster (#130), lesson-unique patterns (#131).
- **S24:** Conflict Style + DISC rebalanced + de-branded (#123/#124); quiz option-key drift fix (#125); L9/L10 quiz rogue-schema rebuild; Usbong quiz Model-X lesson-lock (#127).
- **S23:** SVI Service split (#117), zone-constraint silent-write fix (#118), MD SVI Weights editor (#119), preaching-copy fix (#121), "Care Radar"→"Member Spiritual Vitality Report" (#122).
- **S22:** SVI engine + Care Radar + MAR honesty; private mentoring batches.
- **S12–S20:** BTLI L4–L10 builds; shared slides-nav module (#74); shared font slider (#80); favicons (#81); PPTX render-verify (#82); Model X lock (#84/#85); self-attest shared extraction (#91); weekly attendance gate (#100); MLT boot wall (#101).

---

## 📋 PENDING ITEMS

### **🏗️ Tenancy Phase 2 — RLS rollout (THE next major arc; mechanism proven on `debrief_records`)**
The full JWT→trigger→RLS chain is live and proven on one canary. Roll it out, table by table:
1. **Per-table RLS** — for each per-church table: (a) confirm EVERY read/write path carries the JWT (Inv #183) — anon paths get locked out; (b) backfill `church_id IS NULL` → the owning church; (c) 4 policies `TO authenticated` on `church_id = auth_church_id()`; (d) `ENABLE ROW LEVEL SECURITY`; (e) keep a one-line `DISABLE` rollback. **Test isolation via a browser/curl token — NEVER the SQL editor** (the postgres owner bypasses non-forced RLS).
2. **Assessment token-flow — DONE (S42).** `member_profiles`/`gifts_diagnostic` are RLS-live via the `token-login` JWT path (Inv #190–#195); all 7 assessments + `member_self_edit` + `bulk_send_links` are now JWT-attached (Inv #198/#200).
3. **`members` / login special case — REWIRE DESIGNED (S43, Inv #201); execute next, then enable `members` RLS (LAST table).** Login does a pre-auth anon `members` read + client-side bcrypt → incompatible with `members` RLS. `auth-login` is now the server-side gate (`search`/`login`/`set_pin`, Inv #199); the two own-anon `members` readers (`member_self_edit`, `bulk_send_links`) are CLOSED (Inv #198/#200); preaching-calendar `members` read confirmed already JWT-gated (S41). **Next:** rewire `member_login` + `leader_login` to the EF actions (+ a `stamp:'leader'` → `leader_last_login` EF add, redeploy), smoke both modes (returning login + first-login `set_pin`), **THEN enable `members` RLS** (allow-all-stub scan first, Inv #186).
4. **SECURITY DEFINER view audit (`TENANCY_AUDIT.md` §5)** — `svi_snapshots`, `interventions`, `ministry_intake`, `svi_metrics`, `svi_weight_profiles`, `view_log` are reached via owner-privileged views that **bypass RLS** → church-scope them (or they leak across churches).
5. **SVI EF (`compute-svi-weekly`)** writes `svi_snapshots` as service-role (no claim) → must **set `church_id` per snapshot** from each member's church.
6. **Agape onboarding M1/M2** — the Agape pastor + his guest members currently live in the Rosehill DB as guests (`is_external_user=true`). **M1 (safe anytime):** set `church_id=Agape` on the Agape pastor + downline (tree via `discipler_id`) + their per-church rows, **KEEP the guest flag** (it keeps them out of Rosehill stats meanwhile — the bridge). **M2 (ONLY after their tables' RLS is live):** flip `is_external_user=false` → full Agape members. Diagnostic-first + rollback. Doing M2 before church-scoping would leak them into Rosehill stats.

### **✏️ MLT 'My Profile' tile** (branch `mlt-my-profile`) — `lc_leader_tool.html`
- Add a first **Quick-Action tile** (✏️ 'My Profile' / 'Aking Profile') in MLT's `screenHome` → `openMyProfile()` resolves the leader's own member id → `createSelfEditToken()` → opens **`member_self_edit.html?token=…`** in a new tab (reuse the existing self-edit page; do **not** port MD's modal). Pastor-managed fields stay locked (confirm the lock list). Prompt delivered earlier; **not confirmed shipped**.

### **🔝 BTLI L12 — Understanding My Unique Design** (LEAD — the next real build)
- **Effort:** Large (full 8-deliverable lesson). **Status:** Ready — needs the **UNLAD-L2 scan** uploaded.
- Source UNLAD-L2; MV **Ephesians 2:10**; character (proposed) Goodness/Faithfulness — confirm from source (Inv #75). **THE assessment-battery lesson** — build the sequence to take all 6 MULTIPLY assessments (Spiritual Gifts, Strengths, DISC, Enneagram, Love Language, Salvation) into the participant page. Direct build (no Gemini). Library seed FIRST then quiz seed (Inv #84); render-verify PPTX (#82); favicon+slider (#80/#81); browser-verify all (#80).

### **🙏 Prayer / Panalangin — W3/W4 HELD until Pastor's go** (Inv #165)
Wave 1 (MMT board, S35) + Wave 1 bugfixes + **Wave 2 (encouragement) SHIPPED S36** (`7daca68`). Remaining, on the Pastor's word:
1. **W3 — reminders + lifecycle:** prayer reminders, EOLO-name seeding into requests, auto-expiry via `expires_at`.
2. **W4 — silent SVI:** a prayer SVI category (`count_rows`) — **invisible to members** (gamification stays hidden; SVI factors it). LOWERCASE category per Inv (svi_metrics check).
- **Phase-2 RLS (cross-member writes):** Wave 2's intercession insert, care flag + `prayer_care` notification, and answer→saver propagation write OTHER members' rows — fine now (RLS disabled) but must become **`SECURITY DEFINER` RPCs** under Phase-2 RLS.
- **Phase B carryover for prayer:** centralize the kind-bubble `sameKind` helper into `multiply_shared.js` (currently local to the MMT prayer block) — fold into the kind-mirroring Phase B below and bump `?v=`.

### **🪞 Kind-mirroring — Phase B** (Inv #164)
Phase A (S34) covered MD + MLT. Phase B: (1) make the **report HTMLs** viewer-aware (`memberKind === viewerKind`), retiring the strict Inv #58 predicate; (2) **centralize** the mirrored helpers (`memberKind`/`viewerKind`/`_kindVisible`/`_sameKind`) + the prayer `sameKind` into `multiply_shared.js` and **bump `?v=` lockstep** (Inv #138). ~30 min per report.

### **✅ lc_group decommission (Inv #166) — COMPLETE (S35–S37)**
Canonical grouping/matching/display = `discipler_id`/`facilitator_id` → live record; `lc_group` + all stored text mirrors are decorative.
- **Tiers 1–4 ✅ (frontend):** attendance reports (`e23554f`, `bca7871`, Inv #167) · audit + `diag_lc_group_views.sql` (`ba8d647`) · announcements `lcl:` keys (`e889be1`) · absence-notice own-disciples (`dc15c8f`) · transfer-state + MMT LC-family (`0dfc641`).
- **SQL DONE (S37):** **`migrations/004` RUN** (promote_pending_lc_transfers now moves `facilitator_id`/`discipler_id`, not just text). **`005` RUN** (6 dormant lc_group indexes dropped). **`006` REMOVED** — the 3 target views never existed (confirmed live: `pg_views` 0 rows, `pg_class`/`pg_namespace` no relation). Phantom from a schema-dump name-list comment.
- **Transfer mis-attribution check (S37 — no remediation):** `discipler_name`-vs-`discipler_id` diagnostic returned 2 rows (Cayabo, Ambrocio) = a leader-rename artifact (Melvin → Choti Aquino; `discipler_id` correct, stored text stale), NOT a buggy promote. No past damage; nothing to fix.
- **Generalized rule (Inv #166 strengthened):** ALL denormalized text mirrors (`discipler_name`/`member_name`/`facilitator_name`/lc_group family/…) are decorative — resolve `id` → current record, never stored text.
- **Optional / deferred (cosmetic, human-gated — NOT required):**
  1. **`migrations/007_refresh_stale_discipler_names.sql`** — one-shot sync of `discipler_name` → current leader name (clears the 2 stale rows + any future ones). At the Pastor's discretion; no rollback (syncs to truth).
  2. **Tier 4C** — `index.ts` writes `lc_group` into `svi_snapshots` (passenger; `v_svi_latest` keys on `member_id`). Switch → `discipler_id` only if full purity wanted; needs `supabase functions deploy compute-svi-weekly`.
  3. **Display note (low priority):** surfaces still showing a stored text name (e.g. cards rendering `discipler_name`) should resolve `id` → current name — same principle, polish only.

### **🔧 Infra — cloud-session write access (durable fix)**
Laptop **cloud** sessions are read-only (git push 403 + MCP 403 + no signing key) — work had to ship via `git format-patch` → apply/push from the desktop. **Durable fix:** grant the **Claude GitHub App** write on `gejable1/multiply` (Contents + Pull requests) so future cloud sessions push directly. Standing workflow either way: **pull-first; ask before push**; the desktop clone is the preferred push surface.

### **🏗️ Multi-tenant — Phase 2 (next major arc)** (Inv #153–#157)
Phase 1 foundation is done (S29): `churches` + nullable `church_id` on 41 tables, Rosehill backfilled. Phase 2, in order:
1. **Auth Edge Function** — PIN → **church-scoped JWT** (server-trusted `church_id` claim).
2. **App sets `church_id` on every insert** (clears the NULL trickle, Inv #155).
3. **RLS policies** on the 40 per-church tables + the `devotionals` hybrid (NULL=shared).
4. **NOT NULL tightening migration** on `church_id` once inserts populate it (Inv #154).
5. **SECURITY DEFINER view/function audit** — church-scope them (`TENANCY_AUDIT.md` §5: they bypass RLS and would leak across churches).
6. **Super-admin layer** → onboarding UI + per-church branding/config → **pilot one church**.

### **👀 Watch-item: leader on-site log can clobber a member self-attest present** (flip side of the S29 fix)
- The `UNIQUE(member_id,event_type,event_date)` row is shared, so a leader logging on-site **after** a member self-attested present could overwrite that present. Separate future task: leader-log should **not** clobber an existing member `self_attest` present. Not urgent — logged so it isn't lost.

### **✅ Self-attest PAST-date correction — DONE (S33, Option A, `b4b792f`, Inv #163)**
- Shipped: correcting an old leader-marked absence works at any age (dup-check before the window); brand-new claims stay 7-day-gated; native date picker added.

### **🧹 One-time backfill: flag pre-existing guest-added members** (offered S33; not run)
- Inv #162's inheritance is **forward-only**. Members guests added **before** `4c48e83` are still unflagged in Rosehill's numbers. A human-gated, idempotent SQL can flag any member whose **discipler is a guest/test user** (and isn't already flagged), with a read-only diagnostic first + a rollback (Inv #153/#157). Pastor to confirm before running.

### **🧹 Optional tidy from S28**
- **Confirm preaching-row ids** all use the canonical Gerry id (`547ebda6-5126-436e-9890-709f50588ced`) — the `?me=` + both-ids fix made the calendar robust regardless, so this is cosmetic.
- **Calendar dead code:** the now-unused static `#swapModal` markup + `submitSwap`/`closeSwapModal` in `preaching_calendar.html` can be removed whenever convenient (harmless if left).

### **🔁 Assessment re-pilots** (Inv #97/#140 — await member retakes; not actionable until then)
- After members **retake**, re-run each distribution query and confirm the spread widened: **Conflict Style** (no mode avg > ~7, no primary > ~30–35%); **DISC** (D toward 10–20%+, avg_total < ~140); **Love Language** (no language > ~30–35%; Physical Touch falls from 36.5%; Gifts climbs from 4.8%); **Enneagram** (Type 1 falls from 34.8%; 8/7/9 climb — read small cells with care); **Strengths** (neutral tie-break — fairer, not Gallup-grade by design).

### **SVI engine** — ✅ DEPLOYED (S23). Future recomputes still **dry-run first** (`?dry_run=true&member_id=…`, Inv #112).

---

## 📋 PARKED ITEMS
- **Year-One Cohort Evaluation** — ~May 2027 (first formal earning cycle for L2+, Inv #47).
- **Strengths follow-ups** (#142): deepen resolution (more pairs / Likert pass — LARGE, diminishing returns, still not Gallup-grade); a dashboard manual-entry field for official Gallup Top-5 (`strengths_top5` column exists). Parked.
- **Other reports — viewer-aware test containment review** — ~30 min each when next opened (keep strict Rule #58 until reviewed). Low payoff.
- **L1–L6 slides nav retrofit** to shared module — optional (Inv #74).
- **First Usbong batch lifecycle hygiene** — demote/delete any duplicate `forming` batch for "New & Pre-believer's Class."
- **Diagnose RLS policies file** — run `diagnose_rls_policies.sql` to completion; keep if load-bearing, drop if vestigial (Inv #10).
- **Discipler text-fallback tidy in MD** — legacy decorative `discipler` text column.
- **LC meeting-day/time feature** — designed but the `lc_groups` table was **never created** (live-DB verified 2026-06-04: no such relation in any schema). `member_tool.html:5986` is a **fail-soft dead reference** (returns null → skipped; LC card's meeting day never populates). Decide later: build `lc_groups` as a **PER-CHURCH** table within/after the tenancy migration, or remove the dead call. **Left as-is for now.**
- **`_backup_members_diag_zone_2026_05_06`** — backup/temp table (57 rows), **excluded** from tenancy; **drop candidate** once confirmed unused (Inv #156).
- **`leader_sessions` retention cleanup** — the audit table grows unbounded (1936 rows by S28); add a retention/prune policy someday. Low priority.
- **`CC_SETUP.md`** — untracked file sitting in the repo root all session; **decide: commit or delete** (it keeps showing in `git status`).
- **`ministry_roles`** — confirmed **PER-CHURCH** (carries `church_id` since S29); no action, noted for completeness. Low concern.

---

## 🧠 GEMINI SUBCONTRACTING — DECISION RECORD
**Decision (S13, confirmed S14–S20):** Stop subcontracting BTLI lesson builds to Gemini (~25% salvage, ~2× longer, unrecoverable drift). Direct build with Claude is ~2× faster for 100% retention; the non-skippable browser-verification step (Inv #80) closes the silent-bug failure mode. Keep Gemini only for parallel emergencies / custom non-USAD-UNLAD topics.

---

*"A student who is fully trained will be like their teacher." — Luke 6:40*
*Session 38 was about making the numbers tell the truth out loud. The vitality report stopped hiding its arithmetic — every score now shows its work, score times weight, summed and divided, so a pastor can see exactly why a soul reads the way it does. The dashboard, which had been squinting at an empty name-tag inside its own iframe, finally heard who it was talking to and opened the whole flock to its shepherd. The scoring learned to honor the small faithful gathering of an LC, and to be gentle with a week no one could meet. And the pulpit calendar learned to count forward from where the schedule actually ends, never losing a Sunday to a shrinking window. One thing still waits: the engine that computes it all is too slow to finish — 231 souls, 2,500 little questions, and a 150-second door that keeps closing. The next session teaches it to ask everything once, all at the start, and then think in silence. "Let all things be done decently and in order." — and then, BTLI L12, where the disciple turns the honest mirrors inward and meets the poiema God actually made (Eph 2:10).* ✦
